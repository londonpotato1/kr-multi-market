# v0.4.1 — Realtime Tuning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.4.1 of `kr-multi-market` — 가격 실시간성 튜닝 (A: HL WebSocket env + B: SWR 5s→2s + C: source 별 독립 TTL + D: Naver 장중 2s / 휴장 7s) + premium skew guard.

**Architecture:** 6-Task split. Wave 1 (source-cache.ts) → Wave 2A (skew guard, safe merge first) → Wave 2B (cache split + Codex #1) → Wave 3 (SWR client) → Wave 4 (README + .env.example) → Wave 5 (Codex #2 final). 2 Codex checkpoints + 카파시 grep.

**Tech Stack:** Vite 8 + React 19 + Express 4 + Vitest 4. 변경 파일 8개 이내, 새 파일 1개 (`source-cache.ts`).

**Spec reference:** `(spec)/2026-05-12-v0.4.1-realtime-tuning-design.md` (commit `b40068f`).

**Path 단축**: `(spec)` = `docs/superpowers/specs`, `(plan)` = `docs/superpowers/plans`.

---

## File Inventory

```
server/lib/source-cache.ts        [CREATE] SOURCE_TTL_MS + naverTtl()
server/lib/normalize.ts           [MODIFY] computePremiumWithSkew 추가
server/lib/assemble.ts            [MODIFY] computePremium → computePremiumWithSkew (W2A),
                                            source 별 singleFlight 통합 (W2B)
server/index.ts                   [MODIFY] singleFlight 단일 호출 → source 별 분리 (W2B),
                                            Cache-Control s-maxage 4 → 2 (W3)
src/hooks/usePrices.ts            [MODIFY] refreshInterval 5000 → 2000, dedup 4000 → 1500
.env.example                      [MODIFY] HL_USE_WS 권장 주석
README.md                         [MODIFY] 갱신 주기 표

server/tests/source-cache.test.ts [CREATE]
server/tests/premium-skew.test.ts [CREATE]
```

---

## Agent Workflow

| Task | 1차 agent | 검증 | Codex |
|---|---|---|---|
| 1 source-cache | coder TDD | tester | — |
| 2A skew guard | coder TDD | tester | — |
| 2B cache split | coder | code-reviewer | **Codex #1** (assemble integration + race condition) |
| 3 SWR client | bash 직접 | quick-validator | — |
| 4 README+env | bash 직접 | — | — |
| 5 smoke | bash + 사용자 | — | **Codex #2** (final v0.4.1) |

### 카파시 prompt template (의무 주입)

```
[카파시 스타일 / AI slop 금지 — 의무 검증]
- 신규 파일은 source-cache.ts 1개만
- 허용 helper: naverTtl(), computePremiumWithSkew (그 외 X)
- 허용 상수: SOURCE_TTL_MS, MAX_PREMIUM_SKEW_MS
- HL WS 코드 신규 작성 X (기존 hyperliquid-ws.ts reuse)
- "혹시 나중에" 보존 코드 X
```

---

# Task 1: source-cache.ts 신규 (TDD)

**Files:**
- Create: `server/lib/source-cache.ts`
- Create: `server/tests/source-cache.test.ts`

- [ ] **Step 1: Failing tests**

`server/tests/source-cache.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest';
import { SOURCE_TTL_MS, naverTtl } from '../lib/source-cache.js';
import * as session from '../lib/session.js';

describe('SOURCE_TTL_MS', () => {
  it('defines TTL for hyperliquid/binance/upbit/yahoo', () => {
    expect(SOURCE_TTL_MS.hyperliquid).toBe(1000);
    expect(SOURCE_TTL_MS.binance).toBe(1000);
    expect(SOURCE_TTL_MS.upbit).toBe(2000);
    expect(SOURCE_TTL_MS.yahoo).toBe(5000);
  });
});

describe('naverTtl', () => {
  it('returns 2000 during KRX open session', () => {
    vi.spyOn(session, 'getSessionState').mockReturnValue({
      krx: true, krxAfter: false, krxNight: false,
      nyseRegular: false, nysePrePost: false, cme: true,
      hyperliquid: true, binance: true,
    });
    expect(naverTtl()).toBe(2000);
    vi.restoreAllMocks();
  });

  it('returns 7000 when KRX closed', () => {
    vi.spyOn(session, 'getSessionState').mockReturnValue({
      krx: false, krxAfter: true, krxNight: false,
      nyseRegular: false, nysePrePost: false, cme: true,
      hyperliquid: true, binance: true,
    });
    expect(naverTtl()).toBe(7000);
    vi.restoreAllMocks();
  });
});
```

- [ ] **Step 2: Run — Expected FAIL**

```bash
pnpm test -- server/tests/source-cache.test.ts
```
Expected: "Cannot find module '../lib/source-cache'"

- [ ] **Step 3: Implementation**

`server/lib/source-cache.ts`:

```ts
import { getSessionState } from './session.js';

// Source 별 cache TTL (ms) — Naver 는 동적 (naverTtl() 참조).
export const SOURCE_TTL_MS = {
  hyperliquid: 1000,
  binance: 1000,
  upbit: 2000,
  yahoo: 5000,
} as const;

// Naver: KRX 장중 2s / 휴장 7s (spec §2.4 D).
// 7s 는 Naver 응답의 권장 pollingInterval.
export function naverTtl(): number {
  return getSessionState().krx ? 2000 : 7000;
}
```

- [ ] **Step 4: Run — Expected PASS**

```bash
pnpm test -- server/tests/source-cache.test.ts
```
Expected: 3 tests pass.

- [ ] **Step 5: Commit**

```bash
git add server/lib/source-cache.ts server/tests/source-cache.test.ts
git commit -m "feat(server): add source-cache.ts (SOURCE_TTL_MS + naverTtl) (W1)"
```

---

# Task 2A: computePremiumWithSkew (TDD, safe merge)

**Files:**
- Modify: `server/lib/normalize.ts` (computePremiumWithSkew 추가)
- Modify: `server/lib/assemble.ts` (computePremium 호출 → computePremiumWithSkew)
- Create: `server/tests/premium-skew.test.ts`

> 이 Task 후 머지 안전 — single 4s cache 상태에서 source asOf 거의 동일 → skew <1s → guard 항상 통과 → 시각 회귀 0.

- [ ] **Step 1: Failing tests**

`server/tests/premium-skew.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { computePremiumWithSkew } from '../lib/normalize.js';
import type { PricePoint, FxRates } from '@shared/types/prices.js';

const now = Date.now();
const fxOk: FxRates = { officialUsdKrw: 1471.7, usdtKrw: 1470, divergencePct: 0 };
const mkPP = (price: number, asOf: number, source: PricePoint['source'] = 'hyperliquid'): PricePoint => ({
  source, symbol: '', price, unit: 'USD', status: 'ok',
  asOf, receivedAt: now, schemaVersion: 1,
});

describe('computePremiumWithSkew', () => {
  it('returns normal premium when skew within 5s', () => {
    const hl = mkPP(189, now);
    const naver = mkPP(279000, now - 3000, 'naver'); // 3s skew
    const result = computePremiumWithSkew(hl, naver, fxOk);
    expect(result.pctUsd).not.toBeNull();
    expect(result.guard).toBe('normal');
  });

  it('returns null + guard=warn when skew exceeds 5s', () => {
    const hl = mkPP(189, now);
    const naver = mkPP(279000, now - 6000, 'naver'); // 6s skew
    const result = computePremiumWithSkew(hl, naver, fxOk);
    expect(result.pctUsd).toBeNull();
    expect(result.guard).toBe('warn');
  });

  it('returns null premium when hl undefined', () => {
    const naver = mkPP(279000, now, 'naver');
    const result = computePremiumWithSkew(undefined, naver, fxOk);
    expect(result.pctUsd).toBeNull();
  });

  it('returns null premium when naver undefined', () => {
    const hl = mkPP(189, now);
    const result = computePremiumWithSkew(hl, undefined, fxOk);
    expect(result.pctUsd).toBeNull();
  });

  it('boundary: skew = exactly 5000ms → guard=warn (strict >)', () => {
    // spec §2.5: skewMs > MAX_PREMIUM_SKEW_MS → warn
    // 정확히 5000 일 때는 정상 (strict >)
    const hl = mkPP(189, now);
    const naver5000 = mkPP(279000, now - 5000, 'naver');
    expect(computePremiumWithSkew(hl, naver5000, fxOk).guard).toBe('normal');
    const naver5001 = mkPP(279000, now - 5001, 'naver');
    expect(computePremiumWithSkew(hl, naver5001, fxOk).guard).toBe('warn');
  });
});
```

- [ ] **Step 2: Run — Expected FAIL**

```bash
pnpm test -- server/tests/premium-skew.test.ts
```

- [ ] **Step 3: Implementation — normalize.ts 에 추가**

`server/lib/normalize.ts` 끝에 추가:

```ts
import type { PricePoint, FxRates, Premium } from '@shared/types/prices.js';

// (computePremium 은 기존 export. import + re-export 안 함, 직접 사용)

// spec §2.5: HL vs Naver fetch 시점 격차 > 5s 시 premium 신뢰성 낮음 → null + guard='warn'
export const MAX_PREMIUM_SKEW_MS = 5000;

export function computePremiumWithSkew(
  hl: PricePoint | undefined,
  naver: PricePoint | undefined,
  fx: FxRates
): Premium {
  if (!hl || !naver) {
    return { pctUsd: null, pctUsdt: null, guard: 'normal' };
  }
  const skewMs = Math.abs(hl.asOf - naver.asOf);
  if (skewMs > MAX_PREMIUM_SKEW_MS) {
    return { pctUsd: null, pctUsdt: null, guard: 'warn' };
  }
  return computePremium(hl.price, naver.price, fx);
}
```

> ⚠️ normalize.ts 의 기존 `computePremium(hlPrice, naverPrice, fx)` 시그니처는 그대로. wrapper 가 PricePoint 받음.

- [ ] **Step 4: assemble.ts 호출처 교체**

기존 `computePremium(t.hl.price, t.naver?.price, fx)` 패턴 (assemble.ts:229 근방) → `computePremiumWithSkew(t.hl, t.naver, fx)`.

기존 코드 (~226-230 라인):
```ts
  for (const tickerKey of ['samsung', 'skhynix', 'hyundai']) {
    const t = tickers[tickerKey];
    if (!t?.hl) continue;
    const premium: Premium = computePremium(t.hl.price, t.naver?.price, fx);
    // ...
  }
```

변경 후:
```ts
  for (const tickerKey of ['samsung', 'skhynix', 'hyundai']) {
    const t = tickers[tickerKey];
    if (!t?.hl) continue;
    const premium: Premium = computePremiumWithSkew(t.hl, t.naver, fx);
    // ...
  }
```

`computePremium` import → `computePremiumWithSkew` import 추가 (또는 둘 다).

- [ ] **Step 5: Run — Expected PASS**

```bash
pnpm test -- server/tests/premium-skew.test.ts
pnpm test  # 전체
```
Expected: 모두 통과 (skew test 5개 + 기존 통합 테스트 영향 없음 — 단일 4s cache 에서 skew <1s).

- [ ] **Step 6: Commit**

```bash
git add server/lib/normalize.ts server/lib/assemble.ts server/tests/premium-skew.test.ts
git commit -m "feat(server): add computePremiumWithSkew (5s threshold, safe merge) (W2A)"
```

---

# Task 2B: server/index.ts singleFlight 분리

**Files:**
- Modify: `server/index.ts` (single `'prices'` key → 5개 source key)
- Modify: `server/lib/assemble.ts` (source 분리 결과 받기)

- [ ] **Step 1: 현재 server/index.ts 핵심 부분 확인**

```bash
sed -n '85,115p' server/index.ts
```

기존 (예상):
```ts
const response = await singleFlight<PricesResponse>('prices', 4000, async () => {
  const [hl, naver, yahoo, upbit, binance] = await Promise.all([
    fetchHyperliquid(),
    fetchNaver(),
    fetchYahoo(),
    fetchUpbit(),
    fetchBinanceFutures(),
  ]);
  return assemblePricesResponse({ hl, naver, yahoo, upbit, binance });
});
```

- [ ] **Step 2: source 별 singleFlight 분리**

`server/index.ts` 의 위 블록을 다음으로 교체:

```ts
import { SOURCE_TTL_MS, naverTtl } from './lib/source-cache.js';

// ... 기존 imports + handler ...

const [hl, naver, yahoo, upbit, binance] = await Promise.all([
  singleFlight('source:hyperliquid', SOURCE_TTL_MS.hyperliquid, fetchHyperliquid),
  singleFlight('source:naver',       naverTtl(),                  fetchNaver),
  singleFlight('source:yahoo',       SOURCE_TTL_MS.yahoo,         fetchYahoo),
  singleFlight('source:upbit',       SOURCE_TTL_MS.upbit,         fetchUpbit),
  singleFlight('source:binance',     SOURCE_TTL_MS.binance,       fetchBinanceFutures),
]);
const response = assemblePricesResponse({ hl, naver, yahoo, upbit, binance });

if (HL_USE_WS) {
  // 기존 HL WS 덮어쓰기 로직 그대로
  // ...
}
```

> ⚠️ `singleFlight<T>` 의 인자 시그니처: `(key: string, ttlMs: number, factory: () => Promise<T>)`. 각 fetchSource() 가 `Promise<Result<PricePoint[]>>` 반환. 기존 cache.ts API 변경 없음.

- [ ] **Step 3: tsc 검증**

```bash
pnpm exec tsc --noEmit -p server/tsconfig.json
```
Expected: clean.

- [ ] **Step 4: 전체 테스트**

```bash
pnpm test
```
Expected: 모두 통과 + skew test 가 source 분리 후에도 정상 작동 (장중/마감 시 skew <5s 유지).

- [ ] **Step 5: Commit**

```bash
git add server/index.ts
git commit -m "refactor(server): split singleFlight per source (HL=1s/Binance=1s/Upbit=2s/Yahoo=5s/Naver dynamic) (W2B)"
```

- [ ] **Step 6: Codex #1 체크포인트 (Wave 2B 종료)**

```
Agent: codex:codex-rescue
prompt: |
  Mode: REVIEW ONLY. Wave 2B of v0.4.1 PLAN complete.

  Files changed (this Wave):
  - server/index.ts (singleFlight single → 5 source-keyed calls)
  - server/lib/assemble.ts (assemble result reception — no signature change)

  Verify:
  1. 5 singleFlight 호출이 동시 (Promise.all) 작동 — race condition 없음
  2. naverTtl() 매 요청 호출 — overhead acceptable
  3. cache key 충돌 없음 ('source:naver' vs old 'prices')
  4. Wave 2A 의 computePremiumWithSkew 가 source 분리 후 정상 작동 (skew <5s 케이스)
  5. HL WS 모드 (HL_USE_WS=true) 시 server/index.ts 의 WS 덮어쓰기 분기가 source 분리 후에도 작동
  6. 응답 시간 측정 — source 5개 별도 cache hit 시 응답 빠른지 (네트워크 fetch 없음)

  [카파시 의무 검증]
  - 신규 helper 추가 없는지
  - 1-use 변수 inline 가능한지

  Output: BLOCK / FLAG / PASS.
```

Expected: PASS. BLOCK 시 inline fix + 재호출.

---

# Task 3: SWR client + Cache-Control 단축

**Files:**
- Modify: `src/hooks/usePrices.ts`
- Modify: `server/index.ts` (Cache-Control header)

- [ ] **Step 1: SWR interval 변경**

`src/hooks/usePrices.ts` 의 useSWR 옵션 변경:

```ts
// before
useSWR(..., {
  refreshInterval: 5000,
  dedupingInterval: 4000,
});

// after
useSWR(..., {
  refreshInterval: 2000,
  dedupingInterval: 1500,
});
```

- [ ] **Step 2: Cache-Control 변경**

`server/index.ts` (line 114 근방):
```ts
// before
res.setHeader('Cache-Control', 's-maxage=4, stale-while-revalidate=10');
// after
res.setHeader('Cache-Control', 's-maxage=2, stale-while-revalidate=8');
```

- [ ] **Step 3: 빌드 + 테스트**

```bash
pnpm exec tsc --noEmit -p tsconfig.app.json
pnpm exec tsc --noEmit -p server/tsconfig.json
pnpm test
```

- [ ] **Step 4: Commit**

```bash
git add src/hooks/usePrices.ts server/index.ts
git commit -m "feat(client/server): SWR 5s→2s + Cache-Control s-maxage 4→2 (W3)"
```

---

# Task 4: README + .env.example

**Files:**
- Modify: `README.md` (갱신 주기 표)
- Modify: `.env.example` (HL_USE_WS 권장 주석)

- [ ] **Step 1: .env.example 갱신**

`.env.example` 의 `HL_USE_WS` 라인 (또는 신규) 추가/갱신:

```
# Hyperliquid WebSocket (allMids subscription) — 실시간 가격 (1초 미만)
# default: false (REST polling). realtime 권장: HL_USE_WS=true
HL_USE_WS=false
```

- [ ] **Step 2: README 갱신**

`README.md` 의 Architecture 또는 별도 "Refresh rate" 섹션에:

```markdown
## Price refresh rate (v0.4.1)

| Source | TTL | Note |
|---|---|---|
| Hyperliquid (HL) | 1s (WS) / 1s (REST cache) | `HL_USE_WS=true` 권장 |
| Binance Futures | 1s | |
| Upbit | 2s | |
| Yahoo Finance | 5s | rate limit 보호 |
| Naver | 장중 2s / 휴장 7s | KRX session 동적 |
| SWR client refresh | 2s | server cache 통해 갱신 |

Premium guard: HL ↔ Naver 시점 격차 > 5s 시 premium null + guard='warn' (시각상 N/A).
```

- [ ] **Step 3: Commit**

```bash
git add README.md .env.example
git commit -m "docs(readme): update refresh rate table + HL_USE_WS guidance (W4)"
```

---

# Task 5: Smoke + Codex #2 final

**Files:** (검증 only)

- [ ] **Step 1: 전체 검증**

```bash
pnpm build
pnpm exec tsc --noEmit -p tsconfig.app.json
pnpm exec tsc --noEmit -p server/tsconfig.json
pnpm test
```
Expected: 모두 통과.

- [ ] **Step 2: dev 서버 시작 후 smoke (사용자 수동)**

```bash
pnpm dev
```
브라우저 http://localhost:5173:
1. 우상단 ↻ 타임스탬프가 ~2초마다 갱신
2. 다른 콘솔 탭에서: `curl -s http://localhost:3001/api/prices | jq '.tickers.samsung.naver.asOf'` 두 번 → 장중이면 ~2초 차이
3. `.env.local` 에 `HL_USE_WS=true` 추가 후 재시작 → HL 가격이 1초 미만 변동
4. KRX 장중 (09:00-15:30 KST) 에 Naver 응답 confirm (현재 시각 의존)

- [ ] **Step 3: Codex #2 final review**

```
Agent: codex:codex-rescue
prompt: |
  Mode: REVIEW ONLY. Pre-merge final v0.4.1 review.

  Spec: (spec)/2026-05-12-v0.4.1-realtime-tuning-design.md
  Plan: (plan)/2026-05-12-v0.4.1-realtime-tuning.md

  Verify spec §6 검증 기준 7 items:
  1. pnpm build 통과
  2. pnpm test 통과 (source-cache + premium-skew + 기존 모두)
  3. SOURCE_TTL_MS 5 entries 정의
  4. naverTtl() 장중/휴장 분기
  5. SWR refreshInterval=2000, dedupingInterval=1500
  6. Cache-Control s-maxage=2
  7. README + .env.example 갱신

  Plus:
  - computePremiumWithSkew 가 모든 호출처에서 사용 (assemble.ts samsung/skhynix/hyundai 루프)
  - HL WS asOf 가 PricePoint 에 정상 반영 (HL_USE_WS=true 시)
  - server cache 분리 후 race condition 없음

  [카파시 의무 검증]

  Output: BLOCK / FLAG / PASS.
```

Expected: PASS. BLOCK 시 fix + 재호출.

- [ ] **Step 4: 최종 commit + 사용자에게 머지 결정 위임**

```bash
git log --oneline b40068f..HEAD | head -10
# v0.4.1 commit 목록 확인
```

PR 또는 main merge 결정은 사용자 위임 (본 plan 범위 밖).

---

## Wave 종료별 검증 매트릭스

| Task | 종료 | build | test | Codex |
|---|---|---|---|---|
| 1 source-cache | TDD 끝 | ✓ | +3 tests | — |
| 2A skew guard | TDD 끝 | ✓ | +5 tests | — |
| 2B cache split | refactor 끝 | ✓ | 기존 + skew 정상 | **#1** |
| 3 SWR client | 1-line 변경 | ✓ | 기존 그대로 | — |
| 4 README + env | 문서 | ✓ | 기존 그대로 | — |
| 5 smoke | final | ✓ | + 사용자 smoke | **#2** |

---

## Open Decisions

1. **새 브랜치 vs 현재 (`feature/v0.4.0-beginner-friendly`)**: v0.4.0 와 v0.4.1 모두 미머지. 같은 브랜치에 쌓을지 새 브랜치 (`feature/v0.4.1-realtime-tuning`) 분리할지. 추천: **같은 브랜치** (v0.4.0 + v0.4.1 통합 PR — v0.4.1 가 v0.4.0 의 server data 흐름 위에서 작동).
2. **Execution mode**: subagent-driven (per-task fresh subagent + Codex 2회) vs inline batch.
3. **HL_USE_WS 활성화 시점**: 사용자가 `.env.local` 에 `HL_USE_WS=true` 추가는 PR 머지 후 vs Wave 5 smoke 단계? 추천: Wave 5 smoke 단계에서 사용자 시험.

---

## References

- Spec v3: `(spec)/2026-05-12-v0.4.1-realtime-tuning-design.md` (commit `b40068f`)
- v0.4.0 spec/plan: `(spec)/2026-05-12-v0.4.0-beginner-friendly-design.md`, `(plan)/2026-05-12-v0.4.0-beginner-friendly.md`
- 3 Codex reviews resolved: #1 BLOCK→fix, #2 BLOCK→fix, #3 FLAG→fix
- 사용자 결정: A+B+C+D (all), Naver = (d) 장중 2s / 휴장 7s
