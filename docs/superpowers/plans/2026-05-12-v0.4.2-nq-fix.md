# v0.4.2 — Nasdaq 100 데이터 누락 fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Nasdaq 100 (`nq`) 카드의 빈 가격 ("—") 문제를 다중 source redundancy (Bybit + Bitget + Yahoo Finnhub fallback + Binance retry + 옵셔널 Polygon/TwelveData) 와 NQ unit 정규화 (모든 source = QQQ ETF USD) 로 해소한다.

**Architecture:** server source layer 에 신규 2 (Bybit, Bitget) + 옵셔널 2 (Polygon, TwelveData) source 를 추가하고, Yahoo 에 Finnhub fallback 분기 + Binance 에 재시도 wrapper 를 삽입한다. assemble.ts 의 NQ mapping 을 QQQ ETF symbol 로 통일하여 단위 일관성 확보. Frontend IndexCompactCard 는 7 source 우선순위 chain 으로 headline 산출, 5개 VenueRow 렌더링 (Polygon/TwelveData 는 env 활성 시에만 headline 기여).

**Tech Stack:** TypeScript 6 strict, Express 4, Vitest 4, React 19, SWR 2, vitest happy-dom.

**Spec reference:** `docs/superpowers/specs/2026-05-12-v0.4.2-nq-fix-design.md` (v3.1, commit `a333557`).

**카파시 / AI slop 가드 (모든 task 에 적용)**:
- 새 source 파일 4개만 (`bybit.ts` + `bitget.ts` + `polygon.ts` + `twelvedata.ts`). 추상화/팩토리 X.
- 기존 `binance.ts` 패턴 1:1 복제. 새 헬퍼 X.
- Finnhub fallback 은 `yahoo.ts` 내 분기 (별도 파일 X).
- Binance retry 는 `binance.ts` 내 분기 (별도 helper X).
- v0.4.2 범위 = NQ 만. EWY/SP500/KR200/KRW 변경 X.
- 사용자 미지시 기능 삽입 X.

---

## File Structure (변경 mapping)

### 신규 파일 (10)

| 경로 | 책임 |
|---|---|
| `server/lib/sources/bybit.ts` | Bybit Linear /v5/market/tickers QQQUSDT |
| `server/lib/sources/bitget.ts` | Bitget USDT-M /api/v2/mix/market/tickers QQQUSDT |
| `server/lib/sources/polygon.ts` | Polygon /v2/aggs/ticker/QQQ/prev (옵셔널, env 분기) |
| `server/lib/sources/twelvedata.ts` | TwelveData /price?symbol=QQQ (옵셔널, env 분기) |
| `server/lib/sources/finnhub.ts` | Finnhub /quote?symbol=QQQ (yahoo.ts 가 import) |
| `server/tests/sources/bybit.test.ts` | bybit 응답 파싱 |
| `server/tests/sources/bitget.test.ts` | bitget 응답 파싱 |
| `server/tests/sources/polygon.test.ts` | polygon env skip + parse |
| `server/tests/sources/twelvedata.test.ts` | twelvedata env skip + parse |
| `server/tests/sources/yahoo-finnhub.test.ts` | yahoo tier 1/2/3 fail 시 finnhub fallback |
| `server/tests/sources/binance-retry.test.ts` | binance 첫 fetch fail → 재시도 success, Retry-After 헤더 |
| `server/tests/assemble-nq-fallback.test.ts` | NQ headline fallback chain (binance only / bybit only / 모두 missing 등) |
| `server/tests/types-completeness.test.ts` | `Record<SourceName, X>` 9-key init 검증 (health.ts) |
| `server/tests/nq-unit-consistency.test.ts` | 모든 source QQQ price ≈ ~$570 합리적 범위 |

### 수정 파일 (10)

| 경로 | 변경 내용 |
|---|---|
| `shared/types/prices.ts` | `SourceName` 4 신규 + `TickerPayload` 4 신규 venue |
| `server/lib/health.ts` | state literal 9 keys 초기화 + recordSourceAttempt `error='disabled'` 분기 |
| `server/lib/source-cache.ts` | `SOURCE_TTL_MS` 에 `bybit:1000, bitget:1000, polygon:12000, twelvedata:120000` 추가 |
| `server/lib/assemble.ts` | `SourceInputs` 9 keys, mapping table 4 신규, recordSourceAttempt 9회, NQ `YAHOO_SYMBOL_TO_TICKER` ETF only |
| `server/lib/sources/yahoo.ts` | Tier 1/2/3 모두 fail 시 finnhub.ts 호출 분기 |
| `server/lib/sources/binance.ts` | `fetchTickers24hrWithRetry` 1회 재시도 + `Retry-After` 헤더 |
| `server/index.ts` | Promise.all 9 source (옵셔널 2개 env 분기), yahoo symbols 에 `QQQ` 추가/`NQ=F`+`^NDX` 제거 |
| `src/components/VenueRow.tsx` | `SOURCE_LABELS` 4 신규 키 (bybit/bitget/polygon/twelvedata) |
| `src/components/IndexCompactCard.tsx` | `pickHeadlineSource` 함수 + 5 VenueRow + headline source label |
| `src/components/IndexCompactCard.test.tsx` | bybit/bitget VenueRow + headline source label + fallback priority 케이스 |
| `README.md` | Source matrix 표 4 신규 source + 가입 URL + free quota + daily quota 경고 |
| `.env.example` | `FINNHUB_TOKEN` + `POLYGON_API_KEY` + `TWELVEDATA_API_KEY` 주석 |

---

## Task 1: Wave 1 — 타입 확장 (SourceName, TickerPayload, 모든 `Record<SourceName,X>` 동시 업데이트)

**Files:**
- Modify: `shared/types/prices.ts:1, 70-78` (SourceName + TickerPayload)
- Modify: `server/lib/health.ts:3-9, 12-21` (state init + recordSourceAttempt disabled 분기)
- Modify: `src/components/VenueRow.tsx:4-10` (SOURCE_LABELS)
- Test: `server/tests/types-completeness.test.ts` (신규)

- [ ] **Step 1: Write the failing test (types-completeness)**

`server/tests/types-completeness.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { getSourceHealth, _resetSourceHealth } from '../lib/health.js';
import type { SourceName } from '@shared/types/prices.js';

describe('types-completeness — SourceName runtime exhaustiveness', () => {
  it('health.ts state initializes all 9 SourceName keys', () => {
    _resetSourceHealth();
    const health = getSourceHealth();
    const expected: SourceName[] = [
      'hyperliquid', 'naver', 'yahoo', 'binance', 'upbit',
      'bybit', 'bitget', 'polygon', 'twelvedata',
    ];
    for (const key of expected) {
      expect(health[key]).toBeDefined();
      expect(health[key].lastSuccess).toBe(0);
      expect(health[key].consecutiveFailures).toBe(0);
    }
    // No extra keys
    expect(Object.keys(health).sort()).toEqual([...expected].sort());
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test server/tests/types-completeness.test.ts`
Expected: FAIL — `health.bybit is undefined` (state 가 5 keys 만 가짐)

- [ ] **Step 3: Extend `SourceName` and `TickerPayload`**

`shared/types/prices.ts:1` (replace line 1):
```ts
export type SourceName = 'hyperliquid' | 'naver' | 'yahoo' | 'binance' | 'upbit'
                       | 'bybit' | 'bitget' | 'polygon' | 'twelvedata';
```

`shared/types/prices.ts:70-78` (replace TickerPayload):
```ts
export type TickerPayload = {
  hl?: PricePoint;
  naver?: PricePoint;
  yahoo?: PricePoint;
  binance?: PricePoint;
  upbit?: PricePoint;
  // === v0.4.2 신규 ===
  bybit?: PricePoint;
  bitget?: PricePoint;
  polygon?: PricePoint;
  twelvedata?: PricePoint;
  premium?: Premium;
  spread?: Spread;
};
```

- [ ] **Step 4: Extend health.ts state init + `disabled` error 분기**

`server/lib/health.ts:3-9` (replace state literal):
```ts
const state: Record<SourceName, SourceHealth> = {
  hyperliquid: { lastSuccess: 0, consecutiveFailures: 0 },
  naver:       { lastSuccess: 0, consecutiveFailures: 0 },
  yahoo:       { lastSuccess: 0, consecutiveFailures: 0 },
  binance:     { lastSuccess: 0, consecutiveFailures: 0 },
  upbit:       { lastSuccess: 0, consecutiveFailures: 0 },
  bybit:       { lastSuccess: 0, consecutiveFailures: 0 },
  bitget:      { lastSuccess: 0, consecutiveFailures: 0 },
  polygon:     { lastSuccess: 0, consecutiveFailures: 0 },
  twelvedata:  { lastSuccess: 0, consecutiveFailures: 0 },
};
```

`server/lib/health.ts:12-21` (replace recordSourceAttempt):
```ts
/** Record one source attempt result. Updates lastSuccess + consecutiveFailures.
 *  v0.4.2: `error === 'disabled'` (optional source env 키 없음) 은 sourceHealth 안 오염. */
export function recordSourceAttempt<T>(source: SourceName, result: Result<T>, now: number = Date.now()): void {
  if (result.ok) {
    state[source] = { lastSuccess: now, consecutiveFailures: 0 };
  } else if (result.error === 'disabled') {
    // optional source skip — fail count 증가 안 함
    return;
  } else {
    state[source] = {
      lastSuccess: state[source].lastSuccess,
      consecutiveFailures: state[source].consecutiveFailures + 1,
    };
  }
}
```

- [ ] **Step 5: Extend VenueRow SOURCE_LABELS**

`src/components/VenueRow.tsx:4-10` (replace):
```tsx
const SOURCE_LABELS: Record<SourceName, string> = {
  hyperliquid: 'HL xyz',
  yahoo: 'Yahoo',
  binance: 'Binance',
  naver: 'KRX',
  upbit: 'Upbit',
  // === v0.4.2 신규 ===
  bybit: 'Bybit',
  bitget: 'Bitget',
  polygon: 'Polygon',
  twelvedata: 'TwelveData',
};
```

- [ ] **Step 6: Run test to verify it passes**

Run: `pnpm test server/tests/types-completeness.test.ts`
Expected: PASS

- [ ] **Step 7: TypeScript exhaustive switch + grep gate 검증**

Run: `pnpm exec tsc --noEmit`
Expected: PASS (server + client 둘 다)

Run: `grep -rn "'hyperliquid'" src/ server/ shared/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v ".test."`
Expected: 각 match 마다 인접 코드가 신규 4 source 도 다루는지 확인. 다음 위치 모두 9-key 완전성 확인:
- `shared/types/prices.ts` (SourceName 정의)
- `server/lib/health.ts` (state literal)
- `src/components/VenueRow.tsx` (SOURCE_LABELS)
- `server/index.ts` (internalHealthHandler 의 health 순회 — 자동 처리, 확인만)

- [ ] **Step 8: Commit**

```bash
git add shared/types/prices.ts server/lib/health.ts src/components/VenueRow.tsx server/tests/types-completeness.test.ts
git commit -m "feat(types): extend SourceName + TickerPayload + health state for v0.4.2 (Wave 1)

- SourceName: bybit, bitget, polygon, twelvedata 추가 (5 → 9)
- TickerPayload: 4 신규 venue 슬롯
- health.ts: state init 9 keys + recordSourceAttempt 'disabled' 면제
- VenueRow.SOURCE_LABELS: 4 신규 라벨
- types-completeness.test.ts: 9-key runtime 검증

Spec §2.7 / §3.3 / Codex ⚠️ #4 (Record<SourceName,X> 동시 업데이트)"
```

---

## Task 2: Wave 2 — Bybit source (TDD)

**Files:**
- Create: `server/lib/sources/bybit.ts`
- Create: `server/tests/sources/bybit.test.ts`
- Modify: `server/lib/source-cache.ts:4-9` (TTL 추가)
- Modify: `server/lib/assemble.ts` (BYBIT_SYMBOL_TO_TICKER + SourceInputs.bybit + recordSourceAttempt)
- Modify: `server/index.ts:11, 91-98` (import + singleFlight)

**Bybit API endpoint (조사 결과)**:
- `GET https://api.bybit.com/v5/market/tickers?category=linear&symbol=QQQUSDT`
- 응답 shape: `{ retCode: 0, result: { list: [{ symbol, lastPrice, price24hPcnt, turnover24h, fundingRate, openInterest, ... }] } }`
- `price24hPcnt` 는 ratio (예: "0.0163" = 1.63%). 100 곱셈 필요.

- [ ] **Step 1: Write the failing test**

`server/tests/sources/bybit.test.ts`:
```ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { fetchBybitLinear } from '../../lib/sources/bybit.js';

afterEach(() => {
  vi.restoreAllMocks();
});

describe('fetchBybitLinear', () => {
  it('parses QQQUSDT response into PricePoint[]', async () => {
    const mockResp = {
      retCode: 0,
      result: {
        list: [{
          symbol: 'QQQUSDT',
          lastPrice: '572.45',
          price24hPcnt: '0.0163',  // 1.63%
          turnover24h: '12345678.9',
          fundingRate: '0.0001',
          openInterest: '50000',
        }],
      },
    };
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify(mockResp), { status: 200 }) as unknown as Response,
    );

    const result = await fetchBybitLinear(['QQQUSDT']);
    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error('unreachable');
    expect(result.data).toHaveLength(1);
    expect(result.data[0]).toMatchObject({
      source: 'bybit',
      symbol: 'QQQUSDT',
      price: 572.45,
      unit: 'USDT',
      status: 'ok',
    });
    // price24hPcnt 0.0163 → change24hPct 1.63
    expect(result.data[0].change24hPct).toBeCloseTo(1.63, 2);
  });

  it('returns ok:false on HTTP error', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response('', { status: 500 }) as unknown as Response,
    );
    const result = await fetchBybitLinear(['QQQUSDT']);
    expect(result.ok).toBe(false);
  });

  it('returns ok:false when retCode != 0', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({ retCode: 10001, retMsg: 'error' }), { status: 200 }) as unknown as Response,
    );
    const result = await fetchBybitLinear(['QQQUSDT']);
    expect(result.ok).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test server/tests/sources/bybit.test.ts`
Expected: FAIL — `Cannot find module '../../lib/sources/bybit.js'`

- [ ] **Step 3: Implement fetchBybitLinear**

`server/lib/sources/bybit.ts` (신규):
```ts
import type { PricePoint, Result } from '@shared/types/prices.js';

const SCHEMA_VERSION = 1;
const TIMEOUT_MS = 5000;
const BYBIT_BASE = 'https://api.bybit.com';

export const BYBIT_SYMBOLS = ['QQQUSDT'] as const;

type BybitTicker = {
  symbol: string;
  lastPrice?: string;
  price24hPcnt?: string;
  turnover24h?: string;
  fundingRate?: string;
  openInterest?: string;
};

type BybitTickersResponse = {
  retCode: number;
  retMsg?: string;
  result?: { list?: BybitTicker[] };
};

function parseNum(v: unknown): number | undefined {
  if (typeof v === 'number') return Number.isFinite(v) ? v : undefined;
  if (typeof v !== 'string') return undefined;
  const n = Number(v);
  return Number.isFinite(n) ? n : undefined;
}

export async function fetchBybitLinear(
  symbols: readonly string[] = BYBIT_SYMBOLS,
): Promise<Result<PricePoint[]>> {
  const start = Date.now();
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), TIMEOUT_MS);
  try {
    // Bybit 은 한 번에 한 symbol. 여러 symbol 시 Promise.all.
    const results = await Promise.all(symbols.map(async (sym) => {
      const url = `${BYBIT_BASE}/v5/market/tickers?category=linear&symbol=${encodeURIComponent(sym)}`;
      const res = await fetch(url, { signal: controller.signal });
      if (!res.ok) throw new Error(`Bybit HTTP ${res.status}`);
      const raw = (await res.json()) as BybitTickersResponse;
      if (raw.retCode !== 0) {
        throw new Error(`Bybit retCode ${raw.retCode}: ${raw.retMsg ?? 'unknown'}`);
      }
      return raw.result?.list?.[0];
    }));

    const data: PricePoint[] = [];
    for (const t of results) {
      if (!t) continue;
      const price = parseNum(t.lastPrice);
      if (!t.symbol || price === undefined) continue;
      const pctRatio = parseNum(t.price24hPcnt);
      data.push({
        source: 'bybit',
        symbol: t.symbol,
        price,
        unit: 'USDT',
        change24hPct: pctRatio !== undefined ? pctRatio * 100 : undefined,
        volume24hUsd: parseNum(t.turnover24h),
        fundingRate8h: parseNum(t.fundingRate),
        openInterestUsd: parseNum(t.openInterest),
        status: 'ok',
        asOf: Date.now(),
        receivedAt: Date.now(),
        schemaVersion: SCHEMA_VERSION,
      });
    }

    if (data.length === 0) {
      return { ok: false, error: 'Bybit returned 0 valid tickers', latencyMs: Date.now() - start };
    }
    return { ok: true, data, latencyMs: Date.now() - start };
  } catch (err) {
    if (err instanceof Error && err.name === 'AbortError') {
      return { ok: false, error: 'Bybit timeout', latencyMs: Date.now() - start };
    }
    return { ok: false, error: err instanceof Error ? err.message : String(err), latencyMs: Date.now() - start };
  } finally {
    clearTimeout(timeout);
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test server/tests/sources/bybit.test.ts`
Expected: PASS (3 cases)

- [ ] **Step 5: Add SOURCE_TTL_MS.bybit**

`server/lib/source-cache.ts:4-9` (replace):
```ts
export const SOURCE_TTL_MS = {
  hyperliquid: 1000,
  binance: 1000,
  upbit: 2000,
  yahoo: 5000,
  bybit: 1000,
  bitget: 1000,
  polygon: 12_000,      // 5/min free cap (60s / 12s = 5)
  twelvedata: 120_000,  // 800/day free cap (24h / 120s = 720 < 800)
} as const;
```

- [ ] **Step 6: Wire into assemble.ts**

`server/lib/assemble.ts` 에 BYBIT_SYMBOL_TO_TICKER 추가 (line 54 직후):
```ts
const BYBIT_SYMBOL_TO_TICKER: Record<string, string> = {
  QQQUSDT: 'nq',
};
```

`SourceInputs` (line 62-68) 에 bybit 추가:
```ts
export type SourceInputs = {
  hl: Result<PricePoint[]>;
  naver: Result<PricePoint[]>;
  yahoo: Result<PricePoint[]>;
  upbit: Result<PricePoint[]>;
  binance: Result<PricePoint[]>;
  bybit: Result<PricePoint[]>;
  bitget: Result<PricePoint[]>;
  polygon: Result<PricePoint[]>;
  twelvedata: Result<PricePoint[]>;
};
```

`assemblePricesResponse` 안 recordSourceAttempt 호출 (line 151-155) 뒤에 추가:
```ts
recordSourceAttempt('bybit', sources.bybit, ts);
recordSourceAttempt('bitget', sources.bitget, ts);
recordSourceAttempt('polygon', sources.polygon, ts);
recordSourceAttempt('twelvedata', sources.twelvedata, ts);
```

binance mapping 다음 (line 219 직후) 에 bybit 통합 추가:
```ts
if (sources.bybit.ok) {
  for (const pp of sources.bybit.data) {
    const tickerKey = BYBIT_SYMBOL_TO_TICKER[pp.symbol];
    if (!tickerKey) continue;
    tickers[tickerKey] = { ...(tickers[tickerKey] ?? {}), bybit: pp };
  }
}
```

- [ ] **Step 7: Wire into server/index.ts**

`server/index.ts:11` 직후에 import 추가:
```ts
import { fetchBybitLinear } from './lib/sources/bybit.js';
import { fetchBitgetFutures } from './lib/sources/bitget.js';
import { fetchPolygon } from './lib/sources/polygon.js';
import { fetchTwelveData } from './lib/sources/twelvedata.js';
```

`server/index.ts:91-98` (replace Promise.all) — 이 단계에서는 bybit + 빈 stub 3개 (다음 Wave 에서 채움):
```ts
const polygonToken = process.env.POLYGON_API_KEY;
const twelveDataToken = process.env.TWELVEDATA_API_KEY;
const usePolygon = polygonToken !== undefined && polygonToken !== '';
const useTwelveData = twelveDataToken !== undefined && twelveDataToken !== '';

const disabledResult = { ok: false as const, error: 'disabled' as const, latencyMs: 0 };

const [hlResult, naver, yahoo, upbit, binance, bybit, bitget, polygon, twelvedata] = await Promise.all([
  singleFlight('source:hyperliquid', SOURCE_TTL_MS.hyperliquid, fetchHyperliquid),
  singleFlight('source:naver',       naverTtl(),                fetchNaver),
  singleFlight('source:yahoo',       SOURCE_TTL_MS.yahoo,
    () => fetchYahoo(['KRW=X', 'EWY', 'NQ=F', 'ES=F', '^NDX', '^GSPC'])),
  singleFlight('source:upbit',       SOURCE_TTL_MS.upbit,       fetchUpbit),
  singleFlight('source:binance',     SOURCE_TTL_MS.binance,     fetchBinanceFutures),
  singleFlight('source:bybit',       SOURCE_TTL_MS.bybit,       fetchBybitLinear),
  // Task 3 에서 활성화
  Promise.resolve(disabledResult),
  // Task 7 에서 활성화 (env 분기)
  Promise.resolve(disabledResult),
  Promise.resolve(disabledResult),
]);
```

`assemblePricesResponse` 호출 (line 120) 변경:
```ts
const response = assemblePricesResponse({ hl, naver, yahoo, upbit, binance, bybit, bitget, polygon, twelvedata });
```

- [ ] **Step 8: Verify build + integration**

Run: `pnpm exec tsc --noEmit && pnpm test`
Expected: 전부 PASS

Run (수동 검증):
```bash
pnpm dev:server &
sleep 3
curl -s http://localhost:3001/api/prices | jq '.tickers.nq.bybit | {source, price, unit}'
kill %1
```
Expected: `{"source": "bybit", "price": ~570, "unit": "USDT"}` 정도

- [ ] **Step 9: Commit**

```bash
git add server/lib/sources/bybit.ts server/tests/sources/bybit.test.ts \
        server/lib/source-cache.ts server/lib/assemble.ts server/index.ts
git commit -m "feat(server/bybit): Bybit Linear QQQUSDT source for NQ (Wave 2)

- fetchBybitLinear: /v5/market/tickers?category=linear&symbol=QQQUSDT
- SOURCE_TTL_MS.bybit = 1000ms (binance 와 동일 빈도)
- SOURCE_TTL_MS.polygon/twelvedata 동시 추가 (다음 Wave 활성화)
- assemble.ts: BYBIT_SYMBOL_TO_TICKER + SourceInputs 확장 (9 keys)
- server/index.ts: Promise.all 9-source skeleton (bitget/polygon/twelvedata disabled stub)

Spec §2.1 / §2.6 / §5 R2"
```

---

## Task 3: Wave 3 — Bitget source (TDD)

**Files:**
- Create: `server/lib/sources/bitget.ts`
- Create: `server/tests/sources/bitget.test.ts`
- Modify: `server/lib/assemble.ts` (BITGET_SYMBOL_TO_TICKER + bitget 통합 분기)
- Modify: `server/index.ts:91` (bitget 활성화)

**Bitget API endpoint**:
- `GET https://api.bitget.com/api/v2/mix/market/tickers?productType=usdt-futures&symbol=QQQUSDT`
- 응답 shape: `{ code: '00000', data: [{ symbol, lastPr, change24h, baseVolume, fundingRate, holdingAmount, ... }] }`
- `change24h` 는 ratio (예: "0.0163"). 100 곱셈 필요.

- [ ] **Step 1: Write the failing test**

`server/tests/sources/bitget.test.ts`:
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { fetchBitgetFutures } from '../../lib/sources/bitget.js';

afterEach(() => vi.restoreAllMocks());

describe('fetchBitgetFutures', () => {
  it('parses QQQUSDT response into PricePoint[]', async () => {
    const mockResp = {
      code: '00000',
      data: [{
        symbol: 'QQQUSDT',
        lastPr: '572.45',
        change24h: '0.0163',
        baseVolume: '12345',
        fundingRate: '0.0001',
        holdingAmount: '50000',
      }],
    };
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify(mockResp), { status: 200 }) as unknown as Response,
    );
    const result = await fetchBitgetFutures(['QQQUSDT']);
    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error('unreachable');
    expect(result.data[0]).toMatchObject({
      source: 'bitget',
      symbol: 'QQQUSDT',
      price: 572.45,
      unit: 'USDT',
    });
    expect(result.data[0].change24hPct).toBeCloseTo(1.63, 2);
  });

  it('returns ok:false on HTTP 500', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response('', { status: 500 }) as unknown as Response,
    );
    const result = await fetchBitgetFutures(['QQQUSDT']);
    expect(result.ok).toBe(false);
  });

  it('returns ok:false when code != 00000', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({ code: '40001', msg: 'error' }), { status: 200 }) as unknown as Response,
    );
    const result = await fetchBitgetFutures(['QQQUSDT']);
    expect(result.ok).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test server/tests/sources/bitget.test.ts`
Expected: FAIL — module not found

- [ ] **Step 3: Implement fetchBitgetFutures**

`server/lib/sources/bitget.ts` (신규):
```ts
import type { PricePoint, Result } from '@shared/types/prices.js';

const SCHEMA_VERSION = 1;
const TIMEOUT_MS = 5000;
const BITGET_BASE = 'https://api.bitget.com';

export const BITGET_SYMBOLS = ['QQQUSDT'] as const;

type BitgetTicker = {
  symbol: string;
  lastPr?: string;
  change24h?: string;
  baseVolume?: string;
  fundingRate?: string;
  holdingAmount?: string;
};

type BitgetResponse = {
  code: string;
  msg?: string;
  data?: BitgetTicker[];
};

function parseNum(v: unknown): number | undefined {
  if (typeof v === 'number') return Number.isFinite(v) ? v : undefined;
  if (typeof v !== 'string') return undefined;
  const n = Number(v);
  return Number.isFinite(n) ? n : undefined;
}

export async function fetchBitgetFutures(
  symbols: readonly string[] = BITGET_SYMBOLS,
): Promise<Result<PricePoint[]>> {
  const start = Date.now();
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), TIMEOUT_MS);
  try {
    // Bitget V2 는 productType=usdt-futures + symbol per call
    const results = await Promise.all(symbols.map(async (sym) => {
      const url = `${BITGET_BASE}/api/v2/mix/market/tickers?productType=usdt-futures&symbol=${encodeURIComponent(sym)}`;
      const res = await fetch(url, { signal: controller.signal });
      if (!res.ok) throw new Error(`Bitget HTTP ${res.status}`);
      const raw = (await res.json()) as BitgetResponse;
      if (raw.code !== '00000') {
        throw new Error(`Bitget code ${raw.code}: ${raw.msg ?? 'unknown'}`);
      }
      return raw.data?.[0];
    }));

    const data: PricePoint[] = [];
    for (const t of results) {
      if (!t) continue;
      const price = parseNum(t.lastPr);
      if (!t.symbol || price === undefined) continue;
      const pctRatio = parseNum(t.change24h);
      data.push({
        source: 'bitget',
        symbol: t.symbol,
        price,
        unit: 'USDT',
        change24hPct: pctRatio !== undefined ? pctRatio * 100 : undefined,
        volume24hUsd: parseNum(t.baseVolume),
        fundingRate8h: parseNum(t.fundingRate),
        openInterestUsd: parseNum(t.holdingAmount),
        status: 'ok',
        asOf: Date.now(),
        receivedAt: Date.now(),
        schemaVersion: SCHEMA_VERSION,
      });
    }

    if (data.length === 0) {
      return { ok: false, error: 'Bitget returned 0 valid tickers', latencyMs: Date.now() - start };
    }
    return { ok: true, data, latencyMs: Date.now() - start };
  } catch (err) {
    if (err instanceof Error && err.name === 'AbortError') {
      return { ok: false, error: 'Bitget timeout', latencyMs: Date.now() - start };
    }
    return { ok: false, error: err instanceof Error ? err.message : String(err), latencyMs: Date.now() - start };
  } finally {
    clearTimeout(timeout);
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test server/tests/sources/bitget.test.ts`
Expected: PASS

- [ ] **Step 5: Wire into assemble.ts**

`server/lib/assemble.ts` 의 BYBIT_SYMBOL_TO_TICKER 다음 줄에 추가:
```ts
const BITGET_SYMBOL_TO_TICKER: Record<string, string> = {
  QQQUSDT: 'nq',
};
```

bybit 통합 분기 다음에 추가:
```ts
if (sources.bitget.ok) {
  for (const pp of sources.bitget.data) {
    const tickerKey = BITGET_SYMBOL_TO_TICKER[pp.symbol];
    if (!tickerKey) continue;
    tickers[tickerKey] = { ...(tickers[tickerKey] ?? {}), bitget: pp };
  }
}
```

- [ ] **Step 6: Wire into server/index.ts**

`server/index.ts` Promise.all 7번째 줄 (`Promise.resolve(disabledResult)` for bitget) 을 교체:
```ts
singleFlight('source:bitget',      SOURCE_TTL_MS.bitget,      fetchBitgetFutures),
```

- [ ] **Step 7: Verify build + integration**

Run: `pnpm exec tsc --noEmit && pnpm test`
Expected: PASS

```bash
pnpm dev:server &
sleep 3
curl -s http://localhost:3001/api/prices | jq '.tickers.nq | {bybit: .bybit.source, bitget: .bitget.source}'
kill %1
```
Expected: `{"bybit": "bybit", "bitget": "bitget"}`

- [ ] **Step 8: Commit**

```bash
git add server/lib/sources/bitget.ts server/tests/sources/bitget.test.ts \
        server/lib/assemble.ts server/index.ts
git commit -m "feat(server/bitget): Bitget USDT-M QQQUSDT source for NQ (Wave 3)

- fetchBitgetFutures: /api/v2/mix/market/tickers?productType=usdt-futures
- assemble.ts: BITGET_SYMBOL_TO_TICKER + 통합 분기
- server/index.ts: bitget singleFlight 활성화

Spec §2.2 / §2.6"
```

---

## Task 4: Wave 4A — Yahoo Finnhub fallback + NQ unit 정규화 (QQQ ETF)

**Files:**
- Create: `server/lib/sources/finnhub.ts`
- Create: `server/tests/sources/yahoo-finnhub.test.ts`
- Modify: `server/lib/sources/yahoo.ts` (Finnhub fallback 분기)
- Modify: `server/lib/assemble.ts:41-47` (YAHOO_SYMBOL_TO_TICKER NQ=F+^NDX → QQQ)
- Modify: `server/index.ts:95` (symbols 변경)

- [ ] **Step 1: Write failing tests**

`server/tests/sources/yahoo-finnhub.test.ts`:
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { fetchYahoo } from '../../lib/sources/yahoo.js';

afterEach(() => {
  vi.restoreAllMocks();
  delete process.env.FINNHUB_TOKEN;
});

describe('Yahoo Finnhub fallback (v0.4.2)', () => {
  it('falls back to Finnhub when all yahoo tiers fail and FINNHUB_TOKEN is set', async () => {
    process.env.FINNHUB_TOKEN = 'test-token';

    let callCount = 0;
    vi.spyOn(global, 'fetch').mockImplementation(async (url: string | URL | Request) => {
      callCount++;
      const u = url.toString();
      if (u.includes('finnhub.io')) {
        // Finnhub /quote response
        return new Response(JSON.stringify({ c: 572.45, pc: 563.21, t: 1700000000 }), { status: 200 }) as unknown as Response;
      }
      // Yahoo: 모든 tier fail
      return new Response('', { status: 429 }) as unknown as Response;
    });

    const result = await fetchYahoo(['QQQ']);
    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error('unreachable');
    expect(result.data[0]).toMatchObject({ source: 'yahoo', symbol: 'QQQ', price: 572.45 });
  });

  it('returns ok:false when yahoo fails and FINNHUB_TOKEN is not set', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response('', { status: 429 }) as unknown as Response,
    );
    const result = await fetchYahoo(['QQQ']);
    expect(result.ok).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test server/tests/sources/yahoo-finnhub.test.ts`
Expected: FAIL — finnhub fallback 미구현

- [ ] **Step 3: Create finnhub.ts helper**

`server/lib/sources/finnhub.ts` (신규):
```ts
import type { PricePoint, Result } from '@shared/types/prices.js';

const SCHEMA_VERSION = 1;
const TIMEOUT_MS = 5000;

type FinnhubQuote = {
  c?: number;   // current price
  pc?: number;  // previous close
  t?: number;   // timestamp (unix sec)
};

/** Map Yahoo symbol → Finnhub symbol. Index symbols 는 미지원, ETF only. */
function toFinnhubSymbol(yahooSym: string): string | null {
  // ETF / equity 직접 통과. NQ=F, ^NDX 등 index/future 는 finnhub 미지원 → null.
  if (yahooSym.startsWith('^')) return null;
  if (yahooSym.endsWith('=F')) return null;
  if (yahooSym.endsWith('=X')) return null;  // FX
  return yahooSym;
}

export async function fetchFinnhub(
  symbols: readonly string[],
  token: string,
): Promise<Result<PricePoint[]>> {
  const start = Date.now();
  const result: PricePoint[] = [];

  await Promise.all(symbols.map(async (sym) => {
    const finnSym = toFinnhubSymbol(sym);
    if (!finnSym) return;

    const url = `https://finnhub.io/api/v1/quote?symbol=${encodeURIComponent(finnSym)}&token=${encodeURIComponent(token)}`;
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), TIMEOUT_MS);
    try {
      const res = await fetch(url, { signal: controller.signal });
      if (!res.ok) return;
      const raw = (await res.json()) as FinnhubQuote;
      if (typeof raw.c !== 'number' || raw.c <= 0) return;
      const pct = raw.pc && raw.pc > 0 ? ((raw.c - raw.pc) / raw.pc) * 100 : 0;
      result.push({
        source: 'yahoo',  // ⚠️ yahoo source slot 유지 (fallback 은 yahoo 의 backup)
        symbol: sym,
        price: raw.c,
        unit: 'USD',
        change24hPct: pct,
        status: 'ok',
        asOf: raw.t ? raw.t * 1000 : Date.now(),
        receivedAt: Date.now(),
        schemaVersion: SCHEMA_VERSION,
        previousClose: raw.pc && raw.pc > 0 ? raw.pc : undefined,
        previousCloseSource: raw.pc && raw.pc > 0 ? 'yahoo' : undefined,
      });
    } catch {
      // swallow per-symbol error
    } finally {
      clearTimeout(timeout);
    }
  }));

  if (result.length === 0) {
    return { ok: false, error: 'Finnhub returned 0 valid items', latencyMs: Date.now() - start };
  }
  return { ok: true, data: result, latencyMs: Date.now() - start };
}
```

- [ ] **Step 4: Wire Finnhub fallback into yahoo.ts**

`server/lib/sources/yahoo.ts` 의 `fetchYahoo` 함수 (line 171-188) 끝부분 변경 — Tier 3 fail 직후 finnhub fallback:

Replace lines 182-188 (Tier 3 + final return) with:
```ts
  // Tier 3: per-symbol chart
  const r3 = await tryChartPerSymbol(symbols);
  if (r3.ok) return r3;
  log.warn(`[yahoo] tier 3 (chart per-symbol) failed: ${r3.error}`);

  // v0.4.2: Tier 4 — Finnhub fallback (FINNHUB_TOKEN 활성 시에만)
  const finnhubToken = process.env.FINNHUB_TOKEN;
  if (finnhubToken) {
    const { fetchFinnhub } = await import('./finnhub.js');
    const r4 = await fetchFinnhub(symbols, finnhubToken);
    if (r4.ok) {
      log.info(`[yahoo] tier 4 (finnhub fallback) succeeded for ${r4.data.length} symbols`);
      return r4;
    }
    log.warn(`[yahoo] tier 4 (finnhub fallback) failed: ${r4.error}`);
  }

  return { ok: false, error: `All Yahoo tiers failed for ${symbols.length} symbols`, latencyMs: 0 };
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `pnpm test server/tests/sources/yahoo-finnhub.test.ts`
Expected: PASS

- [ ] **Step 6: Update YAHOO_SYMBOL_TO_TICKER (QQQ ETF only)**

`server/lib/assemble.ts:41-47` (replace):
```ts
// v0.4.2: NQ 는 QQQ ETF only (NQ=F + ^NDX 제거 — §2.7 unit 정규화).
// 지수 pt 단위는 USDT-perp 와 scale 불일치 → 모든 source USD QQQ 통일.
const YAHOO_SYMBOL_TO_TICKER: Record<string, string> = {
  EWY: 'ewy',
  QQQ: 'nq',
  'ES=F': 'sp500',
  '^GSPC': 'sp500',
};
```

- [ ] **Step 7: Update server/index.ts symbols**

`server/index.ts:94-95` (replace):
```ts
singleFlight('source:yahoo',       SOURCE_TTL_MS.yahoo,
  () => fetchYahoo(['KRW=X', 'EWY', 'QQQ', 'ES=F', '^GSPC'])),
```

또한 `server/lib/assemble.ts:201-205` 의 `yahooMarketOpen` 의 `nq: session.cme` 는 그대로 유지 (QQQ ETF 도 미국 시장 시간). 단, QQQ ETF 는 regular hours 만 → 변경:
```ts
const yahooMarketOpen: Record<string, boolean> = {
  ewy: session.nyseRegular || session.nysePrePost,
  sp500: session.cme || session.nyseRegular,
  nq: session.nyseRegular,  // QQQ ETF 는 NYSE 시간 (이전 NQ=F 는 cme 였음)
};
```

- [ ] **Step 8: Verify**

Run: `pnpm exec tsc --noEmit && pnpm test`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add server/lib/sources/finnhub.ts server/tests/sources/yahoo-finnhub.test.ts \
        server/lib/sources/yahoo.ts server/lib/assemble.ts server/index.ts
git commit -m "feat(server/yahoo): Finnhub Tier-4 fallback + QQQ ETF normalization (Wave 4A)

- finnhub.ts: /quote endpoint per-symbol, source slot 'yahoo' 유지
- yahoo.ts: Tier 3 실패 시 FINNHUB_TOKEN 분기로 Tier 4 시도
- YAHOO_SYMBOL_TO_TICKER: NQ=F + ^NDX 제거, QQQ 추가 (ETF only)
- server/index.ts: fetchYahoo symbols 에 QQQ 포함, NQ=F + ^NDX 제거
- yahooMarketOpen.nq: cme → nyseRegular (QQQ ETF 시간)

Spec §2.3 / §2.7 / Codex 🚨 A,F (unit 정규화) / Codex 🚨 E (yahoo fallback chain)"
```

---

## Task 5: Wave 4B — Binance retry with Retry-After header

**Files:**
- Modify: `server/lib/sources/binance.ts` (fetchTickers24hrWithRetry wrapper)
- Create: `server/tests/sources/binance-retry.test.ts`

- [ ] **Step 1: Write failing test**

`server/tests/sources/binance-retry.test.ts`:
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { fetchBinanceFutures } from '../../lib/sources/binance.js';

afterEach(() => vi.restoreAllMocks());

describe('Binance retry (v0.4.2)', () => {
  it('retries once on first failure and succeeds on second attempt', async () => {
    let call = 0;
    vi.spyOn(global, 'fetch').mockImplementation(async (url) => {
      const u = url.toString();
      if (u.includes('/ticker/24hr')) {
        call++;
        if (call === 1) return new Response('', { status: 500 }) as unknown as Response;
        return new Response(JSON.stringify([
          { symbol: 'QQQUSDT', lastPrice: '572.45', priceChangePercent: '1.63', quoteVolume: '1000', closeTime: 1700000000000 },
        ]), { status: 200 }) as unknown as Response;
      }
      // premiumIndex
      return new Response(JSON.stringify({ symbol: 'QQQUSDT', lastFundingRate: '0.0001' }), { status: 200 }) as unknown as Response;
    });

    const result = await fetchBinanceFutures(['QQQUSDT']);
    expect(result.ok).toBe(true);
    expect(call).toBe(2);
  }, 15_000);

  it('uses Retry-After header for backoff (capped at 5s)', async () => {
    let call = 0;
    const start = Date.now();
    vi.spyOn(global, 'fetch').mockImplementation(async (url) => {
      const u = url.toString();
      if (u.includes('/ticker/24hr')) {
        call++;
        if (call === 1) {
          return new Response('', { status: 429, headers: { 'Retry-After': '2' } }) as unknown as Response;
        }
        return new Response(JSON.stringify([
          { symbol: 'QQQUSDT', lastPrice: '572.45', priceChangePercent: '1.63', quoteVolume: '1000', closeTime: 1700000000000 },
        ]), { status: 200 }) as unknown as Response;
      }
      return new Response(JSON.stringify({ symbol: 'QQQUSDT' }), { status: 200 }) as unknown as Response;
    });

    const result = await fetchBinanceFutures(['QQQUSDT']);
    const elapsed = Date.now() - start;
    expect(result.ok).toBe(true);
    expect(elapsed).toBeGreaterThanOrEqual(2000);   // 2s backoff
    expect(elapsed).toBeLessThan(5000);             // cap
  }, 15_000);

  it('caps Retry-After at 5000ms', async () => {
    let call = 0;
    const start = Date.now();
    vi.spyOn(global, 'fetch').mockImplementation(async (url) => {
      const u = url.toString();
      if (u.includes('/ticker/24hr')) {
        call++;
        if (call === 1) {
          return new Response('', { status: 429, headers: { 'Retry-After': '60' } }) as unknown as Response;
        }
        return new Response(JSON.stringify([
          { symbol: 'QQQUSDT', lastPrice: '572.45', priceChangePercent: '1.63', quoteVolume: '1000', closeTime: 1700000000000 },
        ]), { status: 200 }) as unknown as Response;
      }
      return new Response(JSON.stringify({ symbol: 'QQQUSDT' }), { status: 200 }) as unknown as Response;
    });
    const result = await fetchBinanceFutures(['QQQUSDT']);
    const elapsed = Date.now() - start;
    expect(result.ok).toBe(true);
    expect(elapsed).toBeLessThan(7000);  // cap 5s + slack
  }, 15_000);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test server/tests/sources/binance-retry.test.ts`
Expected: FAIL (현재 binance.ts 는 첫 실패에서 reject)

- [ ] **Step 3: Add fetchTickers24hrWithRetry wrapper**

`server/lib/sources/binance.ts:26-41` (replace fetchTickers24hr) — 기존 함수는 그대로 두고, 새 wrapper 추가:

기존 `fetchTickers24hr` 함수를 `fetchTickers24hrOnce` 로 이름 변경 (라인 26: `async function fetchTickers24hr` → `async function fetchTickers24hrOnce`).

그 다음에 추가:
```ts
async function fetchTickers24hr(symbols: readonly string[]): Promise<BinanceTicker24hr[]> {
  try {
    return await fetchTickers24hrOnce(symbols);
  } catch (err) {
    // v0.4.2: 1회 재시도. 429 Retry-After 헤더 우선 (5s cap).
    let backoffMs = 1000;
    if (err instanceof Error && err.message.includes('HTTP 429')) {
      // 429 분기 — 기존 Error message 에 Retry-After 가 없으므로 fetch 직접 재시도 시 헤더 확인 필요.
      // 단순화: 429 면 일단 backoff 1s (다음 시도가 Retry-After 사용)
      backoffMs = 1000;
    }
    // 429 면 fetch wrapper 가 Response 자체를 throw 하도록 fetchTickers24hrOnce 수정 필요 (Step 4).
    if (err instanceof Response && err.status === 429) {
      const retryAfter = parseInt(err.headers.get('Retry-After') ?? '1', 10);
      backoffMs = Math.min(retryAfter * 1000, 5000);
    }
    await new Promise(r => setTimeout(r, backoffMs));
    return await fetchTickers24hrOnce(symbols);
  }
}
```

- [ ] **Step 4: Update fetchTickers24hrOnce to throw Response on 429**

`server/lib/sources/binance.ts:33-36` (replace `if (!res.ok) { throw new Error(...) }`):
```ts
    if (!res.ok) {
      // v0.4.2: 429 시 Response 객체 자체를 throw (retry wrapper 가 Retry-After 헤더 사용)
      if (res.status === 429) {
        throw res;
      }
      throw new Error(`Binance ticker HTTP ${res.status}`);
    }
```

- [ ] **Step 5: Run test to verify it passes**

Run: `pnpm test server/tests/sources/binance-retry.test.ts`
Expected: PASS (3 cases)

- [ ] **Step 6: Verify existing binance tests still pass**

Run: `pnpm test`
Expected: 전부 PASS (regression 없음)

- [ ] **Step 7: Commit**

```bash
git add server/lib/sources/binance.ts server/tests/sources/binance-retry.test.ts
git commit -m "feat(server/binance): 1회 재시도 + Retry-After 헤더 (Wave 4B)

- fetchTickers24hrOnce: 기존 함수 이름 변경 (내부 호출)
- fetchTickers24hr: wrapper 로 1회 재시도, 429 시 Response throw
- 재시도 backoff: 기본 1s, Retry-After 헤더 있으면 그 값 (5s cap)
- Max latency: 5s (timeout) + 5s (backoff cap) + 5s (재시도) = 11s

Spec §2.4 / Codex ⚠️ #5"
```

---

## Task 6: Wave 5a — Polygon 옵셔널 source

**Files:**
- Create: `server/lib/sources/polygon.ts`
- Create: `server/tests/sources/polygon.test.ts`
- Modify: `server/lib/assemble.ts` (POLYGON_SYMBOL_TO_TICKER + polygon 통합 분기)
- Modify: `server/index.ts` (polygon env 분기)

**Polygon endpoint**:
- `GET https://api.polygon.io/v2/aggs/ticker/QQQ/prev?adjusted=true&apiKey=...`
- 응답: `{ status: 'OK', ticker: 'QQQ', results: [{ c, o, h, l, v, t }] }`
- 단일 ticker per call. QQQ ETF.

- [ ] **Step 1: Write failing test**

`server/tests/sources/polygon.test.ts`:
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { fetchPolygon } from '../../lib/sources/polygon.js';

afterEach(() => {
  vi.restoreAllMocks();
  delete process.env.POLYGON_API_KEY;
});

describe('fetchPolygon (옵셔널 source)', () => {
  it('returns {ok:false, error:"disabled"} when POLYGON_API_KEY not set', async () => {
    const result = await fetchPolygon(['QQQ']);
    expect(result.ok).toBe(false);
    if (result.ok) throw new Error('unreachable');
    expect(result.error).toBe('disabled');
    expect(result.latencyMs).toBe(0);
  });

  it('parses QQQ /v2/aggs response when API key set', async () => {
    process.env.POLYGON_API_KEY = 'test-key';
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({
        status: 'OK',
        ticker: 'QQQ',
        results: [{ c: 572.45, o: 563.21, h: 575.0, l: 562.0, v: 12345, t: 1700000000000 }],
      }), { status: 200 }) as unknown as Response,
    );
    const result = await fetchPolygon(['QQQ']);
    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error('unreachable');
    expect(result.data[0]).toMatchObject({
      source: 'polygon',
      symbol: 'QQQ',
      price: 572.45,
      unit: 'USD',
    });
  });

  it('returns ok:false on HTTP 429 with API key set', async () => {
    process.env.POLYGON_API_KEY = 'test-key';
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response('', { status: 429 }) as unknown as Response,
    );
    const result = await fetchPolygon(['QQQ']);
    expect(result.ok).toBe(false);
    if (result.ok) throw new Error('unreachable');
    expect(result.error).not.toBe('disabled');  // 진짜 fail (sourceHealth 오염)
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test server/tests/sources/polygon.test.ts`
Expected: FAIL — module not found

- [ ] **Step 3: Implement fetchPolygon**

`server/lib/sources/polygon.ts` (신규):
```ts
import type { PricePoint, Result } from '@shared/types/prices.js';

const SCHEMA_VERSION = 1;
const TIMEOUT_MS = 5000;
const POLYGON_BASE = 'https://api.polygon.io';

export const POLYGON_SYMBOLS = ['QQQ'] as const;

type PolygonAggsResponse = {
  status?: string;
  ticker?: string;
  results?: Array<{
    c?: number;  // close
    o?: number;  // open
    h?: number;  // high
    l?: number;  // low
    v?: number;  // volume
    t?: number;  // timestamp (ms)
  }>;
};

export async function fetchPolygon(
  symbols: readonly string[] = POLYGON_SYMBOLS,
): Promise<Result<PricePoint[]>> {
  const token = process.env.POLYGON_API_KEY;
  if (!token) {
    return { ok: false, error: 'disabled', latencyMs: 0 };
  }

  const start = Date.now();
  try {
    const results = await Promise.all(symbols.map(async (sym): Promise<PricePoint | null> => {
      const url = `${POLYGON_BASE}/v2/aggs/ticker/${encodeURIComponent(sym)}/prev?adjusted=true&apiKey=${encodeURIComponent(token)}`;
      const controller = new AbortController();
      const timeout = setTimeout(() => controller.abort(), TIMEOUT_MS);
      try {
        const res = await fetch(url, { signal: controller.signal });
        if (!res.ok) throw new Error(`Polygon HTTP ${res.status}`);
        const raw = (await res.json()) as PolygonAggsResponse;
        const item = raw.results?.[0];
        if (!item || typeof item.c !== 'number') return null;
        const prev = item.o && item.o > 0 ? item.o : item.c;
        const pct = prev > 0 ? ((item.c - prev) / prev) * 100 : 0;
        return {
          source: 'polygon',
          symbol: sym,
          price: item.c,
          unit: 'USD',
          change24hPct: pct,
          volume24hUsd: typeof item.v === 'number' ? item.v * item.c : undefined,
          status: 'ok',
          asOf: item.t ?? Date.now(),
          receivedAt: Date.now(),
          schemaVersion: SCHEMA_VERSION,
        };
      } finally {
        clearTimeout(timeout);
      }
    }));

    const data = results.filter((p): p is PricePoint => p !== null);
    if (data.length === 0) {
      return { ok: false, error: 'Polygon returned 0 valid items', latencyMs: Date.now() - start };
    }
    return { ok: true, data, latencyMs: Date.now() - start };
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err), latencyMs: Date.now() - start };
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test server/tests/sources/polygon.test.ts`
Expected: PASS (3 cases)

- [ ] **Step 5: Wire into assemble.ts**

`server/lib/assemble.ts` 의 BITGET_SYMBOL_TO_TICKER 다음에 추가:
```ts
// Polygon/TwelveData: QQQ ETF only — index symbol 제외 (unit 일관성, §2.7)
const POLYGON_SYMBOL_TO_TICKER: Record<string, string> = {
  QQQ: 'nq',
};
```

bitget 통합 분기 다음에 추가:
```ts
if (sources.polygon.ok) {
  for (const pp of sources.polygon.data) {
    const tickerKey = POLYGON_SYMBOL_TO_TICKER[pp.symbol];
    if (!tickerKey) continue;
    tickers[tickerKey] = { ...(tickers[tickerKey] ?? {}), polygon: pp };
  }
}
```

- [ ] **Step 6: Wire into server/index.ts**

`server/index.ts` Promise.all 8번째 줄 (`Promise.resolve(disabledResult)` for polygon) 을 교체:
```ts
singleFlight('source:polygon',     SOURCE_TTL_MS.polygon,     () => fetchPolygon(['QQQ'])),
```

- [ ] **Step 7: Verify**

Run: `pnpm exec tsc --noEmit && pnpm test`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add server/lib/sources/polygon.ts server/tests/sources/polygon.test.ts \
        server/lib/assemble.ts server/index.ts
git commit -m "feat(server/polygon): 옵셔널 Polygon QQQ source + env disabled skip (Wave 5a)

- fetchPolygon: /v2/aggs/ticker/QQQ/prev, POLYGON_API_KEY 분기
- env 키 없으면 {ok:false, error:'disabled'} (sourceHealth 면제)
- TTL 12s (5/min cap)
- assemble.ts: POLYGON_SYMBOL_TO_TICKER 'QQQ' → 'nq' (ETF only, §2.7)

Spec §2.5 / Codex ⚠️ #1,#3"
```

---

## Task 7: Wave 5b — TwelveData 옵셔널 source

**Files:**
- Create: `server/lib/sources/twelvedata.ts`
- Create: `server/tests/sources/twelvedata.test.ts`
- Modify: `server/lib/assemble.ts` (TWELVEDATA_SYMBOL_TO_TICKER + 통합 분기)
- Modify: `server/index.ts` (twelvedata 활성화)

**TwelveData endpoint**:
- `GET https://api.twelvedata.com/price?symbol=QQQ&apikey=...`
- 응답: `{ price: "572.45" }` (단순). change% 별도 endpoint 필요 — 생략 (생략 가능, headline 만으로 충분).
- `GET https://api.twelvedata.com/quote?symbol=QQQ&apikey=...` 가 change 포함 → 이것 사용.

- [ ] **Step 1: Write failing test**

`server/tests/sources/twelvedata.test.ts`:
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { fetchTwelveData } from '../../lib/sources/twelvedata.js';

afterEach(() => {
  vi.restoreAllMocks();
  delete process.env.TWELVEDATA_API_KEY;
});

describe('fetchTwelveData (옵셔널 source)', () => {
  it('returns {ok:false, error:"disabled"} when TWELVEDATA_API_KEY not set', async () => {
    const result = await fetchTwelveData(['QQQ']);
    expect(result.ok).toBe(false);
    if (result.ok) throw new Error('unreachable');
    expect(result.error).toBe('disabled');
  });

  it('parses /quote response when API key set', async () => {
    process.env.TWELVEDATA_API_KEY = 'test-key';
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({
        symbol: 'QQQ',
        close: '572.45',
        percent_change: '1.63',
        volume: '12345',
        timestamp: 1700000000,
      }), { status: 200 }) as unknown as Response,
    );
    const result = await fetchTwelveData(['QQQ']);
    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error('unreachable');
    expect(result.data[0]).toMatchObject({
      source: 'twelvedata',
      symbol: 'QQQ',
      price: 572.45,
      unit: 'USD',
    });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test server/tests/sources/twelvedata.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement fetchTwelveData**

`server/lib/sources/twelvedata.ts` (신규):
```ts
import type { PricePoint, Result } from '@shared/types/prices.js';

const SCHEMA_VERSION = 1;
const TIMEOUT_MS = 5000;
const TD_BASE = 'https://api.twelvedata.com';

export const TWELVEDATA_SYMBOLS = ['QQQ'] as const;

type TDQuoteResponse = {
  symbol?: string;
  close?: string;
  percent_change?: string;
  volume?: string;
  timestamp?: number;
  status?: string;
  code?: number;
  message?: string;
};

function parseNum(v: unknown): number | undefined {
  if (typeof v === 'number') return Number.isFinite(v) ? v : undefined;
  if (typeof v !== 'string') return undefined;
  const n = Number(v);
  return Number.isFinite(n) ? n : undefined;
}

export async function fetchTwelveData(
  symbols: readonly string[] = TWELVEDATA_SYMBOLS,
): Promise<Result<PricePoint[]>> {
  const token = process.env.TWELVEDATA_API_KEY;
  if (!token) {
    return { ok: false, error: 'disabled', latencyMs: 0 };
  }

  const start = Date.now();
  try {
    const results = await Promise.all(symbols.map(async (sym): Promise<PricePoint | null> => {
      const url = `${TD_BASE}/quote?symbol=${encodeURIComponent(sym)}&apikey=${encodeURIComponent(token)}`;
      const controller = new AbortController();
      const timeout = setTimeout(() => controller.abort(), TIMEOUT_MS);
      try {
        const res = await fetch(url, { signal: controller.signal });
        if (!res.ok) throw new Error(`TwelveData HTTP ${res.status}`);
        const raw = (await res.json()) as TDQuoteResponse;
        if (raw.status === 'error' || raw.code) {
          throw new Error(`TwelveData error: ${raw.message ?? 'unknown'}`);
        }
        const price = parseNum(raw.close);
        if (price === undefined) return null;
        return {
          source: 'twelvedata',
          symbol: sym,
          price,
          unit: 'USD',
          change24hPct: parseNum(raw.percent_change),
          volume24hUsd: parseNum(raw.volume),
          status: 'ok',
          asOf: raw.timestamp ? raw.timestamp * 1000 : Date.now(),
          receivedAt: Date.now(),
          schemaVersion: SCHEMA_VERSION,
        };
      } finally {
        clearTimeout(timeout);
      }
    }));

    const data = results.filter((p): p is PricePoint => p !== null);
    if (data.length === 0) {
      return { ok: false, error: 'TwelveData returned 0 valid items', latencyMs: Date.now() - start };
    }
    return { ok: true, data, latencyMs: Date.now() - start };
  } catch (err) {
    return { ok: false, error: err instanceof Error ? err.message : String(err), latencyMs: Date.now() - start };
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test server/tests/sources/twelvedata.test.ts`
Expected: PASS

- [ ] **Step 5: Wire into assemble.ts**

`server/lib/assemble.ts` 의 POLYGON_SYMBOL_TO_TICKER 다음에 추가:
```ts
const TWELVEDATA_SYMBOL_TO_TICKER: Record<string, string> = {
  QQQ: 'nq',
};
```

polygon 통합 분기 다음에 추가:
```ts
if (sources.twelvedata.ok) {
  for (const pp of sources.twelvedata.data) {
    const tickerKey = TWELVEDATA_SYMBOL_TO_TICKER[pp.symbol];
    if (!tickerKey) continue;
    tickers[tickerKey] = { ...(tickers[tickerKey] ?? {}), twelvedata: pp };
  }
}
```

- [ ] **Step 6: Wire into server/index.ts**

`server/index.ts` Promise.all 9번째 줄 (`Promise.resolve(disabledResult)` for twelvedata) 을 교체:
```ts
singleFlight('source:twelvedata',  SOURCE_TTL_MS.twelvedata,  () => fetchTwelveData(['QQQ'])),
```

- [ ] **Step 7: Verify**

Run: `pnpm exec tsc --noEmit && pnpm test`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add server/lib/sources/twelvedata.ts server/tests/sources/twelvedata.test.ts \
        server/lib/assemble.ts server/index.ts
git commit -m "feat(server/twelvedata): 옵셔널 TwelveData QQQ source (Wave 5b)

- fetchTwelveData: /quote endpoint, TWELVEDATA_API_KEY 분기
- TTL 120s (800/day cap)
- assemble.ts: TWELVEDATA_SYMBOL_TO_TICKER 'QQQ' → 'nq' (ETF only)

Spec §2.5"
```

---

## Task 8: Wave 6a — IndexCompactCard fallback chain + 5 VenueRow + source label

**Files:**
- Modify: `src/components/IndexCompactCard.tsx`
- Modify: `src/components/IndexCompactCard.test.tsx` (테스트 케이스 추가)

- [ ] **Step 1: Write failing test for pickHeadlineSource**

`src/components/IndexCompactCard.test.tsx` 끝에 추가 (마지막 `});` 직전):

```tsx
const mkNqPP = (source: SourceName, price: number, change = 1.63) =>
  mkPP({ source, symbol: source === 'hyperliquid' ? 'xyz_NQ' : 'QQQ', price, unit: source === 'hyperliquid' ? 'USD' : (source === 'binance' || source === 'bybit' || source === 'bitget' ? 'USDT' : 'USD'), change24hPct: change });

describe('IndexCompactCard NQ — v0.4.2 fallback chain + 5 VenueRow', () => {
  it('uses Binance when HL+Yahoo missing (priority: hl > yahoo > binance > bybit > bitget)', () => {
    const payload: TickerPayload = {
      binance: mkNqPP('binance', 572.45),
    };
    render(<IndexCompactCard ticker="nq" label="Nasdaq" payload={payload} fx={fxOk} />);
    // 572.45 * 1470 = 841,501
    expect(screen.getByText(/₩841,501/, { selector: '.index-compact-headline' })).toBeInTheDocument();
    expect(screen.getByText(/출처\s+Binance/)).toBeInTheDocument();
  });

  it('uses Bybit when Binance also missing', () => {
    const payload: TickerPayload = {
      bybit: mkNqPP('bybit', 572.45),
    };
    render(<IndexCompactCard ticker="nq" label="Nasdaq" payload={payload} fx={fxOk} />);
    expect(screen.getByText(/출처\s+Bybit/)).toBeInTheDocument();
  });

  it('uses Bitget when Bybit also missing', () => {
    const payload: TickerPayload = {
      bitget: mkNqPP('bitget', 572.45),
    };
    render(<IndexCompactCard ticker="nq" label="Nasdaq" payload={payload} fx={fxOk} />);
    expect(screen.getByText(/출처\s+Bitget/)).toBeInTheDocument();
  });

  it('uses Polygon when no perp source available', () => {
    const payload: TickerPayload = {
      polygon: mkNqPP('polygon', 572.45),
    };
    render(<IndexCompactCard ticker="nq" label="Nasdaq" payload={payload} fx={fxOk} />);
    expect(screen.getByText(/출처\s+Polygon/)).toBeInTheDocument();
  });

  it('uses TwelveData as last fallback', () => {
    const payload: TickerPayload = {
      twelvedata: mkNqPP('twelvedata', 572.45),
    };
    render(<IndexCompactCard ticker="nq" label="Nasdaq" payload={payload} fx={fxOk} />);
    expect(screen.getByText(/출처\s+TwelveData/)).toBeInTheDocument();
  });

  it('renders bybit + bitget VenueRows when present', () => {
    const payload: TickerPayload = {
      binance: mkNqPP('binance', 572.45),
      bybit: mkNqPP('bybit', 572.50),
      bitget: mkNqPP('bitget', 572.40),
    };
    render(<IndexCompactCard ticker="nq" label="Nasdaq" payload={payload} fx={fxOk} />);
    expect(screen.getByText('Binance')).toBeInTheDocument();
    expect(screen.getByText('Bybit')).toBeInTheDocument();
    expect(screen.getByText('Bitget')).toBeInTheDocument();
  });

  it('does NOT render Polygon/TwelveData as VenueRow (headline fallback only)', () => {
    const payload: TickerPayload = {
      binance: mkNqPP('binance', 572.45),
      polygon: mkNqPP('polygon', 572.50),
      twelvedata: mkNqPP('twelvedata', 572.40),
    };
    render(<IndexCompactCard ticker="nq" label="Nasdaq" payload={payload} fx={fxOk} />);
    expect(screen.getByText('Binance')).toBeInTheDocument();
    expect(screen.queryByText('Polygon', { selector: '.venue-pill' })).not.toBeInTheDocument();
    expect(screen.queryByText('TwelveData', { selector: '.venue-pill' })).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/components/IndexCompactCard.test.tsx`
Expected: FAIL (pickHeadlineSource 미구현 + bybit/bitget VenueRow 미렌더)

- [ ] **Step 3: Rewrite IndexCompactCard.tsx with pickHeadlineSource + 5 VenueRow**

`src/components/IndexCompactCard.tsx` (전체 교체):
```tsx
import type { TickerPayload, FxRates, PricePoint } from '@shared/types/prices.js';
import { VenueRow } from './VenueRow';
import { SpreadRow } from './SpreadRow';
import { fmtKrw, fmtUsd } from '../lib/format';

const SP500_REFERENCE_RATIO = 10;

type Props = {
  ticker: string;
  label: string;
  payload?: TickerPayload;
  fx?: FxRates;
};

/** v0.4.2 — NQ headline fallback chain (모든 source = USD/USDT scale, §2.7).
 *  Priority: HL > Yahoo > Binance > Bybit > Bitget > Polygon > TwelveData. */
function pickHeadlineSource(
  payload: TickerPayload,
  fxAvailable: boolean,
  usdtKrw: number,
): { value: number; source: string; label: string } | null {
  if (!fxAvailable) return null;
  const candidates: Array<[string, PricePoint | undefined, string]> = [
    ['hl',         payload.hl,         'HL'],
    ['yahoo',      payload.yahoo,      'Yahoo'],
    ['binance',    payload.binance,    'Binance'],
    ['bybit',      payload.bybit,      'Bybit'],
    ['bitget',     payload.bitget,     'Bitget'],
    ['polygon',    payload.polygon,    'Polygon'],
    ['twelvedata', payload.twelvedata, 'TwelveData'],
  ];
  for (const [name, pp, label] of candidates) {
    if (pp && Number.isFinite(pp.price) && pp.price > 0) {
      return { value: pp.price * usdtKrw, source: name, label };
    }
  }
  return null;
}

export function IndexCompactCard({ ticker, label, payload, fx }: Props) {
  const hasAnyVenue = !!(
    payload?.hl || payload?.yahoo || payload?.binance ||
    payload?.bybit || payload?.bitget || payload?.polygon || payload?.twelvedata
  );
  if (!payload || !hasAnyVenue) {
    return (
      <article className="card index-compact card-loading" data-testid="index-compact-loading">
        <header className="card-head">
          <h3 className="ts-card-title">{label}</h3>
          <span className="ts-subtitle">{ticker}</span>
        </header>
        <div className="index-compact-headline">—</div>
      </article>
    );
  }

  const usdtKrw = fx?.usdtKrw ?? 0;
  const fxAvailable = usdtKrw > 0;
  const sp500Multiplier = ticker === 'sp500' ? SP500_REFERENCE_RATIO : 1;

  // NQ 카드: pickHeadlineSource 사용 (v0.4.2). EWY/SP500: 기존 HL→Binance fallback 유지.
  const useFallbackChain = ticker === 'nq';

  const hlKrw = payload.hl && fxAvailable ? payload.hl.price * usdtKrw : null;
  const binanceKrw = payload.binance && fxAvailable
    ? payload.binance.price * usdtKrw * sp500Multiplier
    : null;

  const headline = useFallbackChain
    ? pickHeadlineSource(payload, fxAvailable, usdtKrw)
    : (hlKrw !== null ? { value: hlKrw, source: 'hl', label: 'HL' }
       : binanceKrw !== null ? { value: binanceKrw, source: 'binance', label: 'Binance' }
       : null);

  const showKrwHeadline = fxAvailable && headline !== null;
  const showUsdHeadline = !fxAvailable && !!payload.hl;

  return (
    <article className="card index-compact">
      <header className="card-head">
        <h3 className="ts-card-title">{label}</h3>
        <span className="ts-subtitle">{ticker}</span>
      </header>

      {showKrwHeadline && (
        <>
          <div className="index-compact-headline ts-index-headline">
            {fmtKrw(headline!.value, 0)}
          </div>
          <div className="index-compact-usdt ts-subtitle">
            ≈ {fmtUsd(headline!.value / usdtKrw)} USDT
            {useFallbackChain && ` · 출처 ${headline!.label}`}
          </div>
        </>
      )}
      {showUsdHeadline && (
        <div className="index-compact-headline ts-index-headline">
          {fmtUsd(payload.hl!.price)}
        </div>
      )}

      <div className="venues">
        <VenueRow source="hyperliquid" pp={payload.hl} />
        <VenueRow source="yahoo" pp={payload.yahoo} />
        <VenueRow source="binance" pp={payload.binance} />
        <VenueRow source="bybit" pp={payload.bybit} />
        <VenueRow source="bitget" pp={payload.bitget} />
      </div>

      {ticker === 'sp500' && hlKrw !== null && binanceKrw !== null && (
        <div className="krw-conversions num" title="Both venues normalized to KRW via Upbit USDT-KRW + server reference ratio 10x for SPY">
          HL: {fmtKrw(hlKrw, 0)} | Binance×10: {fmtKrw(binanceKrw, 0)}
        </div>
      )}
      {ticker !== 'sp500' && ticker !== 'nq' && hlKrw !== null && binanceKrw !== null && (
        <div className="krw-conversions num">
          HL: {fmtKrw(hlKrw, 0)} | Binance: {fmtKrw(binanceKrw, 0)}
        </div>
      )}

      <SpreadRow
        spread={payload.spread}
        warningNote={ticker === 'sp500' ? 'HL=index pt, Binance=SPY ETF (~10x ratio, server-normalized)' : undefined}
      />
    </article>
  );
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pnpm test src/components/IndexCompactCard.test.tsx`
Expected: PASS (기존 + 신규 케이스)

- [ ] **Step 5: Verify no regression in EWY/SP500 cards**

Run: `pnpm test`
Expected: 전부 PASS

- [ ] **Step 6: Commit**

```bash
git add src/components/IndexCompactCard.tsx src/components/IndexCompactCard.test.tsx
git commit -m "feat(client/IndexCompactCard): NQ fallback chain + 5 VenueRow + source label (Wave 6a)

- pickHeadlineSource: HL > Yahoo > Binance > Bybit > Bitget > Polygon > TwelveData
- NQ 카드 only — EWY/SP500 은 기존 HL→Binance 분기 유지
- VenueRow 5개 (bybit/bitget 추가). Polygon/TwelveData 는 headline fallback only
- 출처 라벨 (Bybit/Bitget/Polygon/TwelveData) micro display

Spec §3.2 / §3.3 / Codex 🚨 #2 (option C) / Codex ⚠️ B"
```

---

## Task 9: Wave 6b — assemble NQ fallback test + NQ unit consistency test

**Files:**
- Create: `server/tests/assemble-nq-fallback.test.ts`
- Create: `server/tests/nq-unit-consistency.test.ts`

- [ ] **Step 1: Write assemble-nq-fallback test**

`server/tests/assemble-nq-fallback.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { assemblePricesResponse, type SourceInputs } from '../lib/assemble.js';
import { _resetSourceHealth } from '../lib/health.js';
import type { PricePoint, Result } from '@shared/types/prices.js';

const now = Date.now();
const disabledResult: Result<PricePoint[]> = { ok: false, error: 'disabled', latencyMs: 0 };
const emptyResult: Result<PricePoint[]> = { ok: false, error: 'all sources failed', latencyMs: 0 };

const mkPP = (source: PricePoint['source'], symbol: string, price: number, unit: PricePoint['unit'] = 'USDT'): PricePoint => ({
  source, symbol, price, unit, status: 'ok', asOf: now, receivedAt: now, schemaVersion: 1,
});

const baseFx = (extra: Partial<SourceInputs> = {}): SourceInputs => ({
  hl: { ok: true, data: [
    { ...mkPP('hyperliquid', 'xyz_KRW', 1471.7, 'USD') },
  ], latencyMs: 1 },
  naver: { ok: false, error: 'n/a', latencyMs: 0 },
  yahoo: { ok: true, data: [
    { ...mkPP('yahoo', 'KRW=X', 1471.7, 'KRW') },
  ], latencyMs: 1 },
  upbit: { ok: true, data: [
    { ...mkPP('upbit', 'KRW-USDT', 1470, 'KRW') },
  ], latencyMs: 1 },
  binance: emptyResult,
  bybit: disabledResult,
  bitget: disabledResult,
  polygon: disabledResult,
  twelvedata: disabledResult,
  ...extra,
});

describe('assemble — NQ fallback (no alias mutation)', () => {
  beforeEach(() => _resetSourceHealth());

  it('keeps bybit in payload.bybit slot (not binance alias)', () => {
    const resp = assemblePricesResponse(baseFx({
      bybit: { ok: true, data: [mkPP('bybit', 'QQQUSDT', 572.45)], latencyMs: 1 },
    }));
    expect(resp.tickers.nq).toBeDefined();
    expect(resp.tickers.nq.bybit).toBeDefined();
    expect(resp.tickers.nq.bybit?.source).toBe('bybit');
    expect(resp.tickers.nq.binance).toBeUndefined();  // alias 없음 검증
  });

  it('keeps bitget in payload.bitget slot', () => {
    const resp = assemblePricesResponse(baseFx({
      bitget: { ok: true, data: [mkPP('bitget', 'QQQUSDT', 572.45)], latencyMs: 1 },
    }));
    expect(resp.tickers.nq.bitget?.source).toBe('bitget');
  });

  it('keeps all 4 venue slots when all sources succeed', () => {
    const resp = assemblePricesResponse(baseFx({
      binance: { ok: true, data: [mkPP('binance', 'QQQUSDT', 572.45)], latencyMs: 1 },
      bybit: { ok: true, data: [mkPP('bybit', 'QQQUSDT', 572.50)], latencyMs: 1 },
      bitget: { ok: true, data: [mkPP('bitget', 'QQQUSDT', 572.40)], latencyMs: 1 },
      polygon: { ok: true, data: [mkPP('polygon', 'QQQ', 572.30, 'USD')], latencyMs: 1 },
    }));
    expect(resp.tickers.nq.binance).toBeDefined();
    expect(resp.tickers.nq.bybit).toBeDefined();
    expect(resp.tickers.nq.bitget).toBeDefined();
    expect(resp.tickers.nq.polygon).toBeDefined();
  });

  it('omits nq ticker entirely when all sources fail (loading state)', () => {
    const resp = assemblePricesResponse(baseFx());
    // nq 가 entry 자체 없음 (Wave 0 동작 그대로)
    expect(resp.tickers.nq).toBeUndefined();
  });

  it('disabled optional source does not pollute sourceHealth', () => {
    const resp = assemblePricesResponse(baseFx({
      bybit: { ok: true, data: [mkPP('bybit', 'QQQUSDT', 572.45)], latencyMs: 1 },
    }));
    // polygon/twelvedata = disabled → consecutiveFailures 0 그대로
    expect(resp.sourceHealth.polygon.consecutiveFailures).toBe(0);
    expect(resp.sourceHealth.twelvedata.consecutiveFailures).toBe(0);
    expect(resp.sourceHealth.polygon.lastSuccess).toBe(0);  // 미시도 상태
  });
});
```

- [ ] **Step 2: Write nq-unit-consistency test**

`server/tests/nq-unit-consistency.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { assemblePricesResponse, type SourceInputs } from '../lib/assemble.js';
import { _resetSourceHealth } from '../lib/health.js';
import type { PricePoint } from '@shared/types/prices.js';

const now = Date.now();
const mkPP = (source: PricePoint['source'], symbol: string, price: number, unit: PricePoint['unit']): PricePoint => ({
  source, symbol, price, unit, status: 'ok', asOf: now, receivedAt: now, schemaVersion: 1,
});

const QQQ_RANGE: [number, number] = [400, 800];  // QQQ ETF reasonable 2024-2026 range

describe('NQ unit consistency — all sources ~$500-700 USD/USDT', () => {
  beforeEach(() => _resetSourceHealth());

  it('rejects unit drift (e.g. NDX index 21000pt) by source filter', () => {
    // 시뮬레이션: 만약 yahoo 가 ^NDX (21000pt) 를 nq slot 에 넣으면 unit consistency 위반
    // 현재 assemble.ts 는 YAHOO_SYMBOL_TO_TICKER 에 ^NDX 없음 → drop 처리 검증.
    const inputs: SourceInputs = {
      hl: { ok: false, error: 'n/a', latencyMs: 0 },
      naver: { ok: false, error: 'n/a', latencyMs: 0 },
      yahoo: { ok: true, data: [mkPP('yahoo', '^NDX', 21000, 'pt')], latencyMs: 1 },  // drop 대상
      upbit: { ok: true, data: [mkPP('upbit', 'KRW-USDT', 1470, 'KRW')], latencyMs: 1 },
      binance: { ok: true, data: [mkPP('binance', 'QQQUSDT', 572.45, 'USDT')], latencyMs: 1 },
      bybit: { ok: false, error: 'disabled', latencyMs: 0 },
      bitget: { ok: false, error: 'disabled', latencyMs: 0 },
      polygon: { ok: false, error: 'disabled', latencyMs: 0 },
      twelvedata: { ok: false, error: 'disabled', latencyMs: 0 },
    };
    const resp = assemblePricesResponse(inputs);
    expect(resp.tickers.nq).toBeDefined();
    // ^NDX 는 YAHOO_SYMBOL_TO_TICKER 에 없음 → drop
    expect(resp.tickers.nq.yahoo).toBeUndefined();
    // binance 는 정상 range
    expect(resp.tickers.nq.binance?.price).toBeGreaterThanOrEqual(QQQ_RANGE[0]);
    expect(resp.tickers.nq.binance?.price).toBeLessThanOrEqual(QQQ_RANGE[1]);
  });

  it('accepts QQQ ETF from Yahoo (USD)', () => {
    const inputs: SourceInputs = {
      hl: { ok: false, error: 'n/a', latencyMs: 0 },
      naver: { ok: false, error: 'n/a', latencyMs: 0 },
      yahoo: { ok: true, data: [mkPP('yahoo', 'QQQ', 572.45, 'USD')], latencyMs: 1 },
      upbit: { ok: true, data: [mkPP('upbit', 'KRW-USDT', 1470, 'KRW')], latencyMs: 1 },
      binance: { ok: false, error: 'n/a', latencyMs: 0 },
      bybit: { ok: false, error: 'disabled', latencyMs: 0 },
      bitget: { ok: false, error: 'disabled', latencyMs: 0 },
      polygon: { ok: false, error: 'disabled', latencyMs: 0 },
      twelvedata: { ok: false, error: 'disabled', latencyMs: 0 },
    };
    const resp = assemblePricesResponse(inputs);
    expect(resp.tickers.nq.yahoo?.price).toBe(572.45);
    expect(resp.tickers.nq.yahoo?.unit).toBe('USD');
  });

  it('all 4 venue sources fall within QQQ range', () => {
    const inputs: SourceInputs = {
      hl: { ok: false, error: 'n/a', latencyMs: 0 },
      naver: { ok: false, error: 'n/a', latencyMs: 0 },
      yahoo: { ok: true, data: [mkPP('yahoo', 'QQQ', 572.45, 'USD')], latencyMs: 1 },
      upbit: { ok: true, data: [mkPP('upbit', 'KRW-USDT', 1470, 'KRW')], latencyMs: 1 },
      binance: { ok: true, data: [mkPP('binance', 'QQQUSDT', 572.50, 'USDT')], latencyMs: 1 },
      bybit: { ok: true, data: [mkPP('bybit', 'QQQUSDT', 572.55, 'USDT')], latencyMs: 1 },
      bitget: { ok: true, data: [mkPP('bitget', 'QQQUSDT', 572.40, 'USDT')], latencyMs: 1 },
      polygon: { ok: false, error: 'disabled', latencyMs: 0 },
      twelvedata: { ok: false, error: 'disabled', latencyMs: 0 },
    };
    const resp = assemblePricesResponse(inputs);
    const venues = [
      resp.tickers.nq.yahoo,
      resp.tickers.nq.binance,
      resp.tickers.nq.bybit,
      resp.tickers.nq.bitget,
    ];
    for (const v of venues) {
      expect(v).toBeDefined();
      expect(v!.price).toBeGreaterThanOrEqual(QQQ_RANGE[0]);
      expect(v!.price).toBeLessThanOrEqual(QQQ_RANGE[1]);
    }
  });
});
```

- [ ] **Step 3: Run tests to verify they pass**

Run: `pnpm test server/tests/assemble-nq-fallback.test.ts server/tests/nq-unit-consistency.test.ts`
Expected: PASS

- [ ] **Step 4: Run full suite (regression check)**

Run: `pnpm test`
Expected: 전부 PASS

- [ ] **Step 5: Commit**

```bash
git add server/tests/assemble-nq-fallback.test.ts server/tests/nq-unit-consistency.test.ts
git commit -m "test(assemble): NQ fallback chain + unit consistency (Wave 6b)

- assemble-nq-fallback.test.ts: 5 케이스 — alias 미발생, 4 venue 슬롯 분리, disabled sourceHealth 면제
- nq-unit-consistency.test.ts: ^NDX/NQ=F 같은 unit 위반 drop, QQQ ETF range ~\$570 검증

Spec §4.1 / Codex 🚨 I"
```

---

## Task 10: Wave 7 — README + .env.example 갱신

**Files:**
- Modify: `README.md`
- Modify: `.env.example`

- [ ] **Step 1: Add source matrix section to README.md**

먼저 기존 README 구조 확인:
```bash
grep -n "^##" README.md | head -20
```

Source matrix 표를 적절한 위치 (가이드/배포 섹션 근처) 에 추가. 다음 마크다운 블록을 README.md 적절한 위치에 삽입:

```markdown
## Source Matrix (v0.4.2)

| Source | Symbol | 가입 URL | Free quota | Note |
|---|---|---|---|---|
| Hyperliquid xyz | xyz:SMSN 등 7 | — | 무제한 | NQ 미상장 |
| Naver Finance | 005930 등 | — | 권장 7s polling | 한국 주식 |
| Yahoo Finance | EWY, QQQ, ES=F, ^GSPC | — | rate-limited (한국 IP 차단) | 1차 — NQ=F/^NDX 제거됨 (§2.7) |
| Finnhub | QQQ | https://finnhub.io/register | 60/min | Yahoo Tier-4 fallback (ETF only) |
| Binance Futures | EWYUSDT, SPYUSDT, QQQUSDT | — | 6000 weight/min | 1회 재시도 + Retry-After |
| **Bybit Linear** ★ | QQQUSDT | — | 60 req/s | v0.4.2 신규 |
| **Bitget USDT-M** ★ | QQQUSDT | — | 20 req/s | v0.4.2 신규 |
| Polygon.io (opt) | QQQ | https://polygon.io/dashboard/api-keys | 5/min free | paid 권장. ETF only (I:NDX 제외) |
| TwelveData (opt) | QQQ | https://twelvedata.com/account/api-keys | 800/day free | paid 권장. ETF only (NDX 제외) |
| Upbit | USDT-KRW | — | 무료 | FX 전용 |

### NQ Unit 정규화 (v0.4.2)

모든 NQ source 가 **QQQ ETF USD 가격** (~$570 range) 으로 통일됨. 이전 버전의 NQ=F futures pt + ^NDX index pt (단위 불일치) 는 v0.4.2 에서 제거.

### Vercel 배포 시 알려진 제약

v0.4.2 옵셔널 source (Polygon/TwelveData) 는 free tier 운영 시 daily quota 소진 위험. 프로덕션은 paid tier 권장.
```

- [ ] **Step 2: Add new env vars to .env.example**

`.env.example` 끝에 추가:
```
# === v0.4.2 신규 ===

# Yahoo Tier-4 fallback — Yahoo rate limit 시 자동 fallback (ETF 전용)
# 가입: https://finnhub.io/register (무료 60/min)
# FINNHUB_TOKEN=

# Polygon.io 옵셔널 — NQ 추가 source. 무료 5/min (paid 권장).
# 가입: https://polygon.io/dashboard/api-keys
# POLYGON_API_KEY=

# TwelveData 옵셔널 — NQ 추가 source. 무료 800/day (paid 권장).
# 가입: https://twelvedata.com/account/api-keys
# TWELVEDATA_API_KEY=
```

- [ ] **Step 3: Verify**

Run: `cat README.md | grep -A 2 "Source Matrix"`
Expected: 표 헤더 확인됨

Run: `cat .env.example | grep -E "FINNHUB|POLYGON|TWELVEDATA"`
Expected: 3 줄 출력

- [ ] **Step 4: Commit**

```bash
git add README.md .env.example
git commit -m "docs(readme): v0.4.2 source matrix + .env.example (Wave 7)

- README: Source Matrix 표 (4 신규 source + 가입 URL + free quota)
- README: NQ Unit 정규화 설명 (§2.7)
- README: Vercel daily quota 경고
- .env.example: FINNHUB_TOKEN, POLYGON_API_KEY, TWELVEDATA_API_KEY 주석

Spec §8.5 / Codex ⚠️ #9"
```

---

## Task 11: Wave 8 — Codex final review + smoke acceptance

**Files:** (검증만, 수정은 review 결과에 따라)

- [ ] **Step 1: Run full build + test suite**

Run:
```bash
pnpm exec tsc --noEmit
pnpm test
pnpm build
```
Expected: 전부 PASS

- [ ] **Step 2: Run /codex review on v0.4.2 diff**

git log 로 v0.4.2 commit 범위 확인:
```bash
git log --oneline a333557..HEAD
```

Codex 에 diff 전달:
```bash
git diff a333557..HEAD --stat
```

Codex 프롬프트:
```
v0.4.2 (NQ fix multi-source redundancy) final review:
- spec: docs/superpowers/specs/2026-05-12-v0.4.2-nq-fix-design.md (v3.1)
- 변경 범위: commit a333557 ~ HEAD
- 검증 포인트:
  1. NQ unit 정규화 (모든 source = QQQ ETF USD scale, §2.7) — drift 검증
  2. fallback chain priority (hl > yahoo > binance > bybit > bitget > polygon > twelvedata)
  3. optional source skip ({ok:false, error:'disabled'}) — sourceHealth 미오염
  4. binance retry — Retry-After 헤더 사용 + 5s cap
  5. Record<SourceName,X> 9-key 완전성 (health.ts, recordSourceAttempt, VenueRow)
  6. v0.4.1 client backward compat — 신규 sourceHealth 키 무시 정상

[카파시 / AI slop 금지 추가 검증]
- 새 source 4개 (bybit/bitget/polygon/twelvedata) 가 binance.ts 패턴 1:1 복제인가?
- 신규 추상화/팩토리/매니저 도입됐는가?
- 1-use 헬퍼 있는가?
- Finnhub fallback 이 yahoo.ts 분기 안에 있는가? (별도 파일 finnhub.ts 는 helper only)
- 사용자 미지시 기능 삽입 있는가?

Output: BLOCK/FLAG/PASS verdict per issue.
```

- [ ] **Step 3: Resolve Codex BLOCK/FLAG (if any)**

BLOCK → 즉시 수정 + 재검토.
FLAG → 우선순위 결정 (즉시 수정 vs v0.5.0 backlog).
PASS → Step 4.

- [ ] **Step 4: Manual smoke — 4 acceptance criteria (spec §7 Wave 8)**

서버 재시작 + 4가지 검증:

```bash
# 서버 재시작
pkill -f "tsx watch.*server/index.ts" 2>/dev/null
sleep 1
pnpm dev:server > /tmp/v042-smoke.log 2>&1 &
sleep 5

# 1. NQ ticker 가 1+ venue 응답
curl -s http://localhost:3001/api/prices | jq '.tickers.nq | keys'
# Expected: ["binance", "bitget", "bybit", ...] 최소 1개

# 2. headline 가격 ~₩838K (QQQ ETF ≈ $570 × usdtKrw ≈ 1470)
# 브라우저 (http://localhost:5173) 에서 NQ 카드 가격 확인
# Expected: 800,000원 ~ 900,000원 범위

# 3. 헤드라인 옆 "출처 Binance" / "출처 Bybit" 등 micro label
# 브라우저에서 NQ 카드 USDT sub-label 확인
# Expected: "≈ $570.45 USDT · 출처 Binance" 형식

# 4. /api/internal/health 가 9 source keys 노출
# HEALTH_TOKEN 설정 후 (또는 .env.local 에 추가):
# curl -s -H "Authorization: Bearer $HEALTH_TOKEN" http://localhost:3001/api/internal/health | jq '.sources | keys'
# Expected: ["binance", "bitget", "bybit", "hyperliquid", "naver", "polygon", "twelvedata", "upbit", "yahoo"]
```

- [ ] **Step 5: Verify v0.4.1 backward compat (Codex ⚠️ C)**

옛 client 가 신규 sourceHealth 키 (`bybit`/`bitget`/`polygon`/`twelvedata`) 무시 정상 동작 확인:
- 옛 SWR cache 가 신규 키 ignore (TypeScript optional 이므로 safe)
- 브라우저 새로고침 후 NQ 카드 정상 렌더

- [ ] **Step 6: Final commit (release tag)**

```bash
git log --oneline a333557..HEAD
# v0.4.2 완료 표시 (선택적)
```

사용자에게 PR merge 결정 위임. plan 의 implementation 단계 종료.

---

## Self-Review

(plan 작성자가 spec v3.1 대비 점검)

### 1. Spec coverage

| Spec section | Task 매핑 |
|---|---|
| §2.1 Bybit | Task 2 |
| §2.2 Bitget | Task 3 |
| §2.3 Yahoo Finnhub fallback | Task 4 |
| §2.4 Binance retry + Retry-After | Task 5 |
| §2.5 Polygon + TwelveData 옵셔널 | Task 6, 7 |
| §2.6 assemble.ts mapping 확장 | Task 2, 3, 6, 7 |
| §2.7 NQ unit 정규화 (QQQ ETF) | Task 4 |
| §3.1 IndexCompactCard 동작 (loading skeleton) | Task 8 |
| §3.2 pickHeadlineSource fallback chain + source label | Task 8 |
| §3.3 VenueRow 5개 + Polygon/TwelveData 미렌더 | Task 8 |
| §4.1 단위 테스트 (8 개) | Task 2,3,4,5,6,7,8,9 모두 |
| §4.3 sourceHealth 모니터링 | Task 1 (`disabled` 면제 로직) |
| §5 Risks R1-R8 | 명시적 mitigation = TTL (R2,R3,R6) Task 2/Task 6/Task 7, retry budget (R7) Task 5, R8 (Polygon 깜빡임) — acknowledge only |
| §6 검증 기준 1-10 | Task 11 |
| §7 Wave 1-8 | Task 1-11 |
| §8.5 README + .env.example | Task 10 |

✅ 누락 없음.

### 2. Placeholder scan

- "TBD" / "TODO" / "implement later" — 0건
- "Add appropriate error handling" — 0건
- 모든 step 에 구체적 코드 블록 포함
- 모든 함수 시그니처 명시

✅ Clean.

### 3. Type consistency

- `fetchBybitLinear` / `fetchBitgetFutures` / `fetchPolygon` / `fetchTwelveData` — 모두 `Promise<Result<PricePoint[]>>` 일관
- `SourceName` 9 keys 모든 Task 에서 동일 순서 (hyperliquid, naver, yahoo, binance, upbit, bybit, bitget, polygon, twelvedata)
- `pickHeadlineSource` candidate 순서 (hl > yahoo > binance > bybit > bitget > polygon > twelvedata) — spec §3.2 와 Task 8 일치
- `TickerPayload` venue 슬롯 (hl/naver/yahoo/binance/upbit + 4 신규) — Task 1 정의, Task 8 사용 일치

✅ Consistent.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-12-v0.4.2-nq-fix.md`. Two execution options:

**1. Subagent-Driven (recommended)** — fresh subagent 1개 per Task, two-stage review (spec compliance + code quality) between Tasks, 11 Task 빠른 iteration. v0.4.0 / v0.4.1 에서 사용한 방식과 동일.

**2. Inline Execution** — 현 세션에서 executing-plans 로 batch 실행 + checkpoint 리뷰.

**추천:** **1 (Subagent-Driven)** — 11 Task 가 대부분 isolated source 파일 추가 (TDD 패턴 동일). subagent 가 spec 안 보고 task text 만으로 실행 가능. 본 세션 context 보호.
