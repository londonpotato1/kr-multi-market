# v0.5.0 — 티커 검색 기능 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자가 임의 ticker (종목명 한글/영문 또는 symbol) 검색하면 3-tier resolver (KRX/NASDAQ → CEX → HL xyz) 로 1 venue 추가 + localStorage 영구 watchlist + 실시간 가격 표시.

**Architecture:** Server-side 3-tier resolver (`/api/search` 단일 endpoint). client 가 결과 dropdown 표시 → 사용자 선택 → localStorage. 메인 `/api/prices?watchlist=...` 확장으로 매 SWR poll 마다 동적 source 호출. WatchlistCard = IndexCompactCard wrapper (변경 0).

**Tech Stack:** TypeScript 6 strict, Express 4, Vitest 4, React 19, SWR 2, vitest happy-dom. v0.4.2 의 9 source 인프라 (Finnhub/Naver/Yahoo/Binance/Bybit/Bitget/Polygon/TwelveData/HL xyz) 재사용.

**Spec reference:** `docs/superpowers/specs/2026-05-13-v0.5.0-ticker-search-design.md` (v4, commit `59259e4`, Codex 3회 통과).

**카파시 / AI slop 가드 (모든 task 에 적용)**:
- 신규 파일 production = 8 (frontend 컴포넌트 4 + hook 1 + lib 1 + server source 1 + route 1)
- 신규 test = 11
- IndexCompactCard 변경 0 (WatchlistCard 가 100% wrapper)
- `finnhub.ts` 는 `searchFinnhub` 함수 추가만 (파일 신규 X)
- 추상화/팩토리/Manager/Strategy 패턴 도입 X
- v0.4.2 source 함수 (`fetchBinanceFutures` 등) 재사용, signature 변경 X
- 사용자 미지시 기능 (multi-watchlist, sync, alerts, chart) 절대 금지

---

## File Structure (변경 mapping)

### 신규 파일 (production 8 + tests 11 = 19)

| 경로 | 책임 |
|---|---|
| `shared/types/prices.ts` (modify) | `WatchlistEntry` + `SearchResult` type 확장 |
| `src/components/SearchBar.tsx` (new, ~80) | Input + Enter 제출 + dropdown 표시 trigger |
| `src/components/SearchDropdown.tsx` (new, ~40) | top 5 결과 list + 클릭 핸들러 |
| `src/components/WatchlistSection.tsx` (new, ~40) | "📌 관심 종목" section 컨테이너 |
| `src/components/WatchlistCard.tsx` (new, ~30) | IndexCompactCard wrapper + X 버튼 |
| `src/hooks/useWatchlist.ts` (new, ~50) | localStorage R/W + SWR mutate |
| `src/lib/ticker-resolver.ts` (new, ~30) | `/api/search` HTTP wrapper |
| `server/lib/sources/naver-search.ts` (new, ~60) | Naver 종목 검색 (한글) |
| `server/routes/search.ts` (new, ~80) | `/api/search` 3-tier resolver |
| `server/lib/sources/finnhub.ts` (modify) | `searchFinnhub(q)` export 추가 |
| `server/index.ts` (modify) | `/api/prices?watchlist=...` 파싱 + 동적 source 호출 + `/api/search` route mount |
| `server/lib/assemble.ts` (modify) | 동적 watchlist mapping 통합 |
| `src/App.tsx` (modify) | SearchBar + WatchlistSection 통합 |

### 신규 test 파일 (11)

- `server/tests/sources/finnhub-search.test.ts`
- `server/tests/sources/naver-search.test.ts`
- `server/tests/routes/search.test.ts`
- `server/tests/assemble-dynamic.test.ts`
- `server/tests/prices-watchlist-parsing.test.ts`
- `src/lib/ticker-resolver.test.ts`
- `src/hooks/useWatchlist.test.ts`
- `src/components/SearchBar.test.tsx`
- `src/components/SearchDropdown.test.tsx`
- `src/components/WatchlistCard.test.tsx`
- `src/components/WatchlistSection.test.tsx`

---

## Task 1: Wave 1 — Types 확장 + useWatchlist hook + localStorage

**Files:**
- Modify: `shared/types/prices.ts` (WatchlistEntry, SearchResult 추가)
- Create: `src/hooks/useWatchlist.ts`
- Create: `src/hooks/useWatchlist.test.ts` (test 위치는 hook 옆에 두는 게 v0.4.0 패턴)

- [ ] **Step 1: Extend shared types**

`shared/types/prices.ts` 끝에 추가:
```ts
// === v0.5.0 신규 (ticker 검색 + watchlist) ===

/** localStorage 에 저장되는 watchlist entry. server 응답에 `key` 로 매핑. */
export type WatchlistEntry = {
  key: string;         // 사용자 display key (lowercase alphanumeric + hyphen, /^[a-z0-9][a-z0-9-]*$/, 최대 32자)
  source: SourceName;  // 9 keys 중 하나
  symbol: string;      // source 별 ticker (/^[A-Za-z0-9_]+$/, 최대 32자 — HL lowercase xyz_ prefix 포함)
  label: string;       // UI display ("Apple Inc.", "삼성전자") — server query 제외
  tier: 1 | 2 | 3;     // 어느 tier 에서 hit
};

/** `/api/search` 응답 entry. WatchlistEntry 와 거의 동일하지만 description 추가. */
export type SearchResult = {
  source: SourceName;
  symbol: string;
  label: string;
  description?: string;  // "NASDAQ", "KRX", "Binance Perp" 등
  tier: 1 | 2 | 3;
};

export type SearchResponse = {
  tier: 1 | 2 | 3 | null;
  results: SearchResult[];
  reason?: 'not_found' | 'empty_query' | 'invalid_chars' | 'too_short' | 'too_long' | 'naver_unavailable';
};
```

- [ ] **Step 2: Write failing useWatchlist test**

`src/hooks/useWatchlist.test.ts` (신규):
```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useWatchlist, WATCHLIST_KEY, MAX_ENTRIES } from './useWatchlist';
import type { WatchlistEntry } from '@shared/types/prices.js';

const sample: WatchlistEntry = {
  key: 'aapl', source: 'binance', symbol: 'AAPLUSDT', label: 'Apple Inc.', tier: 2,
};

beforeEach(() => localStorage.clear());
afterEach(() => localStorage.clear());

describe('useWatchlist', () => {
  it('initializes empty when localStorage clean', () => {
    const { result } = renderHook(() => useWatchlist());
    expect(result.current.entries).toEqual([]);
  });

  it('add entry persists to localStorage', () => {
    const { result } = renderHook(() => useWatchlist());
    act(() => { result.current.add(sample); });
    expect(result.current.entries).toHaveLength(1);
    expect(JSON.parse(localStorage.getItem(WATCHLIST_KEY)!)).toEqual([sample]);
  });

  it('rejects duplicate key', () => {
    const { result } = renderHook(() => useWatchlist());
    act(() => { result.current.add(sample); });
    let err: Error | null = null;
    act(() => { try { result.current.add(sample); } catch (e) { err = e as Error; } });
    expect(err?.message).toMatch(/이미/);
    expect(result.current.entries).toHaveLength(1);
  });

  it('rejects when MAX_ENTRIES reached', () => {
    const { result } = renderHook(() => useWatchlist());
    act(() => {
      for (let i = 0; i < MAX_ENTRIES; i++) {
        result.current.add({ ...sample, key: `t${i}`, symbol: `T${i}USDT` });
      }
    });
    let err: Error | null = null;
    act(() => { try { result.current.add({ ...sample, key: 'overflow' }); } catch (e) { err = e as Error; } });
    expect(err?.message).toMatch(/최대|50/);
    expect(result.current.entries).toHaveLength(MAX_ENTRIES);
  });

  it('remove deletes entry', () => {
    const { result } = renderHook(() => useWatchlist());
    act(() => { result.current.add(sample); });
    act(() => { result.current.remove(sample.key); });
    expect(result.current.entries).toEqual([]);
    expect(JSON.parse(localStorage.getItem(WATCHLIST_KEY) ?? '[]')).toEqual([]);
  });

  it('recovers from corrupt localStorage', () => {
    localStorage.setItem(WATCHLIST_KEY, '{ invalid json');
    const { result } = renderHook(() => useWatchlist());
    expect(result.current.entries).toEqual([]);
  });
});
```

- [ ] **Step 3: Run test — should FAIL (module not found)**

```bash
pnpm test src/hooks/useWatchlist.test.ts
```

- [ ] **Step 4: Implement useWatchlist hook**

`src/hooks/useWatchlist.ts` (신규):
```ts
import { useState, useCallback, useEffect } from 'react';
import { mutate } from 'swr';
import type { WatchlistEntry } from '@shared/types/prices.js';

export const WATCHLIST_KEY = 'kr-multi-market.watchlist.v1';
export const MAX_ENTRIES = 50;

const KEY_REGEX = /^[a-z0-9][a-z0-9-]*$/;
const SYMBOL_REGEX = /^[A-Za-z0-9_]+$/;

function loadFromStorage(): WatchlistEntry[] {
  try {
    const raw = localStorage.getItem(WATCHLIST_KEY);
    if (!raw) return [];
    const parsed = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    return parsed.filter((e): e is WatchlistEntry => {
      return e && typeof e.key === 'string'
        && typeof e.source === 'string'
        && typeof e.symbol === 'string'
        && typeof e.label === 'string'
        && [1, 2, 3].includes(e.tier);
    });
  } catch {
    return [];
  }
}

function saveToStorage(entries: WatchlistEntry[]): void {
  try {
    localStorage.setItem(WATCHLIST_KEY, JSON.stringify(entries));
  } catch (err) {
    console.warn('[useWatchlist] localStorage save failed:', err);
  }
}

export function useWatchlist() {
  const [entries, setEntries] = useState<WatchlistEntry[]>(() => loadFromStorage());

  const add = useCallback((entry: WatchlistEntry): void => {
    if (!KEY_REGEX.test(entry.key) || entry.key.length > 32) {
      throw new Error(`잘못된 key 형식: ${entry.key}`);
    }
    if (!SYMBOL_REGEX.test(entry.symbol) || entry.symbol.length > 32) {
      throw new Error(`잘못된 symbol 형식: ${entry.symbol}`);
    }
    setEntries(prev => {
      if (prev.length >= MAX_ENTRIES) {
        throw new Error(`최대 ${MAX_ENTRIES}개 까지만 추가 가능합니다`);
      }
      if (prev.some(e => e.key === entry.key)) {
        throw new Error(`이미 추가된 종목: ${entry.key}`);
      }
      const next = [...prev, entry];
      saveToStorage(next);
      return next;
    });
    mutate((key: unknown) => typeof key === 'string' && key.startsWith('/api/prices'));
  }, []);

  const remove = useCallback((key: string): void => {
    setEntries(prev => {
      const next = prev.filter(e => e.key !== key);
      saveToStorage(next);
      return next;
    });
    mutate((key: unknown) => typeof key === 'string' && key.startsWith('/api/prices'));
  }, []);

  return { entries, add, remove };
}
```

- [ ] **Step 5: Run test — PASS**

```bash
pnpm test src/hooks/useWatchlist.test.ts
```
Expected: 6/6 cases PASS.

- [ ] **Step 6: Verify regression**

```bash
pnpm exec tsc --noEmit && pnpm test
```
Expected: 전체 PASS (217 + 6 = 223).

- [ ] **Step 7: Commit**

```bash
git add shared/types/prices.ts src/hooks/useWatchlist.ts src/hooks/useWatchlist.test.ts
git commit -m "feat(types+hook): WatchlistEntry types + useWatchlist hook (Wave 1)

- shared/types: WatchlistEntry, SearchResult, SearchResponse 신규
- useWatchlist hook: localStorage R/W, add/remove, regex validation
- 50개 cap, 중복 key reject, corrupt JSON 복구
- SWR mutate trigger 시 즉시 카드 반영

Spec §4.3 / §5 / Codex review #3 통과 (regex case-insensitive symbol)"
```

---

## Task 2: Wave 2a — Server `searchFinnhub` 함수 확장

**Files:**
- Modify: `server/lib/sources/finnhub.ts` (searchFinnhub export 추가)
- Create: `server/tests/sources/finnhub-search.test.ts`

- [ ] **Step 1: Write failing test**

`server/tests/sources/finnhub-search.test.ts` (신규):
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { searchFinnhub } from '../../lib/sources/finnhub.js';

afterEach(() => vi.restoreAllMocks());

describe('searchFinnhub', () => {
  it('parses /search response and filters Common Stock + ETF', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({
        count: 3,
        result: [
          { symbol: 'AAPL', description: 'APPLE INC', type: 'Common Stock' },
          { symbol: 'AAPL.MX', description: 'APPLE INC (MX)', type: 'Common Stock' },
          { symbol: 'AAPL.OPT', description: 'AAPL Option', type: 'Option' },
        ],
      }), { status: 200 }) as unknown as Response,
    );
    const results = await searchFinnhub('Apple', 'test-token');
    expect(results).toHaveLength(2);  // Option filtered out
    expect(results[0]).toMatchObject({
      source: 'yahoo', symbol: 'AAPL', label: 'APPLE INC', tier: 1,
    });
  });

  it('returns [] when token empty', async () => {
    const results = await searchFinnhub('Apple', '');
    expect(results).toEqual([]);
  });

  it('returns [] on HTTP 429', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response('', { status: 429 }) as unknown as Response,
    );
    const results = await searchFinnhub('Apple', 'test-token');
    expect(results).toEqual([]);
  });

  it('limits to top 5 results', async () => {
    const items = Array.from({ length: 20 }, (_, i) => ({
      symbol: `T${i}`, description: `Ticker ${i}`, type: 'Common Stock',
    }));
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({ count: 20, result: items }), { status: 200 }) as unknown as Response,
    );
    const results = await searchFinnhub('T', 'test-token');
    expect(results).toHaveLength(5);
  });
});
```

- [ ] **Step 2: Run test — FAIL (searchFinnhub not exported)**

```bash
pnpm test server/tests/sources/finnhub-search.test.ts
```

- [ ] **Step 3: Add searchFinnhub to finnhub.ts**

`server/lib/sources/finnhub.ts` 끝에 추가:
```ts
import type { SearchResult } from '@shared/types/prices.js';

const FINNHUB_BASE = 'https://finnhub.io';
const SEARCH_TIMEOUT_MS = 5000;
const ALLOWED_TYPES = new Set(['Common Stock', 'ETF']);

type FinnhubSearchItem = {
  symbol?: string;
  description?: string;
  type?: string;
};

type FinnhubSearchResponse = {
  count?: number;
  result?: FinnhubSearchItem[];
};

/** Finnhub /search endpoint — 종목명/ticker 검색.
 *  Returns top 5 results filtered by type (Common Stock + ETF). 코인 자연 제외. */
export async function searchFinnhub(q: string, token: string): Promise<SearchResult[]> {
  if (!token || !q) return [];
  const url = `${FINNHUB_BASE}/api/v1/search?q=${encodeURIComponent(q)}&token=${encodeURIComponent(token)}`;
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), SEARCH_TIMEOUT_MS);
  try {
    const res = await fetch(url, { signal: controller.signal });
    if (!res.ok) {
      log.warn(`[finnhub-search] HTTP ${res.status}`);
      return [];
    }
    const raw = (await res.json()) as FinnhubSearchResponse;
    const items = raw.result ?? [];
    const results: SearchResult[] = [];
    for (const item of items) {
      if (!item.symbol || !item.description || !item.type) continue;
      if (!ALLOWED_TYPES.has(item.type)) continue;
      results.push({
        source: 'yahoo',  // Tier 1 미국 주식 source slot = yahoo (Finnhub fallback 패턴 일관, v0.4.2 Task 4)
        symbol: item.symbol,
        label: item.description,
        description: item.type,
        tier: 1,
      });
      if (results.length >= 5) break;
    }
    return results;
  } catch (err) {
    if (err instanceof Error && err.name === 'AbortError') {
      log.warn('[finnhub-search] timeout');
    } else {
      log.warn('[finnhub-search] error:', err instanceof Error ? err.message : err);
    }
    return [];
  } finally {
    clearTimeout(timeout);
  }
}
```

- [ ] **Step 4: Run test — PASS**

```bash
pnpm test server/tests/sources/finnhub-search.test.ts
```
Expected: 4/4 PASS.

- [ ] **Step 5: Commit**

```bash
git add server/lib/sources/finnhub.ts server/tests/sources/finnhub-search.test.ts
git commit -m "feat(server/finnhub): searchFinnhub /search endpoint (Wave 2a)

- /api/v1/search?q=... with token
- Type filter: Common Stock + ETF only (코인 자연 제외)
- Top 5 results
- source slot = 'yahoo' (Tier 1 미국 주식 패턴 일관)

Spec §3.2 / §4.2"
```

---

## Task 3: Wave 2b — Naver search source

**Files:**
- Create: `server/lib/sources/naver-search.ts`
- Create: `server/tests/sources/naver-search.test.ts`

**Naver API endpoint (사전 조사)**:
- `GET https://m.stock.naver.com/api/json/search/searchListJson.nhn?keyword=...`
- 응답 (예): `{ result: { d: [{ cd: '005930', nm: '삼성전자', ... }, ...] } }`
- 비공식 (undocumented) — 응답 shape 변경 가능성. test mock 으로 안정성 확보.

- [ ] **Step 1: Write failing test**

`server/tests/sources/naver-search.test.ts` (신규):
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { searchNaver } from '../../lib/sources/naver-search.js';

afterEach(() => vi.restoreAllMocks());

describe('searchNaver', () => {
  it('parses /searchListJson response', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({
        result: {
          d: [
            { cd: '005930', nm: '삼성전자', tpcd: 'KOSPI' },
            { cd: '028260', nm: '삼성물산', tpcd: 'KOSPI' },
          ],
        },
      }), { status: 200 }) as unknown as Response,
    );
    const results = await searchNaver('삼성');
    expect(results).toHaveLength(2);
    expect(results[0]).toMatchObject({
      source: 'naver', symbol: '005930', label: '삼성전자', description: 'KOSPI', tier: 1,
    });
  });

  it('returns [] on empty result', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({ result: { d: [] } }), { status: 200 }) as unknown as Response,
    );
    const results = await searchNaver('없는종목');
    expect(results).toEqual([]);
  });

  it('returns [] on HTTP error', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response('', { status: 500 }) as unknown as Response,
    );
    const results = await searchNaver('삼성');
    expect(results).toEqual([]);
  });

  it('limits to top 5 results', async () => {
    const items = Array.from({ length: 20 }, (_, i) => ({
      cd: `00593${i}`, nm: `삼성${i}`, tpcd: 'KOSPI',
    }));
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({ result: { d: items } }), { status: 200 }) as unknown as Response,
    );
    const results = await searchNaver('삼성');
    expect(results).toHaveLength(5);
  });
});
```

- [ ] **Step 2: Run test — FAIL (module not found)**

- [ ] **Step 3: Implement searchNaver**

`server/lib/sources/naver-search.ts` (신규):
```ts
import type { SearchResult } from '@shared/types/prices.js';
import { log } from '../logger.js';

const NAVER_BASE = 'https://m.stock.naver.com';
const SEARCH_TIMEOUT_MS = 5000;

type NaverSearchItem = {
  cd?: string;    // ticker code (6자리)
  nm?: string;    // 종목명
  tpcd?: string;  // 시장 (KOSPI, KOSDAQ, etc.)
};

type NaverSearchResponse = {
  result?: { d?: NaverSearchItem[] };
};

/** Naver 종목 검색 (한글). undocumented endpoint — 응답 shape 변경 시 fail 시 [] 반환. */
export async function searchNaver(q: string): Promise<SearchResult[]> {
  if (!q) return [];
  const url = `${NAVER_BASE}/api/json/search/searchListJson.nhn?keyword=${encodeURIComponent(q)}`;
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), SEARCH_TIMEOUT_MS);
  try {
    const res = await fetch(url, { signal: controller.signal });
    if (!res.ok) {
      log.warn(`[naver-search] HTTP ${res.status}`);
      return [];
    }
    const raw = (await res.json()) as NaverSearchResponse;
    const items = raw.result?.d ?? [];
    const results: SearchResult[] = [];
    for (const item of items) {
      if (!item.cd || !item.nm) continue;
      results.push({
        source: 'naver',
        symbol: item.cd,
        label: item.nm,
        description: item.tpcd ?? 'KRX',
        tier: 1,
      });
      if (results.length >= 5) break;
    }
    return results;
  } catch (err) {
    if (err instanceof Error && err.name === 'AbortError') {
      log.warn('[naver-search] timeout');
    } else {
      log.warn('[naver-search] error:', err instanceof Error ? err.message : err);
    }
    return [];
  } finally {
    clearTimeout(timeout);
  }
}
```

- [ ] **Step 4: Run test — PASS**

- [ ] **Step 5: Commit**

```bash
git add server/lib/sources/naver-search.ts server/tests/sources/naver-search.test.ts
git commit -m "feat(server/naver-search): 한글 종목 검색 endpoint (Wave 2b)

- Naver m.stock /searchListJson (undocumented)
- 응답 shape 검증 + fail 시 [] 반환
- Top 5 results
- description = KOSPI/KOSDAQ 등 시장 분류

Spec §3.2 / §4.2 / R2 (Naver 비공식 endpoint 안전성)"
```

---

## Task 4: Wave 3 — `/api/search` endpoint (3-tier resolver)

**Files:**
- Create: `server/routes/search.ts`
- Create: `server/tests/routes/search.test.ts`
- Modify: `server/index.ts` (route mount)

- [ ] **Step 1: Write failing test**

`server/tests/routes/search.test.ts` (신규):
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import request from 'supertest';
import { app } from '../../index.js';

// 외부 source 모듈 mock (vi.mock 으로 fetch wrapper 우회)
vi.mock('../../lib/sources/finnhub.js', async (importOriginal) => {
  const actual = await importOriginal<typeof import('../../lib/sources/finnhub.js')>();
  return {
    ...actual,
    searchFinnhub: vi.fn(),
  };
});
vi.mock('../../lib/sources/naver-search.js', () => ({ searchNaver: vi.fn() }));

import { searchFinnhub } from '../../lib/sources/finnhub.js';
import { searchNaver } from '../../lib/sources/naver-search.js';
const mockFinnhub = searchFinnhub as ReturnType<typeof vi.fn>;
const mockNaver = searchNaver as ReturnType<typeof vi.fn>;

beforeEach(() => { mockFinnhub.mockReset(); mockNaver.mockReset(); });

describe('GET /api/search', () => {
  it('400 on empty q', async () => {
    const res = await request(app).get('/api/search?q=');
    expect(res.status).toBe(400);
    expect(res.body.reason).toBe('empty_query');
  });

  it('400 on q < 2 chars', async () => {
    const res = await request(app).get('/api/search?q=a');
    expect(res.status).toBe(400);
    expect(res.body.reason).toBe('too_short');
  });

  it('400 on q > 32 chars', async () => {
    const res = await request(app).get(`/api/search?q=${'a'.repeat(33)}`);
    expect(res.status).toBe(400);
    expect(res.body.reason).toBe('too_long');
  });

  it('400 on invalid chars (semicolon, angle bracket)', async () => {
    const res = await request(app).get('/api/search?q=' + encodeURIComponent('a;b'));
    expect(res.status).toBe(400);
    expect(res.body.reason).toBe('invalid_chars');
  });

  it('Tier 1: routes Hangul to Naver', async () => {
    mockNaver.mockResolvedValue([
      { source: 'naver', symbol: '005930', label: '삼성전자', description: 'KOSPI', tier: 1 },
    ]);
    const res = await request(app).get('/api/search?q=' + encodeURIComponent('삼성'));
    expect(res.status).toBe(200);
    expect(res.body.tier).toBe(1);
    expect(res.body.results).toHaveLength(1);
    expect(mockFinnhub).not.toHaveBeenCalled();
  });

  it('Tier 1: routes English to Finnhub', async () => {
    mockFinnhub.mockResolvedValue([
      { source: 'yahoo', symbol: 'AAPL', label: 'Apple Inc.', description: 'Common Stock', tier: 1 },
    ]);
    const res = await request(app).get('/api/search?q=Apple');
    expect(res.status).toBe(200);
    expect(res.body.tier).toBe(1);
    expect(res.body.results[0].symbol).toBe('AAPL');
    expect(mockNaver).not.toHaveBeenCalled();
  });

  it('Tier 1 miss → Tier 3 hit (HL universe)', async () => {
    mockFinnhub.mockResolvedValue([]);
    // Tier 2 도 miss (CEX probe), Tier 3 hit
    // HL universe lookup 은 getLatestMids() — test 환경에선 빈 map. 별도 mock 필요시 추가
    const res = await request(app).get('/api/search?q=NOEXIST');
    expect(res.status).toBe(200);
    expect(res.body.tier).toBeNull();  // 또는 Tier 3 if mock
    expect(res.body.results).toEqual([]);
    expect(res.body.reason).toBe('not_found');
  });

  it('Naver 한글 검색 실패 시 naver_unavailable reason', async () => {
    mockNaver.mockResolvedValue([]);
    const res = await request(app).get('/api/search?q=' + encodeURIComponent('없는한글'));
    expect(res.status).toBe(200);
    expect(res.body.tier).toBeNull();
    expect(res.body.reason).toBe('naver_unavailable');
  });
});
```

⚠️ Tier 2 CEX probe 는 별도 mock 필요. 위 test 는 Tier 1 분기 중심. Tier 2 probe 는 Wave 3 후속 step 에서 추가.

- [ ] **Step 2: Run test — FAIL (route not mounted)**

- [ ] **Step 3: Implement /api/search route**

`server/routes/search.ts` (신규):
```ts
import { type Request, type Response } from 'express';
import { searchFinnhub } from '../lib/sources/finnhub.js';
import { searchNaver } from '../lib/sources/naver-search.js';
import { fetchBinanceFutures } from '../lib/sources/binance.js';
import { fetchBybitLinear } from '../lib/sources/bybit.js';
import { fetchBitgetFutures } from '../lib/sources/bitget.js';
import { getLatestMids } from '../lib/sources/hyperliquid-ws.js';
import type { SearchResponse, SearchResult } from '@shared/types/prices.js';
import { log } from '../lib/logger.js';

const Q_REGEX = /^[a-zA-Z0-9가-힣\s.-]+$/;
const HANGUL_REGEX = /[가-힣]/;

export async function searchHandler(req: Request, res: Response): Promise<void> {
  const q = String(req.query.q ?? '').trim();
  if (q.length === 0) {
    res.status(400).json({ tier: null, results: [], reason: 'empty_query' } as SearchResponse);
    return;
  }
  if (q.length < 2) {
    res.status(400).json({ tier: null, results: [], reason: 'too_short' } as SearchResponse);
    return;
  }
  if (q.length > 32) {
    res.status(400).json({ tier: null, results: [], reason: 'too_long' } as SearchResponse);
    return;
  }
  if (!Q_REGEX.test(q)) {
    res.status(400).json({ tier: null, results: [], reason: 'invalid_chars' } as SearchResponse);
    return;
  }

  const isHangul = HANGUL_REGEX.test(q);

  // Tier 1: KRX (한글) 또는 NASDAQ (영문)
  const tier1Results = isHangul
    ? await searchNaver(q)
    : await searchFinnhub(q, process.env.FINNHUB_TOKEN ?? '');

  if (tier1Results.length > 0) {
    res.json({ tier: 1, results: tier1Results } as SearchResponse);
    return;
  }

  // 한글 입력 + Naver 결과 0개 → Finnhub fallback X (한글 미지원). 사용자에게 영문 권고.
  if (isHangul) {
    res.json({ tier: null, results: [], reason: 'naver_unavailable' } as SearchResponse);
    return;
  }

  // Tier 2: CEX probe (Binance + Bybit + Bitget 병렬, singleFlight 우회)
  const TICKER = q.toUpperCase();
  const cexSymbol = `${TICKER}USDT`;
  const [binResult, bybitResult, bitgetResult] = await Promise.all([
    fetchBinanceFutures([cexSymbol]).catch(() => null),
    fetchBybitLinear([cexSymbol]).catch(() => null),
    fetchBitgetFutures([cexSymbol]).catch(() => null),
  ]);

  const tier2Hits: SearchResult[] = [];
  if (binResult?.ok && binResult.data.length > 0) {
    tier2Hits.push({ source: 'binance', symbol: cexSymbol, label: cexSymbol, description: 'Binance Perp', tier: 2 });
  }
  if (bybitResult?.ok && bybitResult.data.length > 0) {
    tier2Hits.push({ source: 'bybit', symbol: cexSymbol, label: cexSymbol, description: 'Bybit Perp', tier: 2 });
  }
  if (bitgetResult?.ok && bitgetResult.data.length > 0) {
    tier2Hits.push({ source: 'bitget', symbol: cexSymbol, label: cexSymbol, description: 'Bitget Perp', tier: 2 });
  }
  if (tier2Hits.length > 0) {
    // First-match-wins: binance > bybit > bitget 우선순위 (배열 순서)
    res.json({ tier: 2, results: [tier2Hits[0]] } as SearchResponse);
    return;
  }

  // Tier 3: HL xyz allMids lookup
  const mids = getLatestMids();
  const hlKey = `xyz_${TICKER}`;
  if (mids.mids.has(hlKey)) {
    res.json({
      tier: 3,
      results: [{ source: 'hyperliquid', symbol: hlKey, label: TICKER, description: 'Hyperliquid xyz', tier: 3 }],
    } as SearchResponse);
    return;
  }

  res.json({ tier: null, results: [], reason: 'not_found' } as SearchResponse);
}
```

- [ ] **Step 4: Mount route in server/index.ts**

`server/index.ts` 의 기존 route mount 부근에 추가:
```ts
import { searchHandler } from './routes/search.js';
// ...
app.get('/api/search', searchHandler);
```

⚠️ `app.get('/api/prices', pricesHandler)` 위 또는 옆에 mount.

- [ ] **Step 5: Run test — PASS (Tier 1 분기)**

```bash
pnpm test server/tests/routes/search.test.ts
```

- [ ] **Step 6: Commit**

```bash
git add server/routes/search.ts server/tests/routes/search.test.ts server/index.ts
git commit -m "feat(server/search): /api/search 3-tier resolver (Wave 3)

- Tier 1: Hangul detect → searchNaver / 영문 → searchFinnhub
- Tier 2: Binance + Bybit + Bitget 병렬 probe (singleFlight 우회), first-match-wins
- Tier 3: HL xyz allMids xyz_{TICKER} lookup
- Input validation: empty/too_short/too_long/invalid_chars/naver_unavailable/not_found
- Mount in server/index.ts

Spec §3.2 / §3.4 / §6 / Codex review #1-3 통과"
```

---

## Task 5: Wave 4 — `/api/prices?watchlist=...` 확장 + dynamic mapping

**Files:**
- Modify: `server/index.ts` (parseWatchlist + 동적 source-별 symbols 합집합)
- Modify: `server/lib/assemble.ts` (dynamicMapping 인자 추가)
- Create: `server/tests/prices-watchlist-parsing.test.ts`
- Create: `server/tests/assemble-dynamic.test.ts`

- [ ] **Step 1: Write parseWatchlist test**

`server/tests/prices-watchlist-parsing.test.ts` (신규):
```ts
import { describe, it, expect } from 'vitest';
import { parseWatchlist } from '../index.js';  // export 추가 필요

describe('parseWatchlist', () => {
  it('parses single entry', () => {
    const result = parseWatchlist('aapl:finnhub:AAPL');
    expect(result).toEqual([{ key: 'aapl', source: 'yahoo', symbol: 'AAPL' }]);
    // 주의: finnhub 는 search source 만, source slot = 'yahoo' (Tier 1)
  });

  it('parses multiple entries', () => {
    const result = parseWatchlist('aapl:yahoo:AAPL,kakao:naver:035720');
    expect(result).toEqual([
      { key: 'aapl', source: 'yahoo', symbol: 'AAPL' },
      { key: 'kakao', source: 'naver', symbol: '035720' },
    ]);
  });

  it('rejects invalid key (uppercase, special chars)', () => {
    const result = parseWatchlist('AAPL:yahoo:AAPL,bad!key:yahoo:T');
    expect(result).toEqual([]);  // 모두 reject
  });

  it('rejects invalid source (unknown)', () => {
    const result = parseWatchlist('aapl:invalid:AAPL');
    expect(result).toEqual([]);
  });

  it('rejects invalid symbol (special chars)', () => {
    const result = parseWatchlist('aapl:yahoo:AA-PL');
    expect(result).toEqual([]);
  });

  it('returns empty on empty query', () => {
    expect(parseWatchlist('')).toEqual([]);
    expect(parseWatchlist(undefined as unknown as string)).toEqual([]);
  });
});
```

⚠️ Source mapping note: Tier 1 영문 검색 결과 `source: 'yahoo'` (Finnhub fallback 패턴 일관). 즉 client 가 저장한 entry.source 는 9 SourceName 중 하나. parseWatchlist 가 9 keys 만 허용.

- [ ] **Step 2: Add parseWatchlist + use in pricesHandler**

`server/index.ts` 수정:

```ts
import type { WatchlistEntry, SourceName } from '@shared/types/prices.js';

const KEY_REGEX = /^[a-z0-9][a-z0-9-]*$/;
const SYMBOL_REGEX = /^[A-Za-z0-9_]+$/;
const VALID_SOURCES = new Set<SourceName>(['hyperliquid', 'naver', 'yahoo', 'binance', 'upbit', 'bybit', 'bitget', 'polygon', 'twelvedata']);

export function parseWatchlist(query: string | undefined): Array<{ key: string; source: SourceName; symbol: string }> {
  if (!query) return [];
  const entries: Array<{ key: string; source: SourceName; symbol: string }> = [];
  for (const part of query.split(',')) {
    const [key, source, symbol] = part.split(':');
    if (!key || !source || !symbol) continue;
    if (key.length > 32 || !KEY_REGEX.test(key)) continue;
    if (symbol.length > 32 || !SYMBOL_REGEX.test(symbol)) continue;
    if (!VALID_SOURCES.has(source as SourceName)) continue;
    entries.push({ key, source: source as SourceName, symbol });
  }
  return entries;
}
```

`pricesHandler` 안에서 watchlist 사용:

```ts
const watchlist = parseWatchlist(typeof _req.query.watchlist === 'string' ? _req.query.watchlist : undefined);

// 각 source 의 watchlist 추가 symbols 추출
const wlBySource: Record<SourceName, string[]> = {
  hyperliquid: [], naver: [], yahoo: [], binance: [], upbit: [],
  bybit: [], bitget: [], polygon: [], twelvedata: [],
};
for (const entry of watchlist) {
  wlBySource[entry.source].push(entry.symbol);
}

// Promise.all 의 fetchYahoo 등 호출에 wlBySource[source] 추가:
// () => fetchYahoo(['KRW=X', 'EWY', 'QQQ', 'ES=F', '^GSPC', ...wlBySource.yahoo])
// () => fetchBinanceFutures([...BINANCE_SYMBOLS, ...wlBySource.binance])
// ... 동일 패턴 각 source.
```

또 `assemblePricesResponse` 호출 시 watchlist 인자 추가:
```ts
const response = assemblePricesResponse({ hl, naver, yahoo, upbit, binance, bybit, bitget, polygon, twelvedata }, watchlist);
```

- [ ] **Step 3: Update assemble.ts (dynamic mapping)**

`server/lib/assemble.ts` 의 `assemblePricesResponse` 시그니처 + 동적 mapping 추가:

```ts
export function assemblePricesResponse(
  sources: SourceInputs,
  watchlist: Array<{ key: string; source: SourceName; symbol: string }> = [],
): PricesResponse {
  // ... 기존 정적 mapping 로직 ...

  // === v0.5.0 동적 mapping: watchlist entry 별로 tickers[key] 추가 ===
  for (const entry of watchlist) {
    const sourceData = sources[entry.source === 'hyperliquid' ? 'hl' : entry.source];
    if (!sourceData?.ok) continue;
    const pp = sourceData.data.find(p => p.symbol === entry.symbol);
    if (!pp) continue;
    // 정적 ticker key 와 충돌 시 skip (client 가 add 시 거부했어야 함, 방어적)
    if (tickers[entry.key]) continue;
    tickers[entry.key] = { [entry.source === 'hyperliquid' ? 'hl' : entry.source]: pp };
  }

  // ... 기존 spread/premium/sourceHealth 로직 ...
}
```

⚠️ `SourceInputs.hl` 키와 `SourceName 'hyperliquid'` 불일치 — assemble 안에서 변환. 또는 SourceInputs 키도 `hyperliquid` 으로 통일 (회귀 위험). 일단 위 분기로 처리.

- [ ] **Step 4: Write assemble-dynamic test**

`server/tests/assemble-dynamic.test.ts` (신규):
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { assemblePricesResponse, type SourceInputs } from '../lib/assemble.js';
import { _resetSourceHealth } from '../lib/health.js';
import type { PricePoint } from '@shared/types/prices.js';

const now = Date.now();
const mkPP = (source: PricePoint['source'], symbol: string, price: number, unit: PricePoint['unit'] = 'USDT'): PricePoint => ({
  source, symbol, price, unit, status: 'ok', asOf: now, receivedAt: now, schemaVersion: 1,
});

const minimal = (): SourceInputs => ({
  hl: { ok: false, error: 'n/a', latencyMs: 0 },
  naver: { ok: false, error: 'n/a', latencyMs: 0 },
  yahoo: { ok: true, data: [mkPP('yahoo', 'KRW=X', 1471.7, 'KRW')], latencyMs: 1 },
  upbit: { ok: true, data: [mkPP('upbit', 'KRW-USDT', 1470, 'KRW')], latencyMs: 1 },
  binance: { ok: false, error: 'n/a', latencyMs: 0 },
  bybit: { ok: false, error: 'disabled', latencyMs: 0 },
  bitget: { ok: false, error: 'disabled', latencyMs: 0 },
  polygon: { ok: false, error: 'disabled', latencyMs: 0 },
  twelvedata: { ok: false, error: 'disabled', latencyMs: 0 },
});

describe('assemble — dynamic watchlist mapping', () => {
  beforeEach(() => _resetSourceHealth());

  it('adds watchlist ticker to response tickers[key]', () => {
    const inputs = minimal();
    inputs.binance = { ok: true, data: [mkPP('binance', 'AAPLUSDT', 195.50)], latencyMs: 1 };
    const resp = assemblePricesResponse(inputs, [
      { key: 'aapl', source: 'binance', symbol: 'AAPLUSDT' },
    ]);
    expect(resp.tickers.aapl).toBeDefined();
    expect(resp.tickers.aapl.binance?.price).toBe(195.50);
  });

  it('skips entry when source data missing', () => {
    const resp = assemblePricesResponse(minimal(), [
      { key: 'aapl', source: 'binance', symbol: 'AAPLUSDT' },
    ]);
    expect(resp.tickers.aapl).toBeUndefined();
  });

  it('skips entry when key collides with static ticker', () => {
    const inputs = minimal();
    inputs.binance = { ok: true, data: [mkPP('binance', 'EWYUSDT', 60)], latencyMs: 1 };
    const resp = assemblePricesResponse(inputs, [
      { key: 'ewy', source: 'binance', symbol: 'EWYUSDT' },  // 정적 'ewy' 와 충돌
    ]);
    // 정적 ewy 의 binance entry 가 그대로, watchlist 가 덮어쓰지 않음
    expect(resp.tickers.ewy?.binance).toBeDefined();
  });

  it('no watchlist arg = v0.4.2 behavior unchanged', () => {
    const resp = assemblePricesResponse(minimal());
    // 정적 ticker 만 응답
    expect(resp.tickers.aapl).toBeUndefined();
  });

  it('multiple watchlist entries via different sources', () => {
    const inputs = minimal();
    inputs.binance = { ok: true, data: [mkPP('binance', 'AAPLUSDT', 195)], latencyMs: 1 };
    inputs.naver = { ok: true, data: [mkPP('naver', '035720', 50000, 'KRW')], latencyMs: 1 };
    const resp = assemblePricesResponse(inputs, [
      { key: 'aapl', source: 'binance', symbol: 'AAPLUSDT' },
      { key: 'kakao', source: 'naver', symbol: '035720' },
    ]);
    expect(resp.tickers.aapl.binance?.price).toBe(195);
    expect(resp.tickers.kakao.naver?.price).toBe(50000);
  });
});
```

- [ ] **Step 5: Run all new tests — PASS**

```bash
pnpm test server/tests/prices-watchlist-parsing.test.ts server/tests/assemble-dynamic.test.ts
```

- [ ] **Step 6: Full regression**

```bash
pnpm exec tsc --noEmit && pnpm test
```
Expected: 전부 PASS (223 + 14 = 237).

- [ ] **Step 7: Commit**

```bash
git add server/index.ts server/lib/assemble.ts \
        server/tests/prices-watchlist-parsing.test.ts server/tests/assemble-dynamic.test.ts
git commit -m "feat(server): /api/prices?watchlist=... 동적 mapping (Wave 4)

- parseWatchlist(query): KEY:SOURCE:SYMBOL,... → entry list, regex validation
- pricesHandler: watchlist 추출 + source-별 symbols 합집합으로 기존 fetcher 호출
- assemble.ts: 동적 watchlist 인자 추가, tickers[entry.key] 매핑
- 정적 ticker 충돌 시 skip (방어)
- v0.4.2 backward compat: watchlist 없으면 정적 7 ticker 그대로

Spec §3.3 / §4.2 / Codex BLOCK 1 fix (encoding)"
```

---

## Task 6: Wave 5a — Frontend `ticker-resolver` + SearchBar/SearchDropdown

**Files:**
- Create: `src/lib/ticker-resolver.ts`
- Create: `src/lib/ticker-resolver.test.ts`
- Create: `src/components/SearchBar.tsx`
- Create: `src/components/SearchBar.test.tsx`
- Create: `src/components/SearchDropdown.tsx`
- Create: `src/components/SearchDropdown.test.tsx`

- [ ] **Step 1: Write resolver test**

`src/lib/ticker-resolver.test.ts` (신규):
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { searchTicker } from './ticker-resolver';

afterEach(() => vi.restoreAllMocks());

describe('searchTicker', () => {
  it('returns response from /api/search', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({
        tier: 1,
        results: [{ source: 'yahoo', symbol: 'AAPL', label: 'Apple Inc.', description: 'Common Stock', tier: 1 }],
      }), { status: 200 }) as unknown as Response,
    );
    const res = await searchTicker('Apple');
    expect(res.tier).toBe(1);
    expect(res.results).toHaveLength(1);
  });

  it('returns empty results on 400', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({ tier: null, results: [], reason: 'too_short' }), { status: 400 }) as unknown as Response,
    );
    const res = await searchTicker('a');
    expect(res.results).toEqual([]);
    expect(res.reason).toBe('too_short');
  });

  it('returns empty results on network error', async () => {
    vi.spyOn(global, 'fetch').mockRejectedValue(new Error('network'));
    const res = await searchTicker('Apple');
    expect(res.results).toEqual([]);
  });
});
```

- [ ] **Step 2: Implement resolver**

`src/lib/ticker-resolver.ts` (신규):
```ts
import type { SearchResponse } from '@shared/types/prices.js';

/** `/api/search?q=...` HTTP wrapper. server 가 3-tier resolution 담당. */
export async function searchTicker(q: string): Promise<SearchResponse> {
  try {
    const res = await fetch(`/api/search?q=${encodeURIComponent(q)}`);
    if (!res.ok && res.status !== 400) {
      return { tier: null, results: [], reason: 'not_found' };
    }
    return (await res.json()) as SearchResponse;
  } catch {
    return { tier: null, results: [], reason: 'not_found' };
  }
}
```

- [ ] **Step 3: Run resolver test — PASS**

- [ ] **Step 4: Implement SearchDropdown**

`src/components/SearchDropdown.tsx` (신규):
```tsx
import type { SearchResult, SearchResponse } from '@shared/types/prices.js';

type Props = {
  response: SearchResponse | null;
  onPick: (result: SearchResult) => void;
  onClose: () => void;
};

const REASON_MESSAGES: Record<NonNullable<SearchResponse['reason']>, string> = {
  empty_query: '검색어를 입력하세요',
  too_short: '검색어는 2자 이상이어야 합니다',
  too_long: '검색어는 32자 이하여야 합니다',
  invalid_chars: '허용되지 않는 문자가 포함됨',
  naver_unavailable: '한국 주식 검색 일시 장애. 영문 ticker (예: 005930) 로 직접 입력하세요',
  not_found: '검색 결과 없음 (Tier 1/2/3 모두 miss)',
};

export function SearchDropdown({ response, onPick, onClose }: Props) {
  if (!response) return null;
  return (
    <div className="search-dropdown" role="listbox">
      {response.results.length === 0 && (
        <div className="search-dropdown-empty">
          {response.reason ? REASON_MESSAGES[response.reason] : '검색 결과 없음'}
        </div>
      )}
      {response.results.map((r) => (
        <button
          key={`${r.source}:${r.symbol}`}
          type="button"
          className="search-dropdown-item"
          role="option"
          onClick={() => { onPick(r); onClose(); }}
        >
          <span className="search-item-label">{r.label}</span>
          <span className="search-item-meta">
            {r.symbol} · {r.description ?? r.source} · Tier {r.tier}
          </span>
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 5: Write SearchDropdown test**

`src/components/SearchDropdown.test.tsx` (신규):
```tsx
import { describe, it, expect, vi, afterEach } from 'vitest';
import { render, screen, cleanup, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import { SearchDropdown } from './SearchDropdown';

afterEach(() => cleanup());

describe('SearchDropdown', () => {
  it('renders top 5 results', () => {
    const results = Array.from({ length: 5 }, (_, i) => ({
      source: 'yahoo' as const, symbol: `T${i}`, label: `Ticker ${i}`, tier: 1 as const,
    }));
    render(<SearchDropdown response={{ tier: 1, results }} onPick={vi.fn()} onClose={vi.fn()} />);
    expect(screen.getAllByRole('option')).toHaveLength(5);
  });

  it('shows reason message when results empty', () => {
    render(<SearchDropdown response={{ tier: null, results: [], reason: 'not_found' }} onPick={vi.fn()} onClose={vi.fn()} />);
    expect(screen.getByText(/검색 결과 없음/)).toBeInTheDocument();
  });

  it('click triggers onPick + onClose', () => {
    const onPick = vi.fn();
    const onClose = vi.fn();
    render(<SearchDropdown
      response={{ tier: 1, results: [{ source: 'yahoo', symbol: 'AAPL', label: 'Apple', tier: 1 }] }}
      onPick={onPick} onClose={onClose} />);
    fireEvent.click(screen.getByRole('option'));
    expect(onPick).toHaveBeenCalledWith(expect.objectContaining({ symbol: 'AAPL' }));
    expect(onClose).toHaveBeenCalled();
  });
});
```

- [ ] **Step 6: Implement SearchBar**

`src/components/SearchBar.tsx` (신규):
```tsx
import { useState, useCallback } from 'react';
import type { SearchResponse, SearchResult, WatchlistEntry } from '@shared/types/prices.js';
import { searchTicker } from '../lib/ticker-resolver';
import { SearchDropdown } from './SearchDropdown';

type Props = {
  onAdd: (entry: WatchlistEntry) => void;
};

function buildKey(label: string, existingKeys: string[]): string {
  // label → lowercase alphanumeric + hyphen. 충돌 시 -1, -2 suffix.
  const base = label.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').slice(0, 32) || 'item';
  if (!existingKeys.includes(base)) return base;
  let i = 1;
  while (existingKeys.includes(`${base}-${i}`)) i++;
  return `${base}-${i}`.slice(0, 32);
}

export function SearchBar({ onAdd }: Props) {
  const [query, setQuery] = useState('');
  const [response, setResponse] = useState<SearchResponse | null>(null);
  const [loading, setLoading] = useState(false);

  const handleSubmit = useCallback(async (e: React.FormEvent) => {
    e.preventDefault();
    const q = query.trim();
    if (q.length < 2) return;
    setLoading(true);
    const res = await searchTicker(q);
    setResponse(res);
    setLoading(false);
  }, [query]);

  const handlePick = useCallback((result: SearchResult) => {
    // buildKey 는 label → base key 생성. 충돌 해소는 useWatchlist.add 가 책임 (auto-suffix).
    const key = buildKey(result.label, []);
    try {
      onAdd({
        key,
        source: result.source,
        symbol: result.symbol,
        label: result.label,
        tier: result.tier,
      });
      setQuery('');
      setResponse(null);
    } catch (err) {
      alert((err as Error).message);
    }
  }, [onAdd]);

  return (
    <div className="search-bar-wrap">
      <form onSubmit={handleSubmit} className="search-bar-form">
        <input
          type="text"
          className="search-bar-input"
          placeholder="🔍 종목 검색 (예: Apple, 삼성전자, 005930)..."
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          aria-label="종목 검색"
          maxLength={32}
        />
        <button type="submit" disabled={query.trim().length < 2 || loading}>
          {loading ? '검색 중...' : '추가'}
        </button>
      </form>
      <SearchDropdown response={response} onPick={handlePick} onClose={() => setResponse(null)} />
    </div>
  );
}
```

⚠️ `buildKey` 의 `existingKeys` 는 SearchBar 가 useWatchlist 와 직접 통신해야 함 — App.tsx 에서 prop drilling 또는 useWatchlist 안에서 buildKey 책임 이동. v3 spec §5 의 "key 충돌 처리" 위치.

**결정**: `useWatchlist.add()` 가 key 충돌 시 자동 suffix 추가 — useWatchlist.ts 의 add() 수정 필요. SearchBar 는 단순히 label 기반 key 생성, useWatchlist 가 충돌 해소.

⚠️ Task 1 의 useWatchlist 도 같이 업데이트 (Wave 1 closeout 후 별도 fix commit 또는 Task 6 안 통합).

- [ ] **Step 7: Update useWatchlist.add to handle key collision**

`src/hooks/useWatchlist.ts` 의 add 함수 수정:
```ts
const add = useCallback((entry: WatchlistEntry): void => {
  // ... 기존 validation ...
  setEntries(prev => {
    if (prev.length >= MAX_ENTRIES) throw new Error(`최대 ${MAX_ENTRIES}개 까지만 추가 가능합니다`);
    // 충돌 시 자동 suffix
    let key = entry.key;
    if (prev.some(e => e.key === key)) {
      let i = 1;
      while (prev.some(e => e.key === `${entry.key}-${i}`)) i++;
      key = `${entry.key}-${i}`.slice(0, 32);
    }
    const finalEntry = { ...entry, key };
    const next = [...prev, finalEntry];
    saveToStorage(next);
    return next;
  });
  // ...
}, []);
```

⚠️ 이 변경 시 Task 1 의 useWatchlist test (`rejects duplicate key`) 가 fail — test 도 같이 수정.

- [ ] **Step 8: Update useWatchlist test for auto-suffix**

`src/hooks/useWatchlist.test.ts` 의 duplicate test 교체:
```ts
it('auto-suffixes duplicate key', () => {
  const { result } = renderHook(() => useWatchlist());
  act(() => { result.current.add(sample); });
  act(() => { result.current.add({ ...sample, symbol: 'AAPLBYBIT' }); });  // 같은 key
  expect(result.current.entries).toHaveLength(2);
  expect(result.current.entries[0].key).toBe('aapl');
  expect(result.current.entries[1].key).toBe('aapl-1');
});
```

- [ ] **Step 9: Write SearchBar test**

`src/components/SearchBar.test.tsx` (신규):
```tsx
import { describe, it, expect, vi, afterEach } from 'vitest';
import { render, screen, cleanup, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import { SearchBar } from './SearchBar';

afterEach(() => { cleanup(); vi.restoreAllMocks(); });

describe('SearchBar', () => {
  it('renders input + button', () => {
    render(<SearchBar onAdd={vi.fn()} />);
    expect(screen.getByLabelText('종목 검색')).toBeInTheDocument();
    expect(screen.getByText('추가')).toBeInTheDocument();
  });

  it('disables button when query < 2 chars', () => {
    render(<SearchBar onAdd={vi.fn()} />);
    expect(screen.getByText('추가')).toBeDisabled();
    fireEvent.change(screen.getByLabelText('종목 검색'), { target: { value: 'a' } });
    expect(screen.getByText('추가')).toBeDisabled();
    fireEvent.change(screen.getByLabelText('종목 검색'), { target: { value: 'ap' } });
    expect(screen.getByText('추가')).not.toBeDisabled();
  });

  it('submits search + shows dropdown', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValue(
      new Response(JSON.stringify({
        tier: 1,
        results: [{ source: 'yahoo', symbol: 'AAPL', label: 'Apple Inc.', tier: 1 }],
      }), { status: 200 }) as unknown as Response,
    );
    render(<SearchBar onAdd={vi.fn()} />);
    fireEvent.change(screen.getByLabelText('종목 검색'), { target: { value: 'Apple' } });
    fireEvent.click(screen.getByText('추가'));
    await screen.findByRole('option');
    expect(screen.getByText('Apple Inc.')).toBeInTheDocument();
  });
});
```

- [ ] **Step 10: Run all new tests — PASS**

```bash
pnpm test src/lib/ticker-resolver.test.ts src/components/SearchBar.test.tsx src/components/SearchDropdown.test.tsx src/hooks/useWatchlist.test.ts
```

- [ ] **Step 11: Commit**

```bash
git add src/lib/ticker-resolver.ts src/lib/ticker-resolver.test.ts \
        src/components/SearchBar.tsx src/components/SearchBar.test.tsx \
        src/components/SearchDropdown.tsx src/components/SearchDropdown.test.tsx \
        src/hooks/useWatchlist.ts src/hooks/useWatchlist.test.ts
git commit -m "feat(client): SearchBar + SearchDropdown + ticker-resolver (Wave 5a)

- ticker-resolver: /api/search HTTP wrapper, error graceful
- SearchBar: input + Enter 제출 + dropdown trigger
- SearchDropdown: top 5 results + reason messages (empty/too_short/etc)
- useWatchlist auto-suffix duplicate key (-1, -2)

Spec §3.2 / §4.1 / §6"
```

---

## Task 7: Wave 6 — WatchlistCard + WatchlistSection + App integration

**Files:**
- Create: `src/components/WatchlistCard.tsx`
- Create: `src/components/WatchlistCard.test.tsx`
- Create: `src/components/WatchlistSection.tsx`
- Create: `src/components/WatchlistSection.test.tsx`
- Modify: `src/App.tsx` (SearchBar + WatchlistSection 통합)
- Modify: `src/styles/*` (검색바 + section CSS)

- [ ] **Step 1: Implement WatchlistCard**

`src/components/WatchlistCard.tsx` (신규):
```tsx
import type { WatchlistEntry, TickerPayload, FxRates } from '@shared/types/prices.js';
import { IndexCompactCard } from './IndexCompactCard';

type Props = {
  entry: WatchlistEntry;
  payload?: TickerPayload;
  fx?: FxRates;
  onRemove: (key: string) => void;
};

export function WatchlistCard({ entry, payload, fx, onRemove }: Props) {
  return (
    <div className="watchlist-card-wrap">
      <button
        type="button"
        className="watchlist-card-remove"
        aria-label={`${entry.label} 삭제`}
        title="삭제"
        onClick={() => onRemove(entry.key)}
      >
        ✕
      </button>
      <IndexCompactCard
        ticker={entry.key}
        label={entry.label}
        payload={payload}
        fx={fx}
      />
    </div>
  );
}
```

- [ ] **Step 2: WatchlistCard test**

`src/components/WatchlistCard.test.tsx` (신규):
```tsx
import { describe, it, expect, vi, afterEach } from 'vitest';
import { render, screen, cleanup, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import { WatchlistCard } from './WatchlistCard';
import type { WatchlistEntry, FxRates } from '@shared/types/prices.js';

afterEach(() => cleanup());

const entry: WatchlistEntry = { key: 'aapl', source: 'binance', symbol: 'AAPLUSDT', label: 'Apple Inc.', tier: 2 };
const fx: FxRates = { officialUsdKrw: 1471.7, usdtKrw: 1470, divergencePct: 0 };

describe('WatchlistCard', () => {
  it('renders X button + IndexCompactCard', () => {
    render(<WatchlistCard entry={entry} payload={undefined} fx={fx} onRemove={vi.fn()} />);
    expect(screen.getByLabelText('Apple Inc. 삭제')).toBeInTheDocument();
    expect(screen.getByText('Apple Inc.')).toBeInTheDocument();
  });

  it('X click triggers onRemove with key', () => {
    const onRemove = vi.fn();
    render(<WatchlistCard entry={entry} payload={undefined} fx={fx} onRemove={onRemove} />);
    fireEvent.click(screen.getByLabelText('Apple Inc. 삭제'));
    expect(onRemove).toHaveBeenCalledWith('aapl');
  });
});
```

- [ ] **Step 3: Implement WatchlistSection**

`src/components/WatchlistSection.tsx` (신규):
```tsx
import type { WatchlistEntry, PricesResponse, FxRates } from '@shared/types/prices.js';
import { WatchlistCard } from './WatchlistCard';

type Props = {
  entries: WatchlistEntry[];
  prices?: PricesResponse;
  fx?: FxRates;
  onRemove: (key: string) => void;
};

export function WatchlistSection({ entries, prices, fx, onRemove }: Props) {
  return (
    <section className="watchlist-section">
      <h2 className="watchlist-section-title">📌 관심 종목</h2>
      {entries.length === 0 && (
        <div className="watchlist-section-empty">검색해서 종목을 추가하세요</div>
      )}
      {entries.length > 0 && (
        <div className="watchlist-grid">
          {entries.map((entry) => (
            <WatchlistCard
              key={entry.key}
              entry={entry}
              payload={prices?.tickers[entry.key]}
              fx={fx}
              onRemove={onRemove}
            />
          ))}
        </div>
      )}
    </section>
  );
}
```

- [ ] **Step 4: WatchlistSection test**

`src/components/WatchlistSection.test.tsx` (신규):
```tsx
import { describe, it, expect, vi, afterEach } from 'vitest';
import { render, screen, cleanup } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import { WatchlistSection } from './WatchlistSection';
import type { WatchlistEntry, FxRates } from '@shared/types/prices.js';

afterEach(() => cleanup());

const fx: FxRates = { officialUsdKrw: 1471.7, usdtKrw: 1470, divergencePct: 0 };
const sample: WatchlistEntry = { key: 'aapl', source: 'binance', symbol: 'AAPLUSDT', label: 'Apple Inc.', tier: 2 };

describe('WatchlistSection', () => {
  it('shows empty placeholder when entries 0', () => {
    render(<WatchlistSection entries={[]} fx={fx} onRemove={vi.fn()} />);
    expect(screen.getByText(/검색해서 종목을 추가하세요/)).toBeInTheDocument();
  });

  it('renders WatchlistCard per entry', () => {
    render(<WatchlistSection entries={[sample, { ...sample, key: 'msft', label: 'Microsoft' }]} fx={fx} onRemove={vi.fn()} />);
    expect(screen.getByText('Apple Inc.')).toBeInTheDocument();
    expect(screen.getByText('Microsoft')).toBeInTheDocument();
  });
});
```

- [ ] **Step 5: Integrate in App.tsx**

`src/App.tsx` 수정. SearchBar 를 header 아래, WatchlistSection 을 정적 카드 grid 다음에 추가:

```tsx
import { SearchBar } from './components/SearchBar';
import { WatchlistSection } from './components/WatchlistSection';
import { useWatchlist } from './hooks/useWatchlist';

function App() {
  const { entries, add, remove } = useWatchlist();
  // 기존 useSWR('/api/prices', ...) 를 watchlist query 포함하도록 수정
  const watchlistQuery = entries.map(e => `${e.key}:${e.source}:${e.symbol}`).join(',');
  const swrKey = watchlistQuery
    ? `/api/prices?watchlist=${encodeURIComponent(watchlistQuery)}`
    : '/api/prices';
  const { data: prices } = useSWR<PricesResponse>(swrKey, fetcher, { refreshInterval: 2000 });

  return (
    <>
      <Header />
      <SearchBar onAdd={add} />
      {/* 기존 정적 카드 grid */}
      <StockHeroCard ... />
      <div className="index-grid"> ... </div>
      {/* v0.5.0 신규 */}
      <WatchlistSection entries={entries} prices={prices} fx={prices?.fx} onRemove={remove} />
    </>
  );
}
```

⚠️ App.tsx 의 실제 구조 확인 후 정확한 위치 결정. SWR key 가 watchlist 포함하도록 변경 필수.

- [ ] **Step 6: Add minimal CSS**

`src/styles/components/watchlist.css` (신규 또는 기존 layout.css 확장):
```css
.search-bar-wrap {
  position: relative;
  margin: 1rem auto;
  max-width: 600px;
}

.search-bar-form {
  display: flex;
  gap: 0.5rem;
}

.search-bar-input {
  flex: 1;
  padding: 0.5rem 1rem;
  font-size: 1rem;
  border: 1px solid var(--border-color, #d4c5a8);
  border-radius: 6px;
}

.search-dropdown {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  background: var(--card-bg, #f5ede0);
  border: 1px solid var(--border-color, #d4c5a8);
  border-radius: 6px;
  margin-top: 0.25rem;
  z-index: 10;
}

.search-dropdown-item {
  display: block;
  width: 100%;
  padding: 0.5rem 1rem;
  text-align: left;
  background: none;
  border: none;
  border-bottom: 1px solid var(--border-color, #e8dcc4);
  cursor: pointer;
}

.search-dropdown-item:hover {
  background: var(--hover-bg, #ede0c4);
}

.watchlist-section {
  margin: 2rem 1rem;
}

.watchlist-section-title {
  font-size: 1.25rem;
  margin-bottom: 1rem;
}

.watchlist-section-empty {
  padding: 2rem;
  text-align: center;
  color: var(--text-muted, #8a7d68);
}

.watchlist-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 1rem;
}

.watchlist-card-wrap {
  position: relative;
}

.watchlist-card-remove {
  position: absolute;
  top: 0.5rem;
  right: 0.5rem;
  z-index: 2;
  width: 24px;
  height: 24px;
  border: none;
  background: rgba(0, 0, 0, 0.1);
  border-radius: 50%;
  cursor: pointer;
  font-size: 0.875rem;
}

@media (max-width: 640px) {
  .watchlist-grid {
    grid-template-columns: 1fr;
  }
}
```

`src/main.css` 또는 적절한 entry 에서 import:
```css
@import "./styles/components/watchlist.css";
```

- [ ] **Step 7: Run all new tests + regression**

```bash
pnpm exec tsc --noEmit && pnpm test
```
Expected: 전부 PASS (237 + 5 = 242).

- [ ] **Step 8: Commit**

```bash
git add src/components/WatchlistCard.tsx src/components/WatchlistCard.test.tsx \
        src/components/WatchlistSection.tsx src/components/WatchlistSection.test.tsx \
        src/App.tsx src/styles/components/watchlist.css src/main.css
git commit -m "feat(client): WatchlistCard + Section + App integration (Wave 6)

- WatchlistCard = IndexCompactCard wrapper + 우상단 X 버튼 (a11y label)
- WatchlistSection = '📌 관심 종목' grid + 빈 placeholder
- App.tsx: SearchBar + WatchlistSection 통합, SWR key 에 watchlist 포함
- CSS: watchlist.css 신규 (모바일 1-column grid)
- v0.4.2 IndexCompactCard 변경 0

Spec §3.1 / §4.1 / Q11 결정"
```

---

## Task 8: Wave 7 — Edge cases + a11y polish

**Files:**
- Modify: `src/components/SearchBar.tsx` (특수문자 + 길이 client validation)
- Modify: `src/components/SearchDropdown.tsx` (a11y: keyboard navigation)
- Create: `src/components/SearchBar.edge.test.tsx` (또는 기존 test 확장)

- [ ] **Step 1: Client validation**

`src/components/SearchBar.tsx` 의 handleSubmit 강화:
```ts
const Q_REGEX = /^[a-zA-Z0-9가-힣\s.-]+$/;

const handleSubmit = useCallback(async (e: React.FormEvent) => {
  e.preventDefault();
  const q = query.trim();
  if (q.length < 2 || q.length > 32) {
    setResponse({ tier: null, results: [], reason: q.length < 2 ? 'too_short' : 'too_long' });
    return;
  }
  if (!Q_REGEX.test(q)) {
    setResponse({ tier: null, results: [], reason: 'invalid_chars' });
    return;
  }
  setLoading(true);
  const res = await searchTicker(q);
  setResponse(res);
  setLoading(false);
}, [query]);
```

- [ ] **Step 2: Keyboard navigation in SearchDropdown**

```tsx
// onKeyDown 추가 (Enter = pick, Esc = close, Arrow Up/Down = navigate)
// 단순화: focus 자동 이동만 추가 (Tab 으로 list 안 이동 가능하도록 button 자체 사용)
```

(키보드 navigation 은 button role 사용 시 Tab 으로 이동 가능 — 별도 코드 X)

- [ ] **Step 3: Edge case tests**

`src/components/SearchBar.test.tsx` 에 추가:
```tsx
it('rejects q with special chars before API call', async () => {
  const fetchSpy = vi.spyOn(global, 'fetch');
  render(<SearchBar onAdd={vi.fn()} />);
  fireEvent.change(screen.getByLabelText('종목 검색'), { target: { value: 'a;b' } });
  fireEvent.click(screen.getByText('추가'));
  await screen.findByText(/허용되지 않는 문자/);
  expect(fetchSpy).not.toHaveBeenCalled();
});

it('rejects q > 32 chars', async () => {
  const fetchSpy = vi.spyOn(global, 'fetch');
  render(<SearchBar onAdd={vi.fn()} />);
  fireEvent.change(screen.getByLabelText('종목 검색'), { target: { value: 'a'.repeat(33) } });
  // input maxLength=32 라 33자 입력 X — typed 길이는 32. button 가능. 단 server 가 reject.
});
```

- [ ] **Step 4: Run + commit**

```bash
pnpm exec tsc --noEmit && pnpm test
```

```bash
git add src/components/SearchBar.tsx src/components/SearchBar.test.tsx
git commit -m "feat(client): SearchBar edge case validation (Wave 7)

- Client-side reject: special chars, < 2자, > 32자
- API call 우회 (server 호출 줄임)
- 일관 reason 메시지 (SearchDropdown 표시)

Spec §3.2 / §6"
```

---

## Task 9: Wave 8 — README + smoke + Codex final review

**Files:** (검증만, 수정은 review 결과에 따라)

- [ ] **Step 1: Update README**

`README.md` 에 새 section 추가:
```markdown
## 종목 검색 (v0.5.0)

대시보드 상단 검색바에 종목명 또는 ticker 입력:

| 입력 | Tier 1 hit | Tier 2 hit | Tier 3 hit |
|---|---|---|---|
| `Apple`, `AAPL` | Finnhub /search (NASDAQ) | AAPLUSDT (Binance perp) | xyz:AAPL (HL xyz) |
| `삼성전자`, `005930` | Naver 검색 (KRX) | — | — |
| `JP225` | Finnhub 0 hit | JP225USDT 0 hit | xyz:JP225 (HL xyz) |

**제약**:
- 주식만 지원 (코인 자동 제외, Finnhub `Common Stock`/`ETF` filter)
- 최대 50 종목 watchlist
- localStorage 영구 (multi-device sync X)
- 한글 검색 Naver fail 시 영문 ticker 직접 입력
```

- [ ] **Step 2: Manual smoke (5 시나리오)**

```bash
pkill -f "tsx watch.*server/index.ts"; sleep 1; pnpm dev:server > /tmp/v050-smoke.log 2>&1 &; sleep 5

# 1. 검색 → 추가 흐름 (영문)
curl -s "http://localhost:3001/api/search?q=Apple" | jq '.results[0]'

# 2. 검색 (한글)
curl -s "http://localhost:3001/api/search?q=$(echo -n '삼성' | jq -sRr @uri)" | jq '.results[0]'

# 3. 검색 결과 0개 (not_found)
curl -s "http://localhost:3001/api/search?q=NOEXIST" | jq '.reason'

# 4. /api/prices?watchlist=...
curl -s "http://localhost:3001/api/prices?watchlist=aapl:binance:AAPLUSDT" | jq '.tickers.aapl.binance | {source, price}'

# 5. 잘못된 입력
curl -s "http://localhost:3001/api/search?q=a;b" | jq '.reason'  # invalid_chars

# 브라우저 (http://localhost:5173):
# - SearchBar 에 "Apple" → dropdown → 클릭 → 카드 추가 + 가격 표시
# - 새로고침 → 카드 유지
# - X 버튼 → 카드 삭제
```

- [ ] **Step 3: Codex final review**

Codex 에 v0.5.0 전체 commit range 전달:
```bash
git log --oneline 59259e4..HEAD
git diff 59259e4..HEAD --stat
```

Codex 프롬프트:
```
v0.5.0 (티커 검색 기능) final review:
- spec: docs/superpowers/specs/2026-05-13-v0.5.0-ticker-search-design.md (v4)
- 변경 범위: commit 59259e4..HEAD

검증 포인트:
1. 3-tier resolver server-side 완전성
2. Query encoding (KEY:SOURCE:SYMBOL) parsing + validation
3. SingleFlight cache 정책 (정기 poll only, 검색 probe 우회)
4. v0.4.2 회귀 X (IndexCompactCard 변경 0, /api/prices backward compat)
5. 카파시 가드 (신규 파일 production 8, 추상화 0)

[카파시 / AI slop 금지 추가 검증]
- 새 컴포넌트가 기존 IndexCompactCard 100% wrapper?
- ticker-resolver 가 단순 HTTP wrapper (Strategy/Manager 패턴 X)?
- useWatchlist 가 localStorage 직접 호출 (Manager 추상화 X)?
- 3-tier 가 if/else 단일 함수 (Tier 별 클래스 X)?

Output: BLOCK/FLAG/PASS verdict per issue.
```

- [ ] **Step 4: Resolve Codex BLOCK/FLAG (if any)**

BLOCK → 즉시 수정 + 재검토.
FLAG → 우선순위 결정.
PASS → Step 5.

- [ ] **Step 5: v0.5.0 release commit (optional tag)**

```bash
git log --oneline 59259e4..HEAD
# v0.5.0 완료 표시 (선택)
```

사용자에게 PR merge 결정 위임.

---

## Self-Review

(plan 작성자가 spec v4 대비 점검)

### 1. Spec coverage

| Spec section | Task 매핑 |
|---|---|
| §2 Q1 (localStorage) | Task 1 (useWatchlist) |
| §2 Q2 (auto-detect) | Task 4 (Hangul detect in search route) |
| §2 Q3 (Finnhub + Naver) | Task 2 + Task 3 |
| §2 Q4 (Tier fallback) | Task 4 (search route) |
| §2 Q5 (CEX 3개) | Task 4 (Tier 2 probe) |
| §2 Q6 (Dashboard top) | Task 6 (SearchBar) + Task 7 (App.tsx) |
| §2 Q7 (Dropdown top 5) | Task 6 (SearchDropdown) |
| §2 Q8 (별도 section) | Task 7 (WatchlistSection) |
| §2 Q9 (HL allMids) | Task 4 (Tier 3 lookup) |
| §2 Q10 (Enter submit) | Task 6 (SearchBar handleSubmit) |
| §2 Q11 (WatchlistCard wrapper) | Task 7 (WatchlistCard) |
| §2 Q12 (메인 endpoint 확장) | Task 5 (parseWatchlist + assemble dynamic) |
| §3.2 server-side 3-tier | Task 4 |
| §3.3 query encoding | Task 5 (parseWatchlist) |
| §3.4 singleFlight policy | Task 4 (fetchX 직접 호출) |
| §4 Components | Task 1-7 |
| §5 localStorage schema | Task 1 |
| §6 Error handling | Task 1, 4, 6, 8 |
| §7 Testing 40+ cases | Task 1-7 모두 |
| §8 v0.4.2 호환성 | Task 5 (backward compat), Task 7 (IndexCompactCard 변경 X) |
| §12 Wave 1-8 | Task 1-9 |
| §11 완료 정의 | Task 9 (Codex + smoke) |

✅ 모든 spec 요구사항 task 매핑됨.

### 2. Placeholder scan

- "TBD"/"TODO" 검색 → 0건
- 모든 step 에 구체적 코드 또는 명령
- 모든 함수 시그니처 명시

✅ Clean.

### 3. Type consistency

- `WatchlistEntry`/`SearchResult`/`SearchResponse` 시그니처 Task 1 정의, Task 4/5/6 사용 일치
- `SourceName` 9 keys 모든 곳 동일
- `parseWatchlist` 반환 타입 = `{key, source, symbol}` — Task 5 정의, assemble.ts 사용 일치
- `useWatchlist.add/remove` 시그니처 Task 1 정의, Task 6 SearchBar 사용 일치

✅ Consistent.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-13-v0.5.0-ticker-search.md`. Two execution options:

**1. Subagent-Driven (recommended)** — fresh subagent 1개 per Task, two-stage review (spec + code quality) between Tasks. v0.4.2 에서 사용한 방식 (11 Task / 20 commits / Codex 통과).

**2. Inline Execution** — 현 세션에서 executing-plans batch + checkpoint.

**추천:** **1 (Subagent-Driven)** — 9 Task 가 대부분 isolated (Wave 별 분리). 본 세션 context 보호.
