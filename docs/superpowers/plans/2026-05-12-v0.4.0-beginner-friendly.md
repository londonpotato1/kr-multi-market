# v0.4.0 — Beginner-Friendly Dashboard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.4.0 of `kr-multi-market` — KRX 전일 종가 기준 변동률 + USDT 환산 + 동적 PRIMARY (장중=KRX / 마감=HL→KRW) + 초보자 친화 UX (한글 라벨 / 툴팁 / 카운트다운 / 도움말 / 인트로 / 모바일 단순화).

**Architecture:** 7-Wave 분리. Server-side data extension (Wave 1) → client lib (`market-state.ts` + `labels.ts`, Wave 2) → 신규 UI 컴포넌트 (InfoTip + HelpPanel, Wave 3) → 카드 통합 (Wave 4) → App.tsx + 모바일 (Wave 5) → 정리 (Wave 6) → 검증 (Wave 7). 4 Codex 체크포인트 + 카파시 grep 가드.

**Tech Stack:** Vite 8 + React 19 + TS 6 (strict) + Express 4 + Vitest 4 + happy-dom + SWR 2 + @testing-library + axe-core. pnpm 11.

**Spec reference:** `(spec dir)/2026-05-12-v0.4.0-beginner-friendly-design.md` (commit `cb4b1d6`).

**Path 단축**:
- `(spec)` = `docs/superpowers/specs`
- `(plan)` = `docs/superpowers/plans`

---

## File Inventory (sample/example structure)

### Wave 1 — Server

```
server/lib/sources/naver.ts     [MODIFY] previousClose + previousCloseSource 'naver' 추가
server/lib/sources/yahoo.ts     [MODIFY] previousClose fallback ('yahoo') 추가
server/lib/session.ts           [MODIFY] krxMinsUntilOpen / krxMinsUntilClose 계산
server/lib/cache.ts             [MODIFY] previousClose last-good cache (24h TTL)
shared/types/prices.ts          [MODIFY] PricePoint.previousClose + SessionState 필드 추가
server/tests/sources/naver.test.ts    [MODIFY/CREATE]
server/tests/session.test.ts          [MODIFY/CREATE]
```

### Wave 2 — Client lib

```
src/lib/market-state.ts         [CREATE] pickPrimary + pctVsClose + getStateBadge 3 함수
src/lib/market-state.test.ts    [CREATE]
src/lib/labels.ts               [CREATE] 한글 라벨 매핑 상수 (8 매핑)
```

### Wave 3 — UI 신규 컴포넌트

```
src/components/InfoTip.tsx           [CREATE] hybrid hover+click popover
src/components/InfoTip.test.tsx      [CREATE]
src/components/HelpPanel.tsx         [CREATE] collapsible <details>
src/components/HelpPanel.test.tsx    [CREATE]
src/styles/components/info-tip.css   [CREATE]
src/styles/components/help-panel.css [CREATE]
```

### Wave 4 — 카드 통합

```
src/components/StockHeroCard.tsx       [MODIFY] PRIMARY swap + USDT sub + 종가 변동률
                                                + 한글 라벨 + InfoTip 4개 + 보조 가격 행
src/components/StockHeroCard.test.tsx  [MODIFY] session fixture 추가, 신규 케이스
src/components/IndexCompactCard.tsx    [MODIFY] USDT sub + 한글 라벨 + InfoTip 1개
src/components/IndexCompactCard.test.tsx [MODIFY]
src/components/SignalBadge.tsx         [MODIFY] tier 한글 매핑 (labels.ts)
src/components/SpreadRow.tsx           [MODIFY] "거래소 간 격차 최대" 라벨 + InfoTip
src/styles/components/stock-hero.css   [MODIFY]
src/styles/components/index-compact.css[MODIFY]
```

### Wave 5 — App + 모바일

```
src/App.tsx                            [MODIFY] 부제목 변경 + HelpPanel + 카운트다운 chip 인라인
src/styles/components/fx-header.css    [MODIFY] .market-countdown 스타일 추가
src/tests/a11y.test.tsx                [MODIFY] session.krx + previousClose stub 추가
```

### Wave 6 — 정리

```
src/components/PremiumRow.tsx          [DELETE]
src/styles/components/premium-row.css  [DELETE 또는 통합]
src/App.css                            [MODIFY] premium-row.css @import 제거 (삭제 시)
```

### Wave 7 — 검증

```
(no new files, baseline screenshots updated)
(base)/v0.4.0-{dark,light}-{open,closed}.png  # 4-state captures
```

---

## Agent Workflow

### Per-Task Agent 매핑

| Wave | Task | 1차 agent | 검증 | Codex |
|---|---|---|---|---|
| 1 | W1.1-W1.4 (서버 데이터) | coder | tester | **Codex #1** (Wave 1 끝) |
| 2 | W2.1-W2.2 (lib) | coder TDD | tester | — |
| 3 | W3.1-W3.2 (InfoTip + HelpPanel) | coder TDD | tester | — |
| 4 | W4.1-W4.4 (카드 통합) | coder | code-reviewer | **Codex #2** (Wave 4 끝) |
| 5 | W5.1-W5.2 (App + 모바일) | coder | code-reviewer | — |
| 6 | W6.1-W6.2 (PremiumRow 정리) | refactor-cleaner | code-reviewer | **Codex #3** (Wave 6 끝) |
| 7 | W7.1-W7.3 (검증 + smoke) | bash 직접 + 사용자 | — | **Codex #4** (final v0.4.0) |

### 카파시 prompt template (모든 subagent 의무 주입)

```
[카파시 스타일 / AI slop 금지 — 의무 검증]
- 이 코드가 3줄로 될 걸 과추상화했는가?
- 1-use 헬퍼/팩토리/매니저 양산은 없는가?
- "혹시 나중에" 보존 코드가 끼어 있는가?
- 사용자 지시 없는 로직/기능이 삽입됐는가?
- 미사용 export / type / token 이 생겼는가?

[리뷰 에이전트] 발견된 slop 은 severity 무관 별도 섹션으로 보고.
```

### 두 단계 리뷰

각 Wave Task 후:
1. **quick-validator**: `pnpm exec tsc --noEmit -p tsconfig.app.json` + `pnpm test -- <file>`
2. **code-reviewer** (Wave 4/5/6 만): 카파시 가드 + 품질

실패 시 fresh subagent 로 재실행 (최대 2회).

### Codex 체크포인트 prompt 핵심

| # | 시점 | 검토 포인트 |
|---|---|---|
| **#1** | Wave 1 끝 | `previousClose` source 우선순위 작동 확인 + Naver/Yahoo 실제 응답 검증 + cache fallback + krxMinsUntilOpen/Close 정확성 |
| **#2** | Wave 4 끝 | StockHeroCard 동적 PRIMARY swap 6 case (C1-C6) + isStale UI + 종가 변동률 polish + IndexCompactCard USDT + 한글 라벨 grep 0 영문 누출 |
| **#3** | Wave 6 끝 | PremiumRow 완전 제거 (JSX + CSS class 모두 0 hits) + market-state.ts 2+ consumer 확인 + 1-use 헬퍼 grep |
| **#4** | Wave 7 끝, PR 직전 | 전체 v0.4.0 — spec §8 검증 기준 13 items + 4-state screenshots + axe critical 0 + 모바일 (375px) 카드 height ≤ 380px |

---

# Wave 1 — Server Data Extension

## Task W1.1: Type schema 확장

**Files:**
- Modify: `shared/types/prices.ts` (PricePoint + SessionState 필드)
- Modify: 어떤 server/client 컴포넌트도 영향 없음 (optional fields)

- [ ] **Step 1: PricePoint 에 previousClose + previousCloseSource 추가**

`shared/types/prices.ts` 의 `PricePoint` 정의에 옵셔널 필드 2개:

```ts
export type PricePoint = {
  source: SourceName;
  symbol: string;
  price: number;
  unit: 'USD' | 'KRW' | 'USDT' | 'pt';
  change24hPct?: number;
  volume24hUsd?: number;
  fundingRate8h?: number;
  openInterestUsd?: number;
  status: SourceStatus;
  asOf: number;
  receivedAt: number;
  staleReason?: string;
  schemaVersion: number;
  // === v0.4.0 신규 (Naver only, optional) ===
  previousClose?: number;
  previousCloseSource?: 'naver' | 'yahoo' | 'cache';
};
```

- [ ] **Step 2: SessionState 에 카운트다운 필드 추가**

같은 파일의 `SessionState`:

```ts
export type SessionState = {
  krx: boolean;
  krxAfter: boolean;
  krxNight: boolean;
  nyseRegular: boolean;
  nysePrePost: boolean;
  cme: boolean;
  hyperliquid: true;
  binance: true;
  // === v0.4.0 신규 ===
  krxMinsUntilOpen?: number;   // 장 마감 또는 휴장 시 set
  krxMinsUntilClose?: number;  // 장중에 set
};
```

- [ ] **Step 3: TS 빌드 확인**

```bash
pnpm exec tsc --noEmit -p tsconfig.app.json
pnpm exec tsc --noEmit -p server/tsconfig.json
```
Expected: 둘 다 통과 (optional 필드라 기존 코드 영향 0).

- [ ] **Step 4: Commit**

```bash
git add shared/types/prices.ts
git commit -m "feat(types): add previousClose + previousCloseSource + krxMinsUntilOpen/Close (v0.4.0)"
```

## Task W1.2: KRX countdown 계산 (`server/lib/session.ts`)

**Files:**
- Modify: `server/lib/session.ts` (krxMinsUntilOpen / krxMinsUntilClose 계산)
- Create: `server/tests/session-countdown.test.ts`

- [ ] **Step 1: TDD — failing test 작성**

`server/tests/session-countdown.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { getSessionState } from '../lib/session';

describe('session countdown — KRX', () => {
  it('returns krxMinsUntilClose during open hours (mid-session)', () => {
    // 평일 14:00 KST (KRX 마감까지 90분)
    const krxOpen = new Date('2025-01-15T05:00:00Z'); // 14:00 KST
    const s = getSessionState(krxOpen);
    expect(s.krx).toBe(true);
    expect(s.krxMinsUntilClose).toBe(90);
    expect(s.krxMinsUntilOpen).toBeUndefined();
  });

  it('returns krxMinsUntilOpen after market close (16:00 KST same day)', () => {
    // 평일 16:00 KST → 다음 영업일 09:00 KST 까지 17시간
    const afterClose = new Date('2025-01-15T07:00:00Z'); // 16:00 KST 수요일
    const s = getSessionState(afterClose);
    expect(s.krx).toBe(false);
    expect(s.krxMinsUntilOpen).toBeGreaterThan(0);
    expect(s.krxMinsUntilOpen).toBeLessThanOrEqual(17 * 60); // 17 시간 이내
    expect(s.krxMinsUntilClose).toBeUndefined();
  });

  it('handles weekend (Saturday → Monday open)', () => {
    const saturday = new Date('2025-01-18T03:00:00Z'); // 토요일 12:00 KST
    const s = getSessionState(saturday);
    expect(s.krx).toBe(false);
    expect(s.krxMinsUntilOpen).toBeDefined();
    // 월요일 09:00 KST 까지 약 45시간 (2700분)
    expect(s.krxMinsUntilOpen).toBeGreaterThan(40 * 60);
  });
});
```

- [ ] **Step 2: Run — Expected FAIL**

```bash
pnpm test -- server/tests/session-countdown.test.ts
```
Expected: krxMinsUntilOpen/Close undefined 인 채 fail.

- [ ] **Step 3: 구현 — `getSessionState()` 확장**

`server/lib/session.ts` 의 `getSessionState()` 끝 부분 (return 직전) 에 추가:

```ts
// === v0.4.0 카운트다운 계산 ===
let krxMinsUntilOpen: number | undefined;
let krxMinsUntilClose: number | undefined;

if (krx) {
  // 장중: 마감 15:30 KST 까지 분
  const closeMins = 15 * 60 + 30;
  krxMinsUntilClose = Math.max(0, closeMins - kstMins);
} else {
  // 마감 또는 휴장: 다음 영업일 09:00 KST 까지 분
  krxMinsUntilOpen = computeMinsUntilNextKrxOpen(now);
}
```

새 헬퍼 함수 (같은 파일 안):

```ts
function computeMinsUntilNextKrxOpen(now: Date): number {
  const kst = new Date(now.getTime() + 9 * 60 * 60 * 1000); // UTC → KST
  const target = new Date(kst);
  target.setUTCHours(0, 0, 0, 0); // 다음 09:00 KST 의 자정 기준

  let added = 0;
  // 평일 + 비공휴일 + 시간 09:00 이전 인 다음 시점 탐색 (최대 7일)
  for (let i = 0; i < 7; i++) {
    const candidate = new Date(target.getTime() + i * 24 * 60 * 60 * 1000);
    const wd = candidate.getUTCDay();
    if (wd >= 1 && wd <= 5 && !isKrxHoliday(candidate)) {
      candidate.setUTCHours(9, 0, 0, 0);
      // 만약 오늘이고 시간이 09:00 이전이면 same day, 아니면 다음 영업일
      if (i === 0 && kst.getUTCHours() < 9) {
        return Math.round((candidate.getTime() - kst.getTime()) / 60000);
      } else if (i > 0) {
        return Math.round((candidate.getTime() - kst.getTime()) / 60000);
      }
    }
  }
  return 0; // 안전 폴백
}
```

- [ ] **Step 4: Run — Expected PASS**

```bash
pnpm test -- server/tests/session-countdown.test.ts
```
Expected: 3 tests pass.

- [ ] **Step 5: Commit**

```bash
git add server/lib/session.ts server/tests/session-countdown.test.ts
git commit -m "feat(server/session): add krxMinsUntilOpen/Close countdown calculation (W1.2)"
```

## Task W1.3: Naver previousClose 추출

**Files:**
- Modify: `server/lib/sources/naver.ts` (응답 분석 + previousClose 필드 set)
- Modify: `server/tests/sources/naver.test.ts` (또는 신규)

- [ ] **Step 1: Naver 실제 응답 검증 — 수동 1회**

먼저 dev 환경에서 실제 Naver 응답 캡처 (장중과 휴장 둘 다):

```bash
# 장중 시간에 실행
curl -s "https://polling.finance.naver.com/api/realtime/domestic/stock/005930,000660,005380" | head -100 > /tmp/naver-open.json

# 휴장 시간 (저녁) 에 실행
curl -s "https://polling.finance.naver.com/api/realtime/domestic/stock/005930,000660,005380" | head -100 > /tmp/naver-closed.json
```

응답 안의 어떤 필드가 "전일 종가" 인지 확인. 후보:
- `pcv` (Previous Close Value)
- `nv` (Net Value — 보통 마지막 체결가)
- `closePrice` / `closePriceRaw` (이름 모호)

만약 `pcv` 필드가 있으면 그게 정답. 없으면 Wave 1.4 Yahoo fallback 만으로 진행.

- [ ] **Step 2: TDD — failing test**

`server/tests/sources/naver-prev-close.test.ts` (신규):

```ts
import { describe, it, expect } from 'vitest';
import { parseNaverResponse } from '../../lib/sources/naver'; // export 필요

describe('Naver previousClose extraction', () => {
  it('extracts pcv field when present', () => {
    const stub = {
      datas: [{ itemCode: '005930', nv: '285500', pcv: '290000', cr: '-1.55' }],
    };
    const result = parseNaverResponse(stub, true /* market open */);
    expect(result['samsung']?.previousClose).toBe(290000);
    expect(result['samsung']?.previousCloseSource).toBe('naver');
  });

  it('returns undefined previousClose when pcv missing', () => {
    const stub = { datas: [{ itemCode: '005930', nv: '285500' }] };
    const result = parseNaverResponse(stub, true);
    expect(result['samsung']?.previousClose).toBeUndefined();
  });
});
```

- [ ] **Step 3: Run — Expected FAIL**

```bash
pnpm test -- server/tests/sources/naver-prev-close.test.ts
```
Expected: parseNaverResponse 가 export 안 되어 있어서 fail, 또는 previousClose 가 set 안 되어 fail.

- [ ] **Step 4: 구현 — Naver 파서 확장**

`server/lib/sources/naver.ts` 에서:
1. 응답 item 인터페이스에 `pcv?: string` 추가
2. `parseNaverResponse` 함수 export (이미 internal 이면 export 추가)
3. 가격 객체 생성 시 `previousClose: parseNaverNumber(item.pcv)` (있을 때) + `previousCloseSource: 'naver'` set

핵심 로직 (기존 파싱 코드 안에 1줄 추가):

```ts
const previousClose = item.pcv ? parseNaverNumber(item.pcv) : undefined;
const pricePoint: PricePoint = {
  // ... 기존 ...
  previousClose,
  previousCloseSource: previousClose !== undefined ? 'naver' : undefined,
};
```

- [ ] **Step 5: Run — Expected PASS**

```bash
pnpm test -- server/tests/sources/naver-prev-close.test.ts
```

- [ ] **Step 6: Commit**

```bash
git add server/lib/sources/naver.ts server/tests/sources/naver-prev-close.test.ts
git commit -m "feat(server/naver): extract previousClose from pcv field (W1.3)"
```

> **Note**: Step 1 에서 Naver 응답에 `pcv` 가 없으면 이 task 는 SKIP 하고 W1.4 Yahoo 만 사용. 그 경우 commit msg 에 "Naver pcv unavailable — fallback to W1.4" 명시.

## Task W1.4: Yahoo previousClose fallback + server cache

**Files:**
- Modify: `server/lib/sources/yahoo.ts` (previousClose 필드 set)
- Modify: `server/lib/cache.ts` (또는 새 `server/lib/prev-close-cache.ts`): 24h TTL last-good cache
- Modify: 서버 응답 빌더 (`server/index.ts` 또는 `server/lib/normalize.ts`): naver → yahoo → cache 폴백 체인

- [ ] **Step 1: TDD — failing test (cache 폴백)**

`server/tests/prev-close-cache.test.ts` (신규):

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { storePrevClose, getPrevClose } from '../lib/prev-close-cache';

describe('prev-close-cache', () => {
  beforeEach(() => {
    // cache 초기화
  });

  it('stores and retrieves a value within TTL', () => {
    storePrevClose('samsung', 290000, 'naver');
    const cached = getPrevClose('samsung');
    expect(cached?.value).toBe(290000);
    expect(cached?.source).toBe('naver');
  });

  it('returns undefined after TTL expires (24h)', async () => {
    storePrevClose('samsung', 290000, 'naver');
    // 시간 조작 (Date.now mock 또는 ttl 짧게)
    // ... TTL > 24h 후 ...
    const cached = getPrevClose('samsung');
    expect(cached).toBeUndefined();
  });
});
```

- [ ] **Step 2: Run — Expected FAIL**

```bash
pnpm test -- server/tests/prev-close-cache.test.ts
```

- [ ] **Step 3: 구현 — cache module**

`server/lib/prev-close-cache.ts` 신규:

```ts
type CacheEntry = { value: number; source: 'naver' | 'yahoo'; storedAt: number };
const TTL_MS = 24 * 60 * 60 * 1000; // 24h
const cache = new Map<string, CacheEntry>();

export function storePrevClose(ticker: string, value: number, source: 'naver' | 'yahoo'): void {
  if (value <= 0) return;
  cache.set(ticker, { value, source, storedAt: Date.now() });
}

export function getPrevClose(ticker: string): { value: number; source: 'naver' | 'yahoo' | 'cache' } | undefined {
  const entry = cache.get(ticker);
  if (!entry) return undefined;
  if (Date.now() - entry.storedAt > TTL_MS) {
    cache.delete(ticker);
    return undefined;
  }
  return { value: entry.value, source: 'cache' };
}
```

- [ ] **Step 4: Yahoo source previousClose 추출**

`server/lib/sources/yahoo.ts` 에서 Yahoo chart API 응답의 `meta.previousClose` 또는 `meta.chartPreviousClose` 필드를 추출하여 `PricePoint.previousClose` + `previousCloseSource: 'yahoo'` set.

```ts
const previousClose = result?.meta?.previousClose ?? result?.meta?.chartPreviousClose;
const pp: PricePoint = {
  // ... 기존 ...
  previousClose: previousClose ?? undefined,
  previousCloseSource: previousClose !== undefined ? 'yahoo' : undefined,
};
```

- [ ] **Step 5: 폴백 체인 — server 응답 빌더에서 적용**

`server/index.ts` 또는 응답 빌더에서 ticker 별로:

```ts
import { storePrevClose, getPrevClose } from './lib/prev-close-cache';

function resolvePrevClose(ticker: string, naverPP?: PricePoint, yahooPP?: PricePoint): { value: number; source: 'naver' | 'yahoo' | 'cache' } | undefined {
  if (naverPP?.previousClose !== undefined) {
    storePrevClose(ticker, naverPP.previousClose, 'naver');
    return { value: naverPP.previousClose, source: 'naver' };
  }
  if (yahooPP?.previousClose !== undefined) {
    storePrevClose(ticker, yahooPP.previousClose, 'yahoo');
    return { value: yahooPP.previousClose, source: 'yahoo' };
  }
  return getPrevClose(ticker); // cache 폴백
}

// 응답에 set:
const prev = resolvePrevClose(ticker, naverPP, yahooPP);
if (prev) {
  payload.naver = { ...payload.naver, previousClose: prev.value, previousCloseSource: prev.source };
}
```

- [ ] **Step 6: Run — Expected PASS**

```bash
pnpm test -- server/
```

- [ ] **Step 7: Commit**

```bash
git add server/lib/prev-close-cache.ts server/lib/sources/yahoo.ts server/index.ts server/tests/prev-close-cache.test.ts
git commit -m "feat(server): previousClose fallback chain (naver → yahoo → 24h cache) (W1.4)"
```

## Task W1.5: Codex #1 체크포인트 — Wave 1 종료

- [ ] **Step 1: Codex #1 호출**

```
Agent tool 호출:
  subagent_type: codex:codex-rescue
  prompt: |
    Mode: REVIEW ONLY.
    Context: Wave 1 of v0.4.0 PLAN — server data extension complete.
    Spec: (spec)/2026-05-12-v0.4.0-beginner-friendly-design.md (§2)
    Commits: W1.1-W1.4 added types + Naver pcv + Yahoo fallback + 24h cache.

    Verify:
    1. previousClose extraction working in dev (curl Naver/Yahoo, inspect response)
    2. cache TTL respected, value <= 0 not stored
    3. krxMinsUntilOpen/Close handles weekday/weekend/holiday correctly
    4. PricePoint backward-compat (old clients ignore new fields)
    5. server tsc strict clean

    [카파시 의무 검증]

    Output: BLOCK / FLAG / PASS verdict per spec §2.
```

Expected: PASS. BLOCK 시 inline fix 후 재호출.

---

# Wave 2 — Client lib

## Task W2.1: `src/lib/labels.ts` 생성

**Files:**
- Create: `src/lib/labels.ts`

- [ ] **Step 1: Write file**

```ts
// src/lib/labels.ts — 한글 라벨 매핑 (v0.4.0)
export const PREMIUM_TIER_LABEL = {
  cool: '잠잠',
  warm: '주의',
  hot: '과열',
  na: '데이터 없음',
} as const;

export const SIGNAL_TIER_LABEL = {
  normal: '정상',
  watch: '관찰',
  trade: '매매',
  dislocated: '이탈',
} as const;

export const STATE_BADGE_LABEL = {
  open: '🟢 장중',
  night: '🌙 야간',
  holiday: '🔴 휴장',
} as const;

export const CONTEXT_LABEL = {
  PRIMARY_LIVE: 'KRX 실시간 / HL 야간 발견가',
  PRIMARY_NIGHT: 'HL 야간 발견가 / KRX 전일 종가',
  HL_DOWN: 'HL 가격 없음',
  FX_DOWN: '환율 없음 — USD 표시',
  KRX_STALE: 'KRX 데이터 지연 — HL 임시 사용',
} as const;
```

- [ ] **Step 2: Commit (테스트 불필요 — 단순 상수)**

```bash
git add src/lib/labels.ts
git commit -m "feat(lib): add Korean label mappings (W2.1)"
```

## Task W2.2: `src/lib/market-state.ts` (TDD)

**Files:**
- Create: `src/lib/market-state.ts`
- Create: `src/lib/market-state.test.ts`

- [ ] **Step 1: TDD — failing tests (pickPrimary 6 case + pctVsClose 4 case + getStateBadge 3 case)**

`src/lib/market-state.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { pickPrimary, pctVsClose, getStateBadge } from './market-state';
import type { SessionState, PricePoint, FxRates } from '@shared/types/prices.js';

const now = Date.now();
const mkPP = (price: number, status: 'ok' | 'stale' = 'ok', source: PricePoint['source'] = 'hyperliquid'): PricePoint => ({
  source, symbol: '', price, unit: 'USD', status, asOf: now, receivedAt: now, schemaVersion: 1,
});
const mkSession = (overrides: Partial<SessionState>): SessionState => ({
  krx: false, krxAfter: false, krxNight: false, nyseRegular: false, nysePrePost: false, cme: true,
  hyperliquid: true, binance: true, ...overrides,
});
const fxOk: FxRates = { officialUsdKrw: 1471.7, usdtKrw: 1470, divergencePct: 0 };

describe('pickPrimary', () => {
  it('C1: krx ok + session open → primary=KRX', () => {
    const result = pickPrimary({
      session: mkSession({ krx: true }),
      hl: mkPP(189, 'ok'),
      krx: mkPP(279000, 'ok', 'naver'),
      fx: fxOk,
    });
    expect(result.primary?.source).toBe('krx');
    expect(result.primary?.value).toBe(279000);
  });

  it('C2: krx stale + session open → primary=HL→KRW + isStale via krx', () => {
    const result = pickPrimary({
      session: mkSession({ krx: true }),
      hl: mkPP(189, 'ok'),
      krx: mkPP(279000, 'stale', 'naver'),
      fx: fxOk,
    });
    expect(result.primary?.source).toBe('hl');
    expect(result.contextLabel).toContain('KRX 데이터 지연');
  });

  it('C3: session closed + HL ok + fx ok → primary=HL→KRW', () => {
    const result = pickPrimary({
      session: mkSession({ krx: false }),
      hl: mkPP(189, 'ok'),
      krx: mkPP(279000, 'ok', 'naver'),
      fx: fxOk,
    });
    expect(result.primary?.source).toBe('hl');
    expect(result.primary?.value).toBe(189 * 1470);
  });

  it('C4: session closed + HL ok + fx 0 → primary=HL USD native', () => {
    const result = pickPrimary({
      session: mkSession({ krx: false }),
      hl: mkPP(189, 'ok'),
      fx: { ...fxOk, usdtKrw: 0 },
    });
    expect(result.primary?.source).toBe('hl');
    expect(result.primary?.unit).toBe('USD');
    expect(result.primary?.value).toBe(189);
  });

  it('C5: HL missing + KRX ok → primary=KRX', () => {
    const result = pickPrimary({
      session: mkSession({ krx: false }),
      hl: undefined,
      krx: mkPP(279000, 'ok', 'naver'),
      fx: fxOk,
    });
    expect(result.primary?.source).toBe('krx');
    expect(result.contextLabel).toBe('HL 가격 없음');
  });

  it('C6: both missing → primary null', () => {
    const result = pickPrimary({
      session: mkSession({ krx: false }),
      hl: undefined,
      krx: undefined,
      fx: fxOk,
    });
    expect(result.primary).toBeNull();
  });
});

describe('pctVsClose', () => {
  it('positive change', () => {
    expect(pctVsClose(110, 100)).toBeCloseTo(10);
  });
  it('negative change', () => {
    expect(pctVsClose(90, 100)).toBeCloseTo(-10);
  });
  it('null when primaryKrw null', () => {
    expect(pctVsClose(null, 100)).toBeNull();
  });
  it('null when previousClose <= 0 (divide-by-zero guard)', () => {
    expect(pctVsClose(110, 0)).toBeNull();
    expect(pctVsClose(110, -50)).toBeNull();
    expect(pctVsClose(110, undefined)).toBeNull();
  });
});

describe('getStateBadge', () => {
  it('open when session.krx', () => {
    expect(getStateBadge(mkSession({ krx: true }))).toBe('open');
  });
  it('night when !krx and krxAfter', () => {
    expect(getStateBadge(mkSession({ krx: false, krxAfter: true }))).toBe('night');
  });
  it('night when !krx and krxNight', () => {
    expect(getStateBadge(mkSession({ krx: false, krxNight: true }))).toBe('night');
  });
  it('holiday when !krx and !krxAfter and !krxNight', () => {
    expect(getStateBadge(mkSession({ krx: false }))).toBe('holiday');
  });
});
```

- [ ] **Step 2: Run — Expected FAIL**

```bash
pnpm test -- src/lib/market-state.test.ts
```

- [ ] **Step 3: 구현 — `src/lib/market-state.ts`**

```ts
import type { SessionState, PricePoint, FxRates } from '@shared/types/prices.js';
import { CONTEXT_LABEL } from './labels';

export type Primary = {
  value: number;
  unit: 'KRW' | 'USD';
  source: 'krx' | 'hl';
  isStale?: boolean;
};

export type StateBadge = 'open' | 'night' | 'holiday';

export function pickPrimary(args: {
  session: SessionState;
  hl?: PricePoint;
  krx?: PricePoint;
  fx?: FxRates;
}): {
  primary: Primary | null;
  secondary: Primary | null;
  contextLabel: string;
} {
  const { session, hl, krx, fx } = args;
  const usdtKrw = fx?.usdtKrw ?? 0;
  const fxOk = usdtKrw > 0;
  const hlKrw: Primary | null = (hl && fxOk)
    ? { value: hl.price * usdtKrw, unit: 'KRW', source: 'hl' }
    : null;
  const krxP: Primary | null = krx
    ? { value: krx.price, unit: 'KRW', source: 'krx', isStale: krx.status === 'stale' }
    : null;

  // C6: both missing
  if (!hl && !krx) return { primary: null, secondary: null, contextLabel: '데이터 없음' };

  // C5: HL missing, KRX present
  if (!hl && krx) return { primary: krxP, secondary: null, contextLabel: CONTEXT_LABEL.HL_DOWN };

  // C2: krx stale during open hours → use HL
  if (session.krx && krx?.status === 'stale' && hl) {
    if (hlKrw) return { primary: hlKrw, secondary: krxP, contextLabel: CONTEXT_LABEL.KRX_STALE };
    // fx not ok, fall through
  }

  // C1: open + krx ok → primary=KRX
  if (session.krx && krx?.status === 'ok') {
    return { primary: krxP, secondary: hlKrw, contextLabel: CONTEXT_LABEL.PRIMARY_LIVE };
  }

  // C3/C4: closed → HL
  if (hl) {
    if (fxOk && hlKrw) return { primary: hlKrw, secondary: krxP, contextLabel: CONTEXT_LABEL.PRIMARY_NIGHT };
    // C4: fx 0 → USD native
    return {
      primary: { value: hl.price, unit: 'USD', source: 'hl' },
      secondary: krxP,
      contextLabel: CONTEXT_LABEL.FX_DOWN,
    };
  }

  return { primary: null, secondary: null, contextLabel: '데이터 없음' };
}

export function pctVsClose(primaryKrw: number | null, previousClose: number | undefined): number | null {
  if (primaryKrw === null || previousClose === undefined || previousClose <= 0) return null;
  return ((primaryKrw - previousClose) / previousClose) * 100;
}

export function getStateBadge(session: SessionState): StateBadge {
  if (session.krx) return 'open';
  if (session.krxAfter || session.krxNight) return 'night';
  return 'holiday';
}
```

- [ ] **Step 4: Run — Expected PASS**

```bash
pnpm test -- src/lib/market-state.test.ts
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/market-state.ts src/lib/market-state.test.ts
git commit -m "feat(lib): add market-state (pickPrimary + pctVsClose + getStateBadge) (W2.2)"
```

---

# Wave 3 — UI 신규 컴포넌트

## Task W3.1: InfoTip 컴포넌트 (TDD)

**Files:**
- Create: `src/components/InfoTip.tsx`
- Create: `src/components/InfoTip.test.tsx`
- Create: `src/styles/components/info-tip.css`

- [ ] **Step 1: Failing tests**

`src/components/InfoTip.test.tsx`:

```tsx
import { describe, it, expect, afterEach } from 'vitest';
import { render, screen, fireEvent, cleanup } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import { InfoTip } from './InfoTip';

afterEach(() => cleanup());

describe('InfoTip', () => {
  it('renders button with ⓘ glyph and aria-label', () => {
    render(<InfoTip term="프리미엄" description="KRX vs HL 가격 차이 (%)" />);
    const btn = screen.getByRole('button');
    expect(btn).toHaveAttribute('aria-label', '프리미엄: KRX vs HL 가격 차이 (%)');
    expect(btn).toHaveAttribute('title', 'KRX vs HL 가격 차이 (%)');
    expect(btn.textContent).toBe('ⓘ');
  });

  it('popover hidden by default, aria-expanded=false', () => {
    render(<InfoTip term="X" description="Y" />);
    const btn = screen.getByRole('button');
    expect(btn).toHaveAttribute('aria-expanded', 'false');
    expect(screen.queryByRole('tooltip')).toBeNull();
  });

  it('click opens popover, second click closes', () => {
    render(<InfoTip term="프리미엄" description="설명 텍스트" />);
    const btn = screen.getByRole('button');
    fireEvent.click(btn);
    expect(btn).toHaveAttribute('aria-expanded', 'true');
    expect(screen.getByRole('tooltip')).toBeInTheDocument();
    expect(screen.getByRole('tooltip').textContent).toContain('프리미엄');
    expect(screen.getByRole('tooltip').textContent).toContain('설명 텍스트');
    fireEvent.click(btn);
    expect(btn).toHaveAttribute('aria-expanded', 'false');
  });

  it('blur closes popover', () => {
    render(<InfoTip term="X" description="Y" />);
    const btn = screen.getByRole('button');
    fireEvent.click(btn);
    expect(screen.getByRole('tooltip')).toBeInTheDocument();
    fireEvent.blur(btn);
    expect(screen.queryByRole('tooltip')).toBeNull();
  });
});
```

- [ ] **Step 2: Run FAIL**

```bash
pnpm test -- src/components/InfoTip.test.tsx
```

- [ ] **Step 3: Implement**

`src/components/InfoTip.tsx`:

```tsx
import { useState } from 'react';

type Props = { term: string; description: string; ariaLabel?: string };

export function InfoTip({ term, description, ariaLabel }: Props) {
  const [open, setOpen] = useState(false);
  return (
    <span className="info-tip-wrap">
      <button
        type="button"
        className="info-tip"
        title={description}
        aria-label={ariaLabel ?? `${term}: ${description}`}
        aria-expanded={open}
        onClick={() => setOpen((v) => !v)}
        onBlur={() => setOpen(false)}
      >
        ⓘ
      </button>
      {open && (
        <span className="info-tip-popover" role="tooltip">
          <strong>{term}</strong>: {description}
        </span>
      )}
    </span>
  );
}
```

- [ ] **Step 4: CSS**

`src/styles/components/info-tip.css`:

```css
/* info-tip.css — hybrid hover (title) + click popover (Wave 3) */
.info-tip-wrap { position: relative; display: inline-block; }
.info-tip {
  background: none;
  border: none;
  padding: 0 2px;
  cursor: pointer;
  font-size: 11px;
  color: var(--text-dim);
  line-height: 1;
}
.info-tip:hover, .info-tip:focus-visible {
  color: var(--text);
  outline: none;
}
.info-tip-popover {
  position: absolute;
  top: 100%; left: 0;
  margin-top: 4px;
  z-index: 100;
  background: var(--surface);
  border: 1px solid var(--celadon-border);
  border-radius: 6px;
  padding: 8px 12px;
  font-size: 12px;
  color: var(--text);
  max-width: 280px;
  width: max-content;
  box-shadow: var(--shadow);
  line-height: 1.4;
}
.info-tip-popover strong { color: var(--hanji); }
```

- [ ] **Step 5: Add @import to App.css**

`src/App.css` 의 components 블록에 추가 (alphabetical 또는 cascade 순):

```css
@import './styles/components/info-tip.css';
```

- [ ] **Step 6: Run PASS**

```bash
pnpm test -- src/components/InfoTip.test.tsx
```

- [ ] **Step 7: Commit**

```bash
git add src/components/InfoTip.tsx src/components/InfoTip.test.tsx src/styles/components/info-tip.css src/App.css
git commit -m "feat(client): add InfoTip hybrid hover+click popover (W3.1)"
```

## Task W3.2: HelpPanel 컴포넌트 (TDD)

**Files:**
- Create: `src/components/HelpPanel.tsx`
- Create: `src/components/HelpPanel.test.tsx`
- Create: `src/styles/components/help-panel.css`

- [ ] **Step 1: Failing tests**

`src/components/HelpPanel.test.tsx`:

```tsx
import { describe, it, expect, afterEach } from 'vitest';
import { render, screen, cleanup, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import { HelpPanel } from './HelpPanel';

afterEach(() => cleanup());

describe('HelpPanel', () => {
  it('renders summary and is closed by default', () => {
    render(<HelpPanel />);
    expect(screen.getByText(/도움말 \/ 용어 설명/)).toBeInTheDocument();
    // <details> not open
    const details = screen.getByRole('group') as HTMLDetailsElement;
    expect(details.open).toBe(false);
  });

  it('opens on summary click', () => {
    render(<HelpPanel />);
    const summary = screen.getByText(/도움말 \/ 용어 설명/);
    fireEvent.click(summary);
    // 내용 노출 확인
    expect(screen.getByText(/가격 변동/)).toBeInTheDocument();
    expect(screen.getByText(/프리미엄 단계/)).toBeInTheDocument();
    expect(screen.getByText(/신호 단계/)).toBeInTheDocument();
    expect(screen.getByText(/거래소 약어/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run FAIL**

```bash
pnpm test -- src/components/HelpPanel.test.tsx
```

- [ ] **Step 3: Implement**

`src/components/HelpPanel.tsx`:

```tsx
export function HelpPanel() {
  return (
    <details className="help-panel" role="group">
      <summary>도움말 / 용어 설명 ⓘ</summary>
      <div className="help-content">
        <section>
          <h4>가격 변동</h4>
          <p>▲ 초록 = 상승 / ▼ 빨강 = 하락 / — 회색 = 보합 또는 데이터 없음</p>
        </section>
        <section>
          <h4>프리미엄 단계</h4>
          <p>잠잠 (±1% 이내) · 주의 (±1~3%) · 과열 (±3% 이상)</p>
        </section>
        <section>
          <h4>신호 단계 (Z-스코어)</h4>
          <p>정상 · 관찰 (±2σ) · 매매 (±3σ) · 이탈 (±3σ 초과)</p>
        </section>
        <section>
          <h4>거래소 약어</h4>
          <p>KRX = 한국거래소 · HL = Hyperliquid xyz · Binance = Binance 선물 · Yahoo = Yahoo Finance</p>
        </section>
      </div>
    </details>
  );
}
```

- [ ] **Step 4: CSS**

`src/styles/components/help-panel.css`:

```css
/* help-panel.css — collapsible 도움말 (Wave 3) */
.help-panel {
  background: var(--surface);
  border: 1px solid var(--border-divider);
  border-radius: 8px;
  padding: 8px 14px;
  margin: 16px 0;
  font-size: 12px;
}
.help-panel summary {
  cursor: pointer;
  font-weight: 500;
  color: var(--hanji);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  font-size: 11px;
}
.help-panel summary:hover { color: var(--text); }
.help-content {
  margin-top: 12px;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px 24px;
}
.help-content h4 {
  font-size: 11px;
  font-weight: 600;
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 4px;
}
.help-content p { color: var(--text); line-height: 1.5; }
@media (max-width: 639px) {
  .help-content { grid-template-columns: 1fr; }
}
```

- [ ] **Step 5: Add @import**

App.css 에 `@import './styles/components/help-panel.css';` 추가.

- [ ] **Step 6: Run PASS + Commit**

```bash
pnpm test -- src/components/HelpPanel.test.tsx
git add src/components/HelpPanel.tsx src/components/HelpPanel.test.tsx src/styles/components/help-panel.css src/App.css
git commit -m "feat(client): add HelpPanel collapsible 도움말 (W3.2)"
```

---

# Wave 4 — 카드 통합 (가장 큰 작업)

## Task W4.1: StockHeroCard 통합 변경

**Files:**
- Modify: `src/components/StockHeroCard.tsx` (PRIMARY swap + USDT sub + 종가 변동률 + 한글 라벨 + InfoTip 4개)
- Modify: `src/components/StockHeroCard.test.tsx` (session fixture + 신규 케이스)
- Modify: `src/styles/components/stock-hero.css` (새 영역 스타일)

- [ ] **Step 1: Test fixture 마이그레이션 + 신규 케이스 작성**

`src/components/StockHeroCard.test.tsx` 의 `mkSession` 헬퍼 추가:

```tsx
import { describe, it, expect, afterEach } from 'vitest';
import { render, screen, cleanup } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import { StockHeroCard } from './StockHeroCard';
import type { TickerPayload, FxRates, SessionState } from '@shared/types/prices.js';

afterEach(() => cleanup());

const now = Date.now();
const fxOk: FxRates = { officialUsdKrw: 1471.7, usdtKrw: 1470, divergencePct: 0 };
const sessionOpen: SessionState = { krx: true, krxAfter: false, krxNight: false, nyseRegular: false, nysePrePost: false, cme: true, hyperliquid: true, binance: true, krxMinsUntilClose: 90 };
const sessionClosed: SessionState = { krx: false, krxAfter: true, krxNight: false, nyseRegular: false, nysePrePost: false, cme: true, hyperliquid: true, binance: true, krxMinsUntilOpen: 1020 };

const payloadFull: TickerPayload = {
  hl: { source: 'hyperliquid', symbol: 'xyz_SMSN', price: 189, unit: 'USD', change24hPct: -2.32, status: 'ok', asOf: now, receivedAt: now, schemaVersion: 1 },
  naver: { source: 'naver', symbol: '005930', price: 279000, unit: 'KRW', status: 'ok', asOf: now, receivedAt: now, schemaVersion: 1, previousClose: 281200, previousCloseSource: 'naver' },
  premium: { pctUsd: -0.85, pctUsdt: -0.8, guard: 'normal' },
};
```

신규 + 기존 테스트:

```tsx
describe('StockHeroCard v0.4.0', () => {
  // === 신규 — 동적 PRIMARY ===
  it('session open → primary=KRX, secondary=HL→KRW', () => {
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payloadFull} fx={fxOk} session={sessionOpen} />);
    // primary 279,000 (KRX)
    expect(screen.getByText(/₩279,000/, { selector: '.stock-hero-primary' })).toBeInTheDocument();
    // secondary (반대편) HL 277,830 = 189 * 1470
    expect(screen.getByText(/277,830/)).toBeInTheDocument();
  });

  it('session closed → primary=HL→KRW, secondary=KRX', () => {
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payloadFull} fx={fxOk} session={sessionClosed} />);
    expect(screen.getByText(/₩277,830/, { selector: '.stock-hero-primary' })).toBeInTheDocument();
    expect(screen.getByText(/279,000/)).toBeInTheDocument(); // 보조 KRX
  });

  // === 신규 — 종가 기준 변동률 ===
  it('shows pct-vs-close — open session, KRX vs previousClose', () => {
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payloadFull} fx={fxOk} session={sessionOpen} />);
    // (279000 - 281200) / 281200 = -0.78%
    expect(screen.getByText(/-0\.78%/)).toBeInTheDocument();
    expect(screen.getByText('종가 대비')).toBeInTheDocument();
  });

  it('shows "종가 데이터 없음" when previousClose undefined', () => {
    const payload = { ...payloadFull, naver: { ...payloadFull.naver!, previousClose: undefined } };
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payload} fx={fxOk} session={sessionOpen} />);
    expect(screen.getByText(/종가 데이터 없음/)).toBeInTheDocument();
  });

  // === 신규 — USDT sub-label ===
  it('shows USDT sub-label under PRIMARY', () => {
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payloadFull} fx={fxOk} session={sessionOpen} />);
    // primary KRX 279000 → /1470 ≈ $189.80 USDT
    expect(screen.getByText(/\$189\.80 USDT/)).toBeInTheDocument();
  });

  // === 신규 — 한글 라벨 ===
  it('uses Korean PREMIUM tier labels', () => {
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payloadFull} fx={fxOk} session={sessionOpen} />);
    // premium.pctUsd = -0.85 → tier cool → 잠잠
    expect(screen.getByText(/잠잠/)).toBeInTheDocument();
    // 영문 PREMIUM 누출 없음
    expect(screen.queryByText(/^PREMIUM$/)).toBeNull();
  });

  // === 신규 — InfoTip 4개 ===
  it('renders 4 InfoTips (KRX/Premium/Signal/HL terms)', () => {
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payloadFull} fx={fxOk} session={sessionOpen} />);
    const tips = screen.getAllByRole('button').filter((b) => b.classList.contains('info-tip'));
    expect(tips.length).toBeGreaterThanOrEqual(3); // 최소 3개 (signal 은 KRX closed 시 없을 수 있음)
  });

  // === 신규 — KRX stale mid-session → isStale UI ===
  it('shows ⚠️ icon when KRX stale during open hours', () => {
    const payload: TickerPayload = {
      ...payloadFull,
      naver: { ...payloadFull.naver!, status: 'stale' },
    };
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={payload} fx={fxOk} session={sessionOpen} />);
    expect(screen.getByText('⚠️')).toBeInTheDocument();
    expect(screen.getByText(/KRX 데이터 지연/)).toBeInTheDocument();
  });

  // === 기존 — loading ===
  it('renders loading skeleton when payload undefined', () => {
    render(<StockHeroCard ticker="samsung" label="삼성전자" payload={undefined} fx={fxOk} session={sessionOpen} />);
    expect(screen.getByTestId('stock-hero-loading')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run FAIL**

```bash
pnpm test -- src/components/StockHeroCard.test.tsx
```

- [ ] **Step 3: Rewrite StockHeroCard.tsx**

```tsx
import { useEffect, useRef, useState } from 'react';
import type { TickerPayload, FxRates, SessionState } from '@shared/types/prices.js';
import { fmtKrw, fmtUsd, fmtPct } from '../lib/format';
import { pickPrimary, pctVsClose } from '../lib/market-state';
import { PREMIUM_TIER_LABEL, SIGNAL_TIER_LABEL } from '../lib/labels';
import { PremiumGauge } from './PremiumGauge';
import { SignalBadge } from './SignalBadge';
import { InfoTip } from './InfoTip';

type Props = {
  ticker: string;
  label: string;
  payload?: TickerPayload;
  fx?: FxRates;
  session?: SessionState;
};

function premiumTier(pct: number | null | undefined): 'cool' | 'warm' | 'hot' | 'na' {
  if (pct === null || pct === undefined) return 'na';
  const a = Math.abs(pct);
  if (a < 1) return 'cool';
  if (a < 3) return 'warm';
  return 'hot';
}

const TOOLTIP = {
  premium: { term: '프리미엄', description: '한국 주식이 HL 야간 시장보다 얼마나 비싼지 (%). 양수=KRX 비쌈.' },
  signal: { term: 'Z-스코어 신호', description: '최근 100개 샘플 기준 프리미엄 표준편차. ±1 정상, ±2 관찰, ±3 매매.' },
  hl: { term: 'HL', description: 'Hyperliquid xyz 무기한 선물 — 한국 주식 24시간 거래 탈중앙 거래소.' },
  krx: { term: 'KRX', description: '한국거래소 — 평일 09:00–15:30, 실제 한국 주식 거래소.' },
};

export function StockHeroCard({ ticker, label, payload, fx, session }: Props) {
  const hl = payload?.hl;
  const [flashClass, setFlashClass] = useState<'' | 'flash-up' | 'flash-down'>('');
  const lastPriceRef = useRef<number | null>(null);

  // PRIMARY swap 적용 (장중=KRX, 마감=HL)
  // session 미정 시 기본 sessionClosed 동작 (안전)
  const safeSession: SessionState = session ?? { krx: false, krxAfter: true, krxNight: false, nyseRegular: false, nysePrePost: false, cme: true, hyperliquid: true, binance: true };
  const sel = payload
    ? pickPrimary({ session: safeSession, hl, krx: payload.naver, fx })
    : { primary: null, secondary: null, contextLabel: '데이터 없음' };

  const primaryValue = sel.primary?.value ?? null;
  const primaryUnit = sel.primary?.unit;
  const primaryDisplay = primaryValue !== null
    ? (primaryUnit === 'KRW' ? fmtKrw(primaryValue, 0) : fmtUsd(primaryValue))
    : '—';

  // USDT 환산 (primary 가 KRW 면 / usdtKrw, USD 면 그대로)
  const usdtKrw = fx?.usdtKrw ?? 0;
  const usdtValue = primaryValue !== null && primaryUnit === 'KRW' && usdtKrw > 0
    ? primaryValue / usdtKrw
    : primaryUnit === 'USD' ? primaryValue : null;

  // Flash on primary change
  useEffect(() => {
    if (primaryValue === null) return;
    const prev = lastPriceRef.current;
    if (prev !== null && prev !== primaryValue) {
      setFlashClass(primaryValue > prev ? 'flash-up' : 'flash-down');
      const t = setTimeout(() => setFlashClass(''), 800);
      lastPriceRef.current = primaryValue;
      return () => clearTimeout(t);
    }
    lastPriceRef.current = primaryValue;
  }, [primaryValue]);

  // 종가 변동률
  const previousClose = payload?.naver?.previousClose;
  const pctClose = pctVsClose(primaryUnit === 'KRW' ? primaryValue : null, previousClose);
  const pctCloseClass = pctClose === null ? '' : pctClose > 0 ? 'change-up' : pctClose < 0 ? 'change-down' : 'change-flat';
  const pctArrow = pctClose === null ? '—' : pctClose > 0 ? '▲' : pctClose < 0 ? '▼' : '—';

  if (!payload) {
    return (
      <article className="card stock-hero card-loading" data-testid="stock-hero-loading">
        <header className="card-head">
          <h3 className="ts-card-title">{label}</h3>
          <span className="ts-subtitle">{ticker}</span>
        </header>
        <div className="stock-hero-primary">—</div>
      </article>
    );
  }

  const premium = payload.premium;
  const premiumPct = hl ? (premium?.pctUsd ?? null) : null;
  const tier = premiumTier(premiumPct);
  const krxClosed = payload.naver?.status === 'stale';
  const isStale = sel.primary?.isStale;

  return (
    <article className="card stock-hero">
      <header className="card-head">
        <h3 className="ts-card-title">{label}</h3>
        <div className="ts-subtitle">
          {ticker} ·{' '}
          <InfoTip {...TOOLTIP.krx} />
          {' KRX × '}
          <InfoTip {...TOOLTIP.hl} />
          {' Hyperliquid'}
        </div>
        {hl && <span className="live-dot" title="HL live">●live</span>}
      </header>

      {/* PRIMARY 가격 */}
      <div className={`stock-hero-primary ts-hero-price ${flashClass}`}>
        {primaryDisplay}
        {isStale && <span className="stock-hero-stale-icon">⚠️</span>}
      </div>
      <div className="stock-hero-primary-label ts-subtitle">
        {usdtValue !== null && `≈ ${fmtUsd(usdtValue)} USDT  ·  `}
        {sel.contextLabel}
      </div>

      {/* 종가 기준 변동률 */}
      <div className="stock-hero-pct-close">
        {pctClose === null ? (
          <span className="pct-close-none">— <span className="pct-close-label">종가 데이터 없음</span></span>
        ) : (
          <>
            <span className={`pct-close-value ${pctCloseClass}`}>
              {pctArrow} {fmtPct(pctClose)}
            </span>
            <span className="pct-close-label">종가 대비</span>
          </>
        )}
      </div>

      {/* Premium gauge */}
      <div className="stock-hero-secondary">
        <PremiumGauge pctUsd={premiumPct} tier={tier} />
        <div className="stock-hero-premium-label ts-subtitle">
          프리미엄 {PREMIUM_TIER_LABEL[tier]} <InfoTip {...TOOLTIP.premium} />
        </div>
      </div>

      {/* 보조 가격 + 24h */}
      <dl className="stock-hero-tertiary ts-meta">
        {sel.secondary && (
          <div>
            <dt>{sel.secondary.source === 'hl' ? 'HL' : 'KRX'}</dt>
            <dd>{fmtKrw(sel.secondary.value, 0)}</dd>
          </div>
        )}
        <div>
          <dt>24h</dt>
          <dd className={hl?.change24hPct == null ? '' : hl.change24hPct > 0 ? 'change-up' : 'change-down'}>
            {hl?.change24hPct == null ? '—' : fmtPct(hl.change24hPct)}
          </dd>
        </div>
      </dl>

      {/* Signal */}
      {premiumPct !== null && !krxClosed && (
        <footer className="stock-hero-footer">
          <SignalBadge ticker={ticker} currentPct={premiumPct} />
          <InfoTip {...TOOLTIP.signal} />
        </footer>
      )}
      {krxClosed && !isStale && (
        <footer className="stock-hero-footer">
          <span className="signal-stale">KRX 마감</span>
        </footer>
      )}
    </article>
  );
}
```

- [ ] **Step 4: CSS 신규 영역 추가**

`src/styles/components/stock-hero.css` 끝에 추가:

```css
/* === v0.4.0 — 종가 변동률 + USDT sub + stale icon === */
.stock-hero-pct-close {
  display: flex;
  align-items: baseline;
  gap: 8px;
  border-top: 1px solid var(--border-subtle);
  padding-top: 8px;
}
.pct-close-value {
  font-size: 28px;
  font-weight: 600;
  font-variant-numeric: tabular-nums;
}
.pct-close-label {
  font-size: 11px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.pct-close-none { color: var(--text-muted); }
.stock-hero-stale-icon { font-size: 14px; margin-left: 8px; }

@media (max-width: 639px) {
  .stock-hero-tertiary { display: none; }
  .stock-hero { padding: 16px 12px; min-height: auto; }
  .info-tip-popover { max-width: calc(100vw - 32px); }
}
```

- [ ] **Step 5: Run PASS**

```bash
pnpm exec tsc --noEmit -p tsconfig.app.json
pnpm test -- src/components/StockHeroCard.test.tsx
```

- [ ] **Step 6: Commit**

```bash
git add src/components/StockHeroCard.tsx src/components/StockHeroCard.test.tsx src/styles/components/stock-hero.css
git commit -m "feat(client): StockHeroCard v0.4.0 — dynamic PRIMARY + USDT sub + 종가 변동률 + 한글 + InfoTip x4 (W4.1)"
```

## Task W4.2: IndexCompactCard USDT + 한글

**Files:**
- Modify: `src/components/IndexCompactCard.tsx`
- Modify: `src/components/IndexCompactCard.test.tsx`
- Modify: `src/styles/components/index-compact.css`

- [ ] **Step 1: 변경**

`IndexCompactCard.tsx` 의 headline 아래에 USDT sub 추가:

```tsx
{showKrwHeadline && (
  <>
    <div className="index-compact-headline ts-index-headline">{fmtKrw(headlineKrw!, 0)}</div>
    {fxAvailable && (
      <div className="index-compact-usdt ts-subtitle">
        ≈ {fmtUsd(headlineKrw! / usdtKrw)} USDT
      </div>
    )}
  </>
)}
```

CSS 에 `.index-compact-usdt { font-size: 11px; color: var(--text-dim); margin-top: 2px; }` 추가.

- [ ] **Step 2: Test fixture 갱신 (영문 PREMIUM 라벨 검색 → 한글)**

이미 v0.3.0 테스트가 PREMIUM 영문 사용 안 함. 신규 USDT 테스트만 추가:

```tsx
it('renders USDT sub-label below KRW headline', () => {
  render(<IndexCompactCard ticker="ewy" label="EWY" payload={payloadEwy} fx={fxOk} />);
  // 193.16 USD = 283,945 KRW = 193.16 USDT (USD ≈ USDT)
  expect(screen.getByText(/≈ \$193\.16 USDT/)).toBeInTheDocument();
});
```

- [ ] **Step 3: Run + Commit**

```bash
pnpm test -- src/components/IndexCompactCard.test.tsx
git add src/components/IndexCompactCard.tsx src/components/IndexCompactCard.test.tsx src/styles/components/index-compact.css
git commit -m "feat(client/index): USDT sub-label + 한글 라벨 (W4.2)"
```

## Task W4.3: SignalBadge 한글 매핑

**Files:**
- Modify: `src/components/SignalBadge.tsx`

- [ ] **Step 1: 라벨 매핑 적용**

`SignalBadge.tsx` 의 `tierFromZScore` / `tierFromAbs` 함수에서 영문 label → 한글:

```tsx
import { SIGNAL_TIER_LABEL } from '../lib/labels';

function tierFromZScore(z: number): Tier {
  const a = Math.abs(z);
  if (a < 1) return { label: SIGNAL_TIER_LABEL.normal, className: 'sig-normal' };
  if (a < 2) return { label: SIGNAL_TIER_LABEL.watch, className: 'sig-watch' };
  if (a < 3) return { label: SIGNAL_TIER_LABEL.trade, className: 'sig-trade' };
  return { label: SIGNAL_TIER_LABEL.dislocated, className: 'sig-dislocated' };
}

function tierFromAbs(pct: number): Tier {
  const a = Math.abs(pct);
  if (a < 1) return { label: SIGNAL_TIER_LABEL.normal, className: 'sig-normal' };
  if (a < 3) return { label: SIGNAL_TIER_LABEL.watch, className: 'sig-watch' };
  if (a < 5) return { label: SIGNAL_TIER_LABEL.trade, className: 'sig-trade' };
  return { label: SIGNAL_TIER_LABEL.dislocated, className: 'sig-dislocated' };
}
```

기존 `'정상'` 인라인 한글 → `SIGNAL_TIER_LABEL.normal` 로 매핑 (영문 WATCH/TRADE/DISLOCATED 도).

- [ ] **Step 2: Commit**

```bash
git add src/components/SignalBadge.tsx
git commit -m "refactor(client/signal): use SIGNAL_TIER_LABEL mapping for 한글 (W4.3)"
```

## Task W4.4: SpreadRow 한글 + InfoTip

**Files:**
- Modify: `src/components/SpreadRow.tsx`

- [ ] **Step 1: 라벨 변경**

```tsx
import { InfoTip } from './InfoTip';

// "Max venue spread" → "거래소 간 격차 최대"
return (
  <div className={`spread-row ${isLarge ? 'spread-warn' : ''}`}>
    <span className="spread-label">거래소 간 격차 최대 <InfoTip term="거래소 간 격차" description="여러 거래소(HL/Binance/Yahoo) 가격 차이. 큰 격차는 차익 기회 또는 데이터 이상." /></span>
    <span className="spread-value">{fmtPct(spread.maxPctDiff)}</span>
    {/* ... */}
  </div>
);
```

- [ ] **Step 2: Commit**

```bash
git add src/components/SpreadRow.tsx
git commit -m "refactor(client/spread): 한글 label + InfoTip (W4.4)"
```

## Task W4.5: Codex #2 체크포인트 — Wave 4 종료

- [ ] **Step 1: 사전 검증**

```bash
pnpm build && pnpm test
# 영문 라벨 누출 검증
grep -rnE "PREMIUM\b|^COOL$|^WARM$|^HOT$|^WATCH$|^TRADE$|^DISLOCATED$|KRX CLOSED" src/components/ | grep -v "test\|premium-row\|sig-" | head -20
```

- [ ] **Step 2: Codex #2 호출**

```
Agent: codex:codex-rescue
prompt: |
  Mode: REVIEW ONLY. Wave 4 of v0.4.0 PLAN complete (W4.1-W4.4).
  Files: StockHeroCard.tsx, IndexCompactCard.tsx, SignalBadge.tsx, SpreadRow.tsx + tests.
  Spec: (spec)/2026-05-12-v0.4.0-beginner-friendly-design.md §3-§5.

  Verify per spec:
  1. pickPrimary 6 cases (C1-C6) all rendered correctly
  2. isStale ⚠️ icon + KRX 데이터 지연 contextLabel (§3.6)
  3. 종가 변동률 polish (▲▼ + 색 + 종가 데이터 없음 fallback)
  4. USDT sub-label format "≈ $X.XX USDT · {context}"
  5. 한글 라벨 grep 0 영문 누출 (PREMIUM/COOL/WARM/HOT 카드 노출 없음)
  6. InfoTip 4 사용 (StockHeroCard) + 1 사용 (SpreadRow)
  7. v0.3.0 테스트 마이그레이션 — session fixture 추가 후 모두 pass

  [카파시 의무 검증]

  Output: BLOCK / FLAG / PASS.
```

Expected: PASS. BLOCK 시 fix.

---

# Wave 5 — App.tsx + 모바일

## Task W5.1: App.tsx — 부제목 + HelpPanel + 카운트다운 chip

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 부제목 변경 + HelpPanel + 카운트다운 통합**

`src/App.tsx` 전체 교체:

```tsx
import { usePrices } from './hooks/usePrices';
import { StockHeroCard } from './components/StockHeroCard';
import { IndexCompactCard } from './components/IndexCompactCard';
import { FxHeader } from './components/FxHeader';
import { SessionBadges } from './components/SessionBadges';
import { DegradedBanner } from './components/DegradedBanner';
import { ThemeToggle } from './components/ThemeToggle';
import { HelpPanel } from './components/HelpPanel';
import { getStateBadge } from './lib/market-state';
import { STATE_BADGE_LABEL } from './lib/labels';
import './App.css';

const STOCK_TICKERS = [
  { ticker: 'samsung',  label: '삼성전자' },
  { ticker: 'skhynix',  label: 'SK하이닉스' },
  { ticker: 'hyundai',  label: '현대차' },
];

const INDEX_TICKERS = [
  { ticker: 'ewy',       label: 'EWY' },
  { ticker: 'sp500',     label: 'S&P 500' },
  { ticker: 'nq',        label: 'Nasdaq 100' },
  { ticker: 'kospi200f', label: 'KOSPI 200F' },
];

function formatMins(mins: number): string {
  if (mins < 60) return `${mins}분`;
  const h = Math.floor(mins / 60);
  const m = mins % 60;
  return m === 0 ? `${h}시간` : `${h}시간 ${m}분`;
}

export default function App() {
  const { data, error, isLoading } = usePrices();
  const ts = data?.ts;
  const badge = data?.session ? getStateBadge(data.session) : null;
  const badgeLabel = badge ? STATE_BADGE_LABEL[badge] : null;
  const mins = data?.session
    ? badge === 'open' ? data.session.krxMinsUntilClose
    : badge === 'night' ? data.session.krxMinsUntilOpen
    : undefined
    : undefined;
  const countdownSuffix = mins !== undefined
    ? ` (${badge === 'open' ? '마감' : '개장'}까지 ${formatMins(mins)})`
    : '';

  return (
    <div className="container">
      <header className="app-header">
        <div className="brand">
          <span className="logo-dot" aria-hidden />
          <div>
            <h1>24시간 코스피</h1>
            <div className="sub">한국 주식의 24시간 거래소(HL) 가격과 KRX 종가 비교 · v0.4.0</div>
          </div>
        </div>
        <div className="meta">
          <FxHeader fx={data?.fx} />
          {badge && (
            <span className={`market-countdown state-${badge}`} aria-live="polite">
              {badgeLabel}{countdownSuffix}
            </span>
          )}
          <SessionBadges session={data?.session} />
          <div className="meta-controls">
            <ThemeToggle />
            {error ? (
              <span className="err-pill">⚠ {(error as Error).message}</span>
            ) : isLoading ? (
              <span className="muted">loading…</span>
            ) : ts ? (
              <span className="muted num">↻ {new Date(ts).toLocaleTimeString('ko-KR')}</span>
            ) : null}
          </div>
        </div>
      </header>

      <DegradedBanner sourceHealth={data?.sourceHealth} />

      <HelpPanel />

      <section className="stocks-section">
        <h2 className="section-title">
          한국 주식 <span className="section-subtitle">KRX × Hyperliquid</span>
        </h2>
        <div className="bento-3up">
          {STOCK_TICKERS.map(({ ticker, label }) => (
            <StockHeroCard
              key={ticker}
              ticker={ticker}
              label={label}
              payload={data?.tickers?.[ticker]}
              fx={data?.fx}
              session={data?.session}
            />
          ))}
        </div>
      </section>

      <section className="indices-section">
        <h2 className="section-title">
          지수 / ETF <span className="section-subtitle">Multi-venue</span>
        </h2>
        <div className="bento-4up">
          {INDEX_TICKERS.map(({ ticker, label }) => (
            <IndexCompactCard
              key={ticker}
              ticker={ticker}
              label={label}
              payload={data?.tickers?.[ticker]}
              fx={data?.fx}
            />
          ))}
        </div>
      </section>
    </div>
  );
}
```

- [ ] **Step 2: `.market-countdown` CSS 추가**

`src/styles/components/fx-header.css` 끝에:

```css
/* === v0.4.0 — 시장 상태 카운트다운 chip === */
.market-countdown {
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.04em;
  font-variant-numeric: tabular-nums;
  background: var(--neutral-bg);
  border: 1px solid var(--neutral-border);
  color: var(--text);
}
.market-countdown.state-open { background: var(--up-bg); border-color: var(--up-border); color: var(--up); }
.market-countdown.state-night { background: var(--hanji-soft); border-color: var(--hanji-border); color: var(--hanji); }
.market-countdown.state-holiday { background: var(--down-bg); border-color: var(--ju-red-border); color: var(--ju-red); }
```

- [ ] **Step 3: 빌드 + Commit**

```bash
pnpm exec tsc --noEmit -p tsconfig.app.json
pnpm test
git add src/App.tsx src/styles/components/fx-header.css
git commit -m "feat(client/app): subtitle + HelpPanel + countdown chip + session prop (W5.1)"
```

## Task W5.2: a11y.test.tsx 업데이트

**Files:** Modify `src/tests/a11y.test.tsx`

- [ ] **Step 1: stubData 갱신** — `session.krxMinsUntilOpen: 1020` 추가 + `naver.previousClose: 281200, previousCloseSource: 'naver' as const` 추가
- [ ] **Step 2: 신규 describe 추가** — `session.krx = true` (장중) 모드 axe 검증
- [ ] **Step 3: Run + Commit**

```bash
pnpm test -- src/tests/a11y.test.tsx
git add src/tests/a11y.test.tsx
git commit -m "test(a11y): session.krx open mode + previousClose stub (W5.2)"
```

---

# Wave 6 — PremiumRow 정리

## Task W6.1: PremiumRow.tsx 삭제

**Files:** Delete `src/components/PremiumRow.tsx`

- [ ] **Step 1: 소비자 검증**

```bash
grep -rn "from.*PremiumRow" src/ --include='*.tsx' --include='*.ts'
grep -rn "import.*PremiumRow" src/ --include='*.tsx' --include='*.ts'
```
Expected: 0 hits (StockHeroCard W4.1 에서 직접 premium 렌더).

- [ ] **Step 2: 삭제 + 빌드 + Commit**

```bash
rm src/components/PremiumRow.tsx
pnpm build && pnpm test
git rm src/components/PremiumRow.tsx
git commit -m "refactor(client): remove PremiumRow.tsx (W6.1)"
```

## Task W6.2: premium-row.css 정리

- [ ] **Step 1: CSS class 사용처 grep**

```bash
grep -rn "premium-row" src/components/ src/styles/
grep -rn "premium-label" src/components/ src/styles/
grep -rn "premium-value" src/components/ src/styles/
```
모두 0 → `premium-row.css` 삭제 + App.css 의 해당 @import 제거.
일부 hits → 해당 selector 만 보존.

- [ ] **Step 2: 빌드 + Commit**

```bash
pnpm build && pnpm test
git add src/App.css
git rm src/styles/components/premium-row.css 2>/dev/null || git add src/styles/components/premium-row.css
git commit -m "chore(css): cleanup premium-row.css (W6.2)"
```

## Task W6.3: Codex #3 체크포인트

- [ ] **Step 1: 카파시 grep 가드**

```bash
# 1-use 헬퍼 카운트
grep -rn "^function " src/lib/ src/components/ | wc -l

# market-state.ts consumers (>= 2)
grep -rn "from.*market-state" src/

# PremiumRow 0 hits
grep -rn "from.*PremiumRow" src/
grep -rn "premium-row" src/components/ src/styles/

# 영문 라벨 누출 — 개별 토큰 단위로 분리 grep
grep -rn "^PREMIUM$" src/components/ --include='*.tsx' | grep -v test
grep -rn ">COOL<\|>WARM<\|>HOT<" src/components/ --include='*.tsx' | grep -v test
grep -rn ">WATCH<\|>TRADE<\|>DISLOCATED<" src/components/ --include='*.tsx' | grep -v test
grep -rn "KRX CLOSED" src/components/ --include='*.tsx' | grep -v test
```

- [ ] **Step 2: Codex #3 호출**

```
Agent: codex:codex-rescue
prompt: |
  Mode: REVIEW ONLY. Wave 6 of v0.4.0 PLAN complete.
  W6.1 (PremiumRow.tsx delete) + W6.2 (premium-row.css cleanup).

  Verify:
  1. PremiumRow JSX 사용처 0
  2. .premium-row* CSS class 0
  3. market-state.ts 2+ consumers
  4. 1-use 헬퍼 추가 없음
  5. 영문 라벨 (PREMIUM, COOL, WARM, HOT, WATCH, TRADE, DISLOCATED) 누출 0

  [카파시 의무 검증]
  Output: BLOCK / FLAG / PASS.
```

---

# Wave 7 — 검증 + smoke

## Task W7.1: 4-state 스크린샷 + 모바일

브라우저 `http://localhost:5173` 에서 4 state 캡처:
1. 다크 + 장중 → `(base)/v0.4.0-dark-open.png`
2. 라이트 + 장중 → `(base)/v0.4.0-light-open.png`
3. 다크 + 마감 → `(base)/v0.4.0-dark-closed.png`
4. 라이트 + 마감 → `(base)/v0.4.0-light-closed.png`
5. 모바일 375px → `(base)/v0.4.0-mobile-375.png`

```bash
git add docs/superpowers/specs/baseline/
git commit -m "chore(docs): v0.4.0 4-state + mobile screenshots (W7.1)"
```

## Task W7.2: 최종 검증

- [ ] **Step 1: spec §8 13 items 자동 체크**

```bash
echo "1+2. build/test:" && pnpm build 2>&1 | tail -2 && pnpm test 2>&1 | grep "Tests "
echo "5. axe critical:" && pnpm test -- src/tests/a11y.test.tsx 2>&1 | grep -E "Tests "
echo "7. nightMode (expect 0):" && grep -rn "nightMode" src/ | wc -l
echo "9. PremiumRow (expect 0):" && (ls src/components/PremiumRow.tsx 2>/dev/null || echo "deleted")
echo "10. ts-* defined:" && grep -oE '\.ts-[a-z-]+' src/styles/typography.css | sort -u
echo "10. ts-* used:"    && grep -roE 'ts-[a-z-]+' src/components/ | sed 's/^[^:]*://' | sort -u
```

- [ ] **Step 2: 사용자 smoke 요청**

브라우저에서 11개 시나리오 확인 (PLAN 본문 참조).

## Task W7.3: Codex #4 — final v0.4.0

```
Agent: codex:codex-rescue
prompt: |
  Mode: REVIEW ONLY. Pre-merge final v0.4.0 review.
  Spec §8 13 items verification.
  4-state screenshots + mobile 375px height ≤ 380px.

  [카파시 의무 검증]
  Output: BLOCK / FLAG / PASS.
```

Expected: PASS. BLOCK 시 fix → 재호출.

```bash
git add -A
git commit -m "chore(docs): v0.4.0 final smoke"
```

사용자에게 v0.4.0 머지/태그 결정 위임.

---

## Wave 종료별 검증 매트릭스

| Wave | 종료 | build | test | Codex |
|---|---|---|---|---|
| W1 (서버) | W1.4 끝 | ✓ | ✓ | **#1** |
| W2 (lib) | W2.2 끝 | ✓ | ✓ market-state 13 tests | — |
| W3 (UI 신규) | W3.2 끝 | ✓ | ✓ InfoTip + HelpPanel | — |
| W4 (카드) | W4.4 끝 | ✓ | ✓ v0.3.0 마이그레이션 | **#2** |
| W5 (App + 모바일) | W5.2 끝 | ✓ | ✓ a11y open mode | — |
| W6 (정리) | W6.2 끝 | ✓ | ✓ + grep 가드 | **#3** |
| W7 (검증) | W7.3 끝 | ✓ | ✓ + 4-state + 모바일 | **#4** |

---

## Open Decisions

1. **PR target**: v0.3.0 와 같은 브랜치 (`feature/dashboard-redesign-v0.3.0`) 에 추가 vs 새 브랜치 (`feature/v0.4.0-beginner-friendly`). 추천: **새 브랜치** (v0.4.0 별도 PR 추적).
2. **Phase 분리 vs 단일 머지**: Wave 1 (서버) 만 별개 PR 가능. 추천: **통합 머지** (서버 변경이 옵셔널 필드라 위험 낮음).
3. **Execution mode**: subagent-driven (v0.3.0 처럼) vs inline.

---

## References

- Spec: `(spec)/2026-05-12-v0.4.0-beginner-friendly-design.md` (commit `cb4b1d6`)
- v0.3.0 PLAN: `(plan)/2026-05-12-dashboard-redesign.md`
- 사용자 결정 (Q1-Q5): A / A / C / all / d

